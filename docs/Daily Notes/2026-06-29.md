# 2026-06-29 — Daily Note

## Work Done

- Synced repo: rebased local `deleted old doc` commit on top of 20 remote commits from merged PR #4 (workspace switching, HA, Automations/n8n, Team module, Goals route, server-side shortcuts)

- Skills restructure:
  - Created `docs/skills/` with `diagnose/`, `run-tests/`, `run-agent/`, and `README.md`
  - Moved all three dev tool skills from `agent/skills/` → `docs/skills/`
  - Rewrote `agent/README.md` to document the in-app AI agent architecture (modes, full tool registry, brain skills)
  - Created `agent/skills/life-priorities/README.md` as a pointer to `brain/skills/life-priorities/skill.md`

- Docs audit:
  - Fixed `docs/MEMORY.md`: added `team` to the module IDs list (was missing; AGENTS.md had it correctly)
  - Updated `docs/AGENTS.md` "Agent Skills" section → renamed to "Dev Skills" + new "Brain Skills" section
  - Updated `docs/MAP.md`: agent/ block and docs/ block both reflect new structure

## Decisions Made

- Dev tools (Claude Code use) live in `docs/skills/` — accessible alongside docs, clear purpose
- `agent/` folder now focuses purely on the in-app LogCore AI agent architecture
- Brain skills stay in `brain/skills/` (in-app AI reads them at runtime); `agent/skills/` only holds thin pointer READMEs
- `team` module ID was the only docs gap found post-merge — the PR already included comprehensive doc updates

## Follow-up

- None — docs and structure are clean
