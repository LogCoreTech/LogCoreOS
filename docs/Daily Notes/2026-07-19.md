# 2026-07-19 — Daily Note

## Owner infra change — demo split from personal instance

Owner restructured the deployments (done outside Claude sessions, reported in):

- **Demo instance is now its own VPS**, served through the existing tunnel/public hostname
  (the one previously pointing at the dev box). The dev machine no longer doubles as the demo.
- **Personal/family instance moved to its own separate tunnel** — fully self-hosted, private,
  no longer exposed as the demo target. (Hostname/infra details stay out of this public repo.)
- **No AI API key on the demo yet — deliberate.** Owner is holding the key until the legal
  pages (privacy policy + ToS) are live. Note: the key should also wait for the per-user daily
  message cap (see the AI usage counter TOP PRIORITY task) — legal AND cost protection are both
  gates.
- **Privacy policy + ToS promoted to high priority** — now the active blockers for the demo
  going public.

Remaining demo hardening (launch-surface order in TASKS.md): legal pages → Haiku + per-user
daily message cap → daily reset script → demo banner → UptimeRobot → off-site backups.
Verify open registration stays OFF until legal pages + reset + banner are live.

## Session — cross-module linking round 2 (full circle)

Continuation of the 2026-07-18 linking session; owner interviewed on the round-1 gaps, plan
approved (`~/.claude/plans/set-the-plan-file-dreamy-duckling.md`). Note: several round-1
complaints were the **stale-backend symptom** (frontend dist/ is volume-mounted and went live
instantly; the backend image was never rebuilt) — rounds 1+2 deploy together.

- **Invoices**: `deal_id` on the invoice (public `InvoiceCreate` field; a deal bills many
  invoices — `list_invoices_for_deal()` scan + `GET /finance/deals/{id}/invoices`, contribute
  books without `see_balances` skipped); free **status dropdown** (modal header + list row,
  rows converted button→div for valid nesting); **source-deal context strip** (deal/contact
  links, asset chips, asset-expense total). Legacy `deal.invoice_id` no longer written.
- **Payments**: linked tx carries `deal_id` + auto-links the deal's **single** linked asset
  (`_deal_single_asset`, try/except-guarded); the UI captures the returned invoice's newest
  payment `tx_id` and **opens TransactionModal on it** immediately.
- **Transactions**: internal `deal_id` field (never on public tx models); source chips
  ("From invoice / Deal: X") in the tx modal.
- **Deals**: expanded panel = job dashboard — invoice rows + **Job P&L** (invoiced/collected/
  asset-expenses/net, computed server-side); asset chips click through.
- **Assets**: `contact` template field type (ContactPicker editor, name+jump-link in AssetView,
  no cross-store existence check); `GET /assets/by-contact/{id}` reverse lookup
  (`list_assets_for_contact` filters `list_visible` by contact-type fields); AssetView finance
  section now shows **Income/Expenses/Net**.
- **Contacts**: `?contact=` deep link; **References card** (payee totals + outstanding,
  referencing assets, invoices w/ status+balance); `find_deal()` (access inherits from parent
  contact) + `GET /contacts/deals/{id}`; contact finance view expanded (invoices list +
  per-deal Job P&L).
- **Verified**: 8 new tests, suite **451 green**, black clean, build clean. Docs updated
  (TASKS/MEMORY/API/AGENTS/CHANGELOG).
- **NOT committed / NOT deployed** — owner runs commit + `bash launch.sh --skip-build`
  (rounds 1+2 land together; re-test the round-1 items after deploy per the interview).
