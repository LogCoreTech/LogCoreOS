# 2026-07-02 — Daily Note

## Carry-over from 2026-07-01

All work from yesterday's final session (session 8) was committed locally as `d122a23` but not yet pushed. Push when ready:

```bash
GIT_SSH_COMMAND="ssh -i /home/logcore/.ssh/logcore_github" git push origin master
```

## Current State

App is running and stable at http://localhost:8000. No active tasks in flight. Backlog is in TASKS.md.

## Work Done (session 4)

Docs improvements pass — no code changes.

**docs/hooks/docs_reminder.sh:**
- Added `CHANGELOG.md` to the hardcoded DOCS_UPDATED check
- Added `docker/` to the `find` scan so docker-compose.yml changes get detected
- Added 3 missing trigger rules: `constants.js` → AGENTS.md, `migrations/` → MEMORY.md + MAP.md, `docker-compose.yml` → PROJECT.md
- Added CHANGELOG.md suggestion when any app/ code changed this turn

**docs/MAP.md:**
- Added CHANGELOG.md, SECURITY.md, OWNER_SETUP.md, LICENSE to root listing
- Added TESTING.md under docs/
- Removed OWNER_SETUP.md from docs/ section (it was moved to root)

**docs/AGENTS.md:**
- Added Glossary section (10 terms: Brain, workspace, module, feature role, pool, pseudo-user, stub file, workspace-keyed dict, JTI, ws_path)
- Condensed Testing section to link to new TESTING.md

**New files:**
- `docs/TESTING.md` — full testing guide: brain fixture, 11 test files × 175 tests, coverage gaps, targets
- `SECURITY.md` at root — vulnerability disclosure policy, scope, security design notes

**docs/README.md:** Added TESTING.md row to the table.

---

## Work Done (session 3)

Hook fixes — no code changes.

**docs_reminder.sh:** DOCS_UPDATED check was hardcoded to only 3 files (TASKS.md, MEMORY.md, daily note). Updates to API.md, MAP.md, AGENTS.md, PROJECT.md didn't count — reminder fired on the next turn even after a full docs pass. Fixed: loop now checks all 7 core docs files.

**docs_loader.sh:** Added MAP.md, API.md, PROJECT.md to the session-start Read list. These three add ~15,000 tokens (~60KB) at session start. Trade-off accepted: full context every session.

**AGENTS.md:** Updated line noting that docs_loader.sh now handles all 5 session-start files + API.md (was "steps 1–3 + this file").

---

## Work Done (session 2)

Full docs overhaul — no code changes.

**AGENTS.md:**
- Added `goals` to Valid module IDs list (was missing, caught by MEMORY.md cross-check)
- Added `PoolPrioritiesCard` to Admin render order (added 2026-07-01, not reflected)
- Added `GET /chat/runs` and `GET /chat/runs/{run_id}` to chat API table

**API.md:**
- `GET /tasks/assigned` endpoint added
- `GET /priorities/pool` and `PUT /priorities/pool` endpoints added
- Chat section completely rewritten: now documents all 7 endpoints with correct request fields (mode, history, workspace, cross_workspace)
- Added Automations (n8n) section — all 11 endpoints
- Added Smart Home (Home Assistant) section — all 12 endpoints
- Added Admin — Infisical section
- Added Admin — Feature Roles section

**PROJECT.md:**
- Scheduler table updated: added goal_drift, JTI cleanup, and custom jobs (was only showing 4 of 7 jobs)
- Docker services: added `logcore-n8n` (was missing since 2026-06-29)

---

## Work Done (session 1)

Docs update only — no code changes. Updated TASKS.md, MEMORY.md, and created this daily note to capture the 2026-07-01 session 8 work that was logged in yesterday's note but not reflected in TASKS.md or MEMORY.md.

**TASKS.md:** Added UI bug fixes entry to Done section (sign-out relocation, Brain back button, pool priorities keyboard fix, horizontal scroll fix).

**MEMORY.md — Known Gotchas added:**
- `overflow-y-scroll` vs `overflow-y-auto` on `<main>`: scroll required to prevent scrollbar-induced 4px layout shift
- Never define React components inside other components: causes unmount/remount on every parent render (keyboard dismissal, state reset)

**MEMORY.md — Key Decisions added:**
- Sign-out button belongs in sidebar + mobile drawer, not Settings page
- Brain editor `/brain` shows ← Settings back button in file list view
