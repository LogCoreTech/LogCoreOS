# 2026-07-13 — Daily Note

## Session — v0.2.0 release stamp

- Stamped the **v0.2.0 release** ("The Assets Release"): `VERSION` bumped 0.1.0 → 0.2.0; CHANGELOG `[Unreleased]` split — everything shipped since v0.1.0 (Assets module, Automation Inbox, approve-edits chat default, goals split, fixes) moved under `[0.2.0] — 2026-07-13`; the **Finance entries (Phases A + B) stay under `[Unreleased]`** for their own release later.
- Stamp commit deliberately contains ONLY `VERSION`, `CHANGELOG.md`, and this note — the Finance code (Phases A + B) remains uncommitted in the working tree so the `v0.2.0` tag ships zero finance code. **Commit finance only after the release is published.**
- Tag `v0.2.0` created at the stamp commit and pushed. Release description written (drawn from the CHANGELOG, finance excluded) — owner pastes it when publishing the GitHub release against the existing tag.

## Session — Finance module Phase C (budgets, recurring, forecasting, deviation alerts)

Continuation of the finance build (plan approved 07-12; A + B shipped yesterday).

### Shipped (suite **364 green**, black clean, frontend build clean)

- **`finance_planning_service.py`** + `routers/finance_planning.py` (same `/api/v1/finance` prefix):
  - **Budgets** (`budgets.json` per book): monthly limit per category; status computed on read; alerts escalate none→warn (book `budget_warn_pct`, default 80%)→over, deduped via `alert_state[month]` (never re-fires after "over"). Own books notify the owner; pool books notify admins.
  - **Recurring bills/income** (`recurring.json`): weekly/monthly/yearly cadence, `advance_due()` clamps month-end (Jan 31 → Feb 28); **bill matching** on every landing transaction (manual/sync/CSV): same account+sign, amount ±max(3%, $2), date ±4 days of `next_due` → `last_paid` set, due advanced. Missed (3+ days late, unmatched) notified once per due date (`missed_notified_for`).
  - **Planned one-offs** (`planned.json`) with done check-off.
  - **Projection** — `project_balance()` pure function: current balance + expanded recurring occurrences + pending planned items to the target date, itemized. `GET .../accounts/{aid}/projection?date=`.
  - **Deviation alerts** (the fraud early-warning): per-account `deviation_threshold_cents` vs bank-reported `synced_balance_cents` (auto from SimpleFIN; PATCH account accepts a manual value for cash accounts); checked after every sync + nightly; deduped per day, re-alerts only if the delta moves >10%.
  - Hooks: `on_transactions_added()` (never raises) wired into manual add, SimpleFIN sync, CSV commit; deviation check wired into sync. New scheduler job `job_finance_nightly` (07:30) sweeps every user×workspace store + both pools.
  - All finance alerts carry action `{type:"open_finance_book", book_id}`; NotifBell + Finance page consume `?book=` (deep-link effect now also fires when already mounted).
- **AI tools**: `get_budget_status`, `get_balance_projection` (read-only, `_RESEARCH_TOOLS`).
- **Frontend**: Finance page gains **Budgets** and **Recurring** tabs (BudgetsPanel status bars + limit editor; RecurringPanel recurring CRUD/pause/missed badges + planned one-offs) and a **Projected balance** card on Overview (account + date picker, itemized forecast).
- Stale-book-dict fix at the service layer: planning functions re-load via new `finance_service.fresh_book()` (same bug class as Phase A's `_fresh_book`).

### Tests
14 new in `test_finance_planning.py` (budget math/escalation-dedup, pool audience, cadence clamps, bill match accept/reject, missed-once, projection expansion/cutoff, deviation fire/dedup/re-alert/skips, write-path hook). Suite **364 green**.

### Next
Phase D: invoices, clients/AR ("who paid, who's behind"), tax + year-end export, receipts, printable invoice. Then Phase E sharing.

## Session — Finance module Phase D (invoicing, clients/AR, tax, receipts)

### Shipped (suite **376 green**, black clean, frontend build clean)

- **`finance_invoice_service.py`** + `routers/finance_invoicing.py`: clients per book (`clients.json`; **reserved `contact_id` for the future CRM**, never populated; delete 409s while invoices exist); invoices (`invoices.json`) auto-numbered via `finance_service.next_invoice_number()` (book `invoice_prefix`+`invoice_seq`); `status` = user lifecycle only — subtotal/total/paid/balance/**overdue always derived** in `annotate_invoice()`; partial `payments[]` w/ optional **linked income transaction** (payee = client name, tx ↔ invoice/client ids); auto-flip to `paid` at zero balance, reopen on payment delete; `ar_summary()` per-client invoiced/paid/outstanding/overdue (worst first, last payment date), void excluded.
- **Receipts on transactions** (assets attachment pattern): `receipts/{tx_id}/{uuid}.{ext}`, MIME allowlist, 10 MB, ≤10/tx, metadata on the tx `attachments`, removed with the transaction; endpoints under `/transactions/{tid}/receipts`.
- **Reports**: `pnl()` year/quarter/month + `tax_summary()`/`tax_summary_csv()` (deductible flag + per-book `tax_categories`); **fixed a real date bug** — monthly ranges fabricated day 31 (`2026-02-31` → ValueError in Feb/Apr/etc.); new `month_end()` used by monthly report, P&L and budget status.
- **AI write tools** (approval-gated by default): `create_invoice`, `mark_invoice_paid` (defaults to remaining balance; can log the linked income tx).
- **Frontend**: Finance page gains **Invoices** (AR "who's behind" strip, client chips + modal, invoice list w/ status/overdue, line-item editor, record-payment w/ optional ledger logging, 🖨 print via `InvoicePrint` print-CSS) and **Reports** (P&L period picker, deductible summary + accountant CSV download) tabs; TransactionModal gains deductible + tax bucket + receipts upload/view/delete; BookSettings gains tax buckets + invoice prefix.
- 12 new tests in `test_finance_invoices.py` (numbering incl. prefix change, totals w/ tax, partial→auto-paid→reopen, linked tx attribution, derived overdue, validation, client-delete block, AR math incl. void exclusion, P&L periods, tax summary+CSV quoting, receipt lifecycle/validation/cleanup).

### Next
Phase E (final): asset-style sharing — book shares w/ handshake, pool contributors, per-account overrides, contribute caps + server-side balance stripping, finance share index, sharing UI, AI write tools `add_finance_transaction`/`categorize_transaction`.

## Session — Finance module Phase E (sharing) — MODULE COMPLETE

### Shipped (suite **390 green**, black clean, frontend build clean)

- **`_resolve_book_access` extended to the full model**, returning `(access, caps)`: personal ladder account-by-name > account-group > book-by-name > book-group; pool ladder admin > account-by-name > book-by-name > account-group > book-group > workspace read; edit>contribute>read within a rung, caps union across same-rung contribute entries; `hidden_from` (names + `role:<r>`) beats shares.
- **Share handshake** (assets pattern): PUT access notifies new targets (action `finance_share`), NotifBell Accept/Decline routes to `POST /finance/shares/respond`; accept fills `accepted[]` across book+account entries; decline/leave drops by-name entries entirely (owner list stays clean), group entries just lose the member; **re-sharing preserves acceptance** (no re-request on access changes).
- **Pool contributors** (no handshake; `shared_with` rejected on pools; **`pool_edit` never consulted by finance**).
- **Caps** `{add:[expense|income], edit_own, see_balances, see_all_tx}`, default = employee expense submission. Server-side enforcement: `_strip_balances()` (balances/total/opening/synced removed from responses), tx list `created_by` filter, POST/PATCH sign gating + own-entry checks, `_require_full_read()` 403s on reports/budgets/planning/invoicing reads, `net_worth` skips capped books.
- **`finance_index.py`** share-routing cache (`_system/finance_share_index.json`), warmed in `main._warm_share_index`, reindexed on access writes; `list_visible_books`/`find_book` scan own + pool + indexed sharers only.
- **New router `finance_sharing.py`**: PUT book/account access (owner / pool-admin), `/shares/respond`, `/books/{id}/leave`, `GET /finance/members` + `/finance/roles` pickers.
- **AI write tools** `add_finance_transaction` (caps sign-gated) + `categorize_transaction` (own-entry rule for contribute; learns payee rules on imported tx) — approval-gated like all writes.
- **Frontend**: SharingSection in BookSettings (rows scoped Whole-book/per-account, target picker from members/team/household/roles, access select, caps checkboxes, pending/✓ acceptance count, hidden-from chips) — owner/pool-admin only; Leave button for shared books; contribute UX (Add gated by caps, own entries clickable, kind toggle restricted, Overview/projection hidden when balances stripped, "you see only your own" note); NotifBell routes finance_share responds.
- 14 new tests in `test_finance_sharing.py` (handshake visibility, decline/leave semantics, re-share acceptance, hidden_from + unknown-role 400, by-name-overrides-group, account-override-beats-book, caps union, pool contributors + admin never restricted + shared_with rejection, pool role-hide, index routing incl. rebuild + decline cleanup, default caps, net-worth skip, created_by filter).

**The finance module is feature-complete per the approved plan** (`~/.claude/plans/before-building-a-workflow-valiant-willow.md`): ledger, bank sync, CSV, budgets, recurring, projection+deviation, invoicing/AR/tax/receipts, sharing with caps, 9 AI tools. 90 finance tests across 5 files.

## Session — posting-cadence reminder hook (dev tooling)

- New Claude Code UserPromptSubmit hook `Business/hooks/posting_reminder.sh`: reads `Business/POSTING_LOG.md` (one row per social post), nudges in-session when the last post is 6+ days old (once/day, silent otherwise). Wired into `LogCoreDEV/.claude/settings.json` and `LogCoreOS/.claude/settings.local.json`.
- **Gotcha caught:** `LogCoreOS/.claude/settings.json` is tracked in the PUBLIC repo — private Business-repo hook paths must go in `.claude/settings.local.json`, now gitignored (`.gitignore` gained that line). Never reference the Business repo from tracked LogCoreOS files.

## Session — Reddit community launch (business side)

- Owner launched the LogCoreOS subreddit (pinned welcome post drafted in-session) and cross-posted to relevant subs. Details + reprioritized action list in the private Business repo (`notes/2026-07-13.md`).
- Product-repo consequence: the **launch surface items in TASKS.md are now time-sensitive** — README screenshots/GIF, CONTRIBUTING.md, issue templates move to the front; demo still gated on privacy policy + ToS + message cap.

## Follow-up (updated)

- ✅ v0.2.0 GitHub release published by owner; finance work committed to master as a single `feat:` commit (2026-07-13).
- **Owner: run `bash launch.sh --skip-build`** to deploy the finance module (m007 runs on startup).
- Owner test pass (see TASKS.md "Now" item): books → bank flow → budgets/projection → invoice/AR → share-contribute walkthrough with a second account (verify balance stripping via curl, not just UI) → chat tools.
- Consider later: SimpleFIN token encryption at rest (OS-level disk encryption assumed today), member-facing sync-now, monthly value report off finance data (already backlogged).

## Follow-up

- **Owner: publish the GitHub release** — releases page → Draft new release → choose existing tag `v0.2.0` → paste the prepared description. The Admin → Updates card and `update.sh` key off the latest GitHub *release*, so self-hosters see the update only once it's published.
- After publishing: commit the Finance work as its own `feat:` commit(s) — now covers Phases A+B+C; mark the release done in TASKS.md.
- `bash launch.sh --skip-build` still pending on the owner box (deploys everything since 07-11 incl. the Finance code in the working tree).
