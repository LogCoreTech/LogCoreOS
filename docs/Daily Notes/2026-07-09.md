# 2026-07-09 — Daily Note

## Session — Assets v2 Phase 1b (2nd owner testing round)

Planned in plan mode (2 Q&A rounds). Owner reported an auth/theme regression cluster, a client crash (`e.filter is not a function`), and several UX gaps. Backend logs (checked via sudo docker — owner authorized restricted commands) were clean (only `/me`+`/health`, all 200), confirming everything was client-side or a small backend behavior change.

**Auth/theme (root of the sign-out mess):**
- `logout()` never reset the theme CSS vars → the signed-out user's background lingered on the login page until a reload (and could block the next login). Fixed: `logout()` resets background/accent/dark/density/corner.
- The 30s `/me` poll's `refreshUser()` `catch` called `setUser(null)` on ANY failure (transient blip) → random logout + "background reverts then returns" flicker. Fixed: catch is a no-op; genuine expiry is handled by `request('/auth/me')`'s 401 redirect.
- `e.filter` crash: client-side, couldn't be localized from the minified message (all `.filter` sites guarded) — the two auth fixes remove the churn that stuck the app in the ErrorBoundary. Added a global `window.onerror`/`unhandledrejection` console logger in `main.jsx`, kept ErrorBoundary stack logging, and added defensive `Array.isArray` guards (TagInput, AssetModal `allAssets`, Assets `load`). If it recurs the console stack now pinpoints it.

**Sharing model reworked (owner's "groups" idea):**
- Per-node resolution (dropped the ancestor walk in `_share_access`/`_is_hidden_from`). `update_access(cascade=True default)` writes the audience to the node + all descendants; `cascade=False` = this node only. UI toggle ("Apply to everything inside" / "This one only") shows only when the node has descendants.
- **Create-under-shared inheritance**: `create_asset` copies the parent's `shared_with`+`hidden_from` onto a new child; `POST /assets` routes a child into the **parent's store** (requires edit access). So an edit-shared user can grow a shared subtree owned by the original owner — a "group".
- Existing subtree tests stayed green because cascade defaults True (behavior preserved). 5 new tests; suite **261 green**; black/isort clean.

**UX:**
- Filter/search now render the **foldered tree** (removed the flat branch); orphans-by-sharing float to top level via `childrenMap`.
- New reusable `components/AssetTreePicker.jsx` (expand/collapse) — used by Move and by the create/edit parent chooser (replaced the dropdown).
- `EmojiPicker` popover `right-0`/`grid-cols-7`/scroll so it never runs off-screen; icon text `maxLength=3`.
- `TagInput` suggestions are inline + capped (~4 rows, scroll) so a scrolling modal can't clip them.

**Verified:** build clean (67 modules); relaunched; health OK, `/static` 200. Owner UI walkthrough recommended for the interaction-heavy bits (sign-out cleanliness, share cascade + group inheritance, tree pickers, emoji/tag boxes, and whether `e.filter` recurs — console will now show the stack).

**Commits:** `671f72f` (backend sharing + auth), frontend+docs commit pending this turn.

## Session — crash localized + theme/login fixes (3rd round)

- Enabled Vite **sourcemaps** (`build.sourcemap: true`) + the global error logger paid off: the `e.filter is not a function` crash mapped to **`Layout.jsx:35`** (`notifs.filter`). Root cause: `sugApi.notifications()` returned a non-array on the deployed instance and `list || []` kept the truthy object. Fixed with `Array.isArray` guards in `Layout.jsx` (NotifBell) and `Chat.jsx` (chatNotifications). (Local backend returns a bare list; the deployed `app.logcoretech.com` backend returned an object — the guard fixes the frontend regardless; deployed backend should be updated.)
- **Background flash** root-caused + fixed: theme prefs weren't persisted to `localStorage`, so the pre-React FOUC script applied defaults until `/me` resolved. `login()`/`refreshUser()` now persist background/accent/dark/density/corner.
- **Sign-out accent leak**: `applyAccentColor(null)` early-returned (`if (!hex) return`), so logout never reset the inline accent vars → the login button kept the last user's accent. Fixed: falsy `hex` now removes the inline `--accent-*` overrides (reverts to the brand-orange `:root` default). Also hardcoded the Login submit button to literal `#f97316` (independent of accent) per owner request.
- **New Phase 2 requirement logged**: owner wants ASSET shares to use the same accept/decline notification handshake as the planned template sharing (per-user shares get a request; group shares stay immediate).

## Session — login banner smoothness + sourcemap removed

- Login banner was a **1.4 MB non-interlaced PNG** (1915×821) → painted top-to-bottom as it downloaded (tacky), and static files had no cache headers so logout re-fetched it. Fixes: render it as an `<img>` that **fades in on load** over a solid `bg-charcoal-900` (never shows the partial paint); `<link rel="preload">` in index.html; **Cache-Control public max-age=86400** on root images in `main.py` spa_fallback (backend rebuild). No separate loading screen needed.
- **Removed sourcemaps** (`vite build.sourcemap: false`) now that the crash is localized/fixed — owner doesn't want them in committed code. Verified dist has 0 `.map` files.
- Recommendation (not done): the banner is still 1.4 MB — re-encoding to WebP/optimized JPEG (~150–300 KB) would make first load near-instant. Deferred (needs image tooling + a new binary asset); the fade-in already removes the visual tackiness.

## Follow-up

- Owner: confirm the crash is gone, login page clean orange, and the banner fades in smoothly. Push commits; watch CI. Deployed backend on app.logcoretech.com should be brought to current (its /suggestions/notifications returned a non-array).
- Optional: optimize/shrink login-banner.PNG (→WebP) for faster first load.
- Next: **Assets Phase 2** (template redesign + the asset/template share handshake).
