# 2026-09-05

## UX Polish Batch — swipe gestures backlogged, pull-to-refresh built

Owner: "skip the swipe gesture. put it back on backlog. lets just do the pull to refresh. then
continue to the other items you mentioned." Session continued directly from 2026-09-04's Tier 4 work
(same UX Polish Batch), no new interview — this was a direct steer on the two items flagged as still
open at the end of that session.

**#17 (swipe gestures)** — removed from the UX Polish Batch's active Tier 4 checklist and moved back
into the general Idea Backlog (`docs/TASKS.md`'s Cross-App UX & Polish section), same place #34 (focus
mode) and the original Capacitor/priority-color items live. Real reason it needed its own triage pass
rather than an implementation slot: per-module semantics were never resolved (does it apply to Notes/
Contacts/Assets or just Tasks; what would "swipe right" even mean on a Contact row) — not a quick
build, a design question.

**#32 (pull-to-refresh, the gesture itself)** — new `lib/usePullToRefresh.js`, hand-rolled touch-event
tracking (no gesture library added — small, well-understood amount of code, and this app's own
dependency philosophy favors that). `main.scrollTop === 0` (via `document.querySelector('main')`, the
same well-known-DOM-node shortcut Toast/ConfirmDialog already use for their own portals) gates when a
touch-down counts as "starting a pull"; elastic sqrt-damped resistance on the visual distance so it
doesn't track the finger 1:1. New `components/PullToRefreshIndicator.jsx` renders the small growing/
spinning ring.

Wired into Tasks, Contacts, Goals, Finance, Dashboard, and Brain — the same 6 pages (of the 7 the
diff-refresh fix touched) where it made sense. Two real complications found and designed around, not
guessed past:
- **Dashboard.jsx** disables the gesture (`enabled: !editing`) while its own block grid is in edit
  mode — react-grid-layout's own drag/resize handles live in the same scroll area, and Touch and
  Pointer events both fire for one physical gesture, so the two would otherwise compete for the same
  touch.
- **Notes.jsx deliberately excluded entirely** — its file-tree pane already has its own pointer-based
  drag-to-reorder (`onNotePointerDown`), the same conflict shape as Dashboard's but with no clean
  `enabled` toggle available (dragging is possible at any time, not gated by an edit-mode flag), and a
  drag starting at the very top of the tree would be genuinely ambiguous between "reorder" and "pull to
  refresh." Needs its own conflict-resolution design (e.g., suspending pull detection for the duration
  of any active `pointerdown` on a tree row) — flagged rather than rushed.
- **Brain.jsx** had no callable refresh function at all before this — its file list was only ever
  fetched inline inside a mount-only `useEffect`. Extracted into a real `loadFiles` (`useCallback`)
  first, mirroring every other page's own `load()` shape, so pull-to-refresh had something to call.

**Honest gap**: this could not be live-verified. No browser/touch-device testing tool is available in
this session's environment — tried spinning up the backend locally to at least load the frontend, but
it correctly refuses to start without a real `SECRET_KEY` (a legitimate safety guard, not something to
route around for a quick UI check). Verification is manual-only: pull down from the top of each of the
6 wired pages on a real phone and confirm the indicator fills in, refreshes on release past threshold,
and snaps back cleanly if released early. Tracked as an open item in `docs/TASKS.md` rather than
claimed as done.

`eslint --max-warnings 0` across the full `src` tree and `vite build` both clean.

## Accessibility audit — real keyboard-nav gaps found and fixed

Continued into the item flagged open at the end of 2026-09-04. Rather than guessing at what a
"keyboard-nav/focus audit" should cover, checked for concrete, evidence-based gaps first (grep, not
speculation) — three real ones found, all fixed:

**1. No modal in the entire app trapped Tab focus.** A keyboard user could Tab straight past the last
focusable element in an open modal into the page content behind the overlay — incorrect per the ARIA
Authoring Practices Dialog pattern, and genuinely disorienting. New `lib/useFocusTrap.js`, same
"hook every modal wires in individually" shape `useEscapeToClose.js` already established (no shared
`<Modal>` wrapper exists in this codebase for either concern to ride on). Built and verified by hand
on `ConfirmDialog.jsx` first, then delegated the retrofit across the other ~41 modal instances to two
parallel background agents (31 more files, split ~15/15) — every diff read and verified directly
before trusting it, not just the agents' own summaries.

Several files needed real judgment beyond the mechanical ref-plus-hook-call pattern, and both agents
got them right: a modal whose card element changes *within one persistent component instance* rather
than fresh-mounting needed the hook's `active` param tied to that same condition, so the trap's effect
tears down and re-attaches to the newly-rendered card each time — `Notes.jsx`'s 6 mutually-exclusive
form blocks (newNote/newFolder/rename/move/deleteNote/deleteFolder) share one `modalCardRef`, gated on
`!!modal`; `TaskModal.jsx`'s view/edit mode switch, gated on `mode !== 'view'` (and correctly placed
*before* its own early return to `<TaskView/>`, respecting Rules of Hooks); `GoalModal.jsx`'s delete
popup (a second, separate card), gated on `showDelete`; `WelcomeBackPopup.jsx`'s self-toggled
visibility (mounted unconditionally by `Layout.jsx`, decides its own `if (!state.show) return null`
internally), gated on `!!(state && state.show)`. A few files also had non-`.modal-card` root elements
(`Automations.jsx`'s `LogsModal`/`ImportModal`, `GoalModal.jsx`'s delete popup's own `.card` class,
`assetDisplay.jsx`'s portal-based `ImageLightbox` with no separate overlay/card split at all) — both
agents correctly targeted the real content root in each case rather than blindly assuming `.modal-card`
everywhere.

**2. No skip-to-content link.** A keyboard user had to Tab through the entire sidebar (or mobile
header) on *every single page* before reaching real content. Added to `Layout.jsx` — visually hidden
(`sr-only`) until focused, then jumps to a new `id="main-content"` on `<main>` (also gained
`tabIndex={-1}` so it's actually focusable via the jump — a bare `<main>` isn't natively focusable and
some browsers won't move focus to a fragment target that isn't).

**3. `TagInput.jsx`'s own text input had `outline-none` with nothing replacing it anywhere** — the
wrapping pill/tag field had no `focus-within` styling at all, so a keyboard user tabbing into a tag
field (used across Contacts' Core Values, Assets' template fields, sharing pickers) got zero visual
indication of where focus landed. Fixed with `focus-within:ring-2 focus-within:ring-orange-500` on the
wrapper, matching `index.css`'s own standard `.input` focus-ring exactly rather than inventing a new
style.

**Deliberately left as lower-priority/open**, not chased further this pass: full ARIA-Tabs arrow-key
navigation on Goals'/Finance's own custom tab-switcher widgets (already keyboard-reachable via real
`<button>` elements and Tab+Enter, just not the enhanced arrow-key pattern), and a broader unbounded
page-by-page audit beyond the three concrete gaps found above.

`eslint --max-warnings 0` across the full `src` tree and `vite build` both clean throughout.

Next: broader Toast adoption (still open from 2026-09-04's end-of-session list).

## Broader Toast adoption — replaced every remaining raw browser alert()

Grepped for real gaps first rather than guessing where toast "should" go — found exactly 10 raw
`alert()` call sites across 6 files still using the old blocking-popup pattern (Tier 1's
`confirm()`→`ConfirmDialog` migration never touched `alert()`, a separate call): `Chat.jsx` (load/
delete-chat failures), `Automations.jsx`'s `WorkflowCard` (delete/toggle-active failures), `Contacts.jsx`
(CSV import success+failure, bulk-convert success), `Account.jsx` (timezone/export failures),
`Appearance.jsx` (save failure). All replaced with `toast.error()`/`toast.success()` as appropriate.

Checked each site's own context before swapping rather than doing a blind find-replace: `Account.jsx`'s
`saveTimezone()` and `Appearance.jsx`'s save handler each already had their own inline "Saved ✓"-style
success indicator — only their error path (still a raw `alert()`) needed the toast swap, not a
duplicate success toast on top of an already-adequate one.

**Deliberately did not chase further** — a broader sweep of every silently-swallowed `catch {}` block
in the app looking for more "should this show something" gaps was considered and dropped: the vast
majority of those are already deliberate, previously-reviewed non-fatal cases (e.g. Finance's own
`catch { /* surfaced by reload */ }` pattern, seen repeatedly today) rather than real gaps, and a
low-confidence blanket audit risked flagging intentional silence as broken rather than finding
anything real. `grep -rn "alert("` now returns zero real UI-feedback call sites anywhere in the app.

`eslint --max-warnings 0` across the full `src` tree and `vite build` both clean.

This closes out every item flagged open at the end of 2026-09-04's session: pull-to-refresh, the
accessibility audit remainder, and Toast adoption are all done. Still explicitly deferred per the
owner's own earlier instruction: #4 (bulk actions) and #5 (soft-delete/trash bin), until a joint
design session. Swipe gestures (#17) is back in the general Idea Backlog, not scheduled.

## Owner-reported bug: confirmation popups not fixed on screen, background kept scrolling

Owner: "it appears that the 'are you sure' toasts... are not fixed on the middle of the screen they
are attaching to whatever page they are on... they all need to be separately fixed and stop all other
scrolling while it is open. please check all items."

Root cause: the exact same pre-existing "nested-modal clipping" bug already documented in `docs/MEMORY.md`
for `assetDisplay.jsx`'s `ImageLightbox` and `GoalModal.jsx`'s own `Portal` — `.modal-card` applies
`.card`'s `backdrop-blur-sm`, and `backdrop-filter` on an ancestor creates a new containing block for
`position: fixed` descendants, so a modal rendered nested inside another already-open `.modal-card`
resolves its own "fixed" positioning against that OUTER modal's scrolled box instead of the true
viewport — it visually rides along as the outer modal's own content scrolls. This time it was hitting
`ConfirmDialog` itself, made dramatically more visible by #9's own work earlier this same batch: #9 added
a "Discard changes?" `ConfirmDialog` to six major edit forms, meaning `ConfirmDialog` now opens nested
inside an already-open modal far more often than the handful of pre-existing "Delete this?" call sites
ever did. A pre-existing, narrow bug became a routinely-hit one because of work shipped the same day —
worth naming plainly rather than glossing over.

**Fix, at the root rather than per-call-site**: `ConfirmDialog.jsx` now always renders via
`createPortal(..., document.body)` — this alone fixes every current and future call site (there are
~40) regardless of nesting, permanently, without needing to find and patch each one individually. New
`lib/useScrollLock.js` locks the app's single scrollable `<main>` container while open — module-level
reference-counted (not per-instance state) so a dialog stacked on top of an already-open modal doesn't
prematurely re-enable scrolling when the inner one closes while the outer one is still open.

**"Check all items" — a real, systematic sweep, not just the one reported symptom.** Grepped for every
file with more than one `.fixed inset-0`/`.modal-overlay`-shaped block (the fingerprint of a nested
popup) and checked each candidate by hand rather than assuming the bug was ConfirmDialog-only:
- `AssetModal.jsx`'s `archivePrompt` and `SaveAsTemplateModal` — the *exact two instances* flagged as
  known-unfixed back on 2026-08-29 (`docs/TASKS.md`'s own Product Backlog said so explicitly) — both
  now portaled, closing a documented, aged gap rather than a new one.
- `AssetView.jsx`'s mute-notifications popup — same bug, not previously flagged anywhere; found fresh.
- Along the way, found a *second*, related bug in the same files: `AssetModal.jsx`'s main editor
  (`useFocusTrap(cardRef)` with no `active` tied to its own `mode` view/edit toggle — TaskModal got
  this right during yesterday's retrofit, AssetModal didn't), its own `archivePrompt`, `AssetView.jsx`'s
  mute popup, and `RecurringPanel.jsx`'s two form popups all had a *stale* focus trap: the hook's effect
  (deps `[threshold, enabled, containerRef]`) only ever ran once, at the persistent parent component's
  own mount — while the popup's ref was still null — and never again, since none of those deps change
  when an internal toggle later opens the popup. All four fixed by adding the missing `active` argument
  (mirroring how Notes.jsx/GoalModal.jsx/WelcomeBackPopup.jsx were already correctly handled in
  yesterday's own retrofit — this was a real miss in that pass, not a new class of bug).
- Checked whether `RecurringPanel.jsx`'s/`InvoicesPanel.jsx`'s own popups needed portaling too, not
  just the stale-trap fix — confirmed they don't: neither `RecurringPanel` nor `InvoicesPanel` is
  itself nested inside another modal (both render as normal in-page panel views on the Finance page),
  so their own popups were never subject to the containing-block bug, only the separate stale-trap one.
- Two unrelated findings surfaced during the sweep, deliberately **not** fixed here (a different bug
  class, out of scope for this specific report): `Journal.jsx`'s history drawer and `Tasks.jsx`'s
  reorder-priorities modal have no Escape-to-close, focus-trap, or scroll-lock at all — neither is
  nested inside another modal, so neither has the containing-block bug; they were just apparently
  absent from Tier 1's original retrofit file list. Filed in `docs/TASKS.md`'s Product Backlog rather
  than silently fixed, so the owner can decide whether it's worth a pass now or later.

`eslint --max-warnings 0` across the full `src` tree and `vite build` both clean. Not live-verified —
same environment limitation as pull-to-refresh (no browser/touch-device tool available here); the
owner should confirm on a real device that opening a "Discard changes?"/delete confirm from inside any
edit form now stays centered and blocks background scroll, especially while the outer form's own
content is scrolled partway down.

## Follow-up: "fix the journal things you mentioned"

Fixed `Journal.jsx`'s history drawer specifically (the item named in the Product Backlog note above) —
added `useEscapeToClose`/`useFocusTrap`/`useScrollLock`, the same 3-hook baseline every other modal in
the app already has, plus `aria-label="Close"` on its bare ✕ button (missed by the earlier aria-label
sweep since this file was never in that sweep's target list either). Not nested inside another modal,
so no portal was needed here — this was purely the "missing baseline protections" gap, not the
containing-block bug.

`Tasks.jsx`'s reorder-priorities modal has the identical gap and was reported alongside Journal's, but
the ask was specifically "the journal things" — left it as its own open Product Backlog item rather
than assuming the broader ask, in case it wasn't meant to be included.

`eslint --max-warnings 0` across the full `src` tree and `vite build` both clean.

## Follow-up: "run this check on every single jsx page and popup and make sure they all are as should be"

Owner asked for the reactive spot-fixing above to become a genuine, exhaustive, systematic sweep. Built
a real inventory rather than trusting the existing file lists were complete — grepped for every distinct
way a modal identifies itself in this codebase (`.modal-overlay` class, raw `fixed inset-0`, `role="dialog"`
/`role="alertdialog"`, any component accepting an `onClose` prop) and cross-referenced against which files
already had `useEscapeToClose`/`useFocusTrap`/`useScrollLock`, using `comm` diffs on sorted file lists
rather than eyeballing greps — the same technique that caught Journal/Tasks/Layout/Chat as real gaps
earlier, done exhaustively this time instead of ad hoc.

**Finding #1 — `useScrollLock` coverage was badly behind the other two hooks.** `useEscapeToClose` and
`useFocusTrap` were each in 32-35 files (essentially every modal); `useScrollLock` was only in the 8 files
touched while chasing the original ConfirmDialog report. Retrofitted the other 27 files (~35 modal
instances) via two parallel background agents, each given the *exact* existing `useFocusTrap` call and
its argument to mirror — since both hooks must be active under the identical condition, matching
arguments was mechanical, not a judgment call, so both agents could work from a fully-specified list
with zero ambiguity. Verified every diff directly afterward (spot-checked the trickiest multi-instance
files — GoalModal's two popups, Notes' three) rather than trusting the agents' own summaries.

**Finding #2 — two more modals had NONE of the three hooks at all**, the same class of gap Journal's own
fix closed, found by the `onClose`-prop/`fixed inset-0` cross-reference rather than assumed away:
`Layout.jsx`'s mobile "All Modules" drawer and `Chat.jsx`'s own "Chats" history drawer (near-identical in
shape and even variable naming to Journal's — apparently all three were built the same way, missing the
same retrofit). Both fixed with the full 3-hook treatment plus a missing `aria-label="Close"`.

**Finding #3 — given the ask was now explicitly "every single" one**, fixed `Tasks.jsx`'s
reorder-priorities modal too (deliberately left alone in the previous, narrower "fix the journal things"
request specifically because the ask hadn't been broadened yet).

**Finding #4 — the 2026-09-04 aria-label sweep had a narrower blind spot than realized.** It searched
for the `✕` glyph (U+2715) specifically; a systematic re-grep for the visually-near-identical but
distinct `×` (U+00D7, "multiplication sign" — a different Unicode character screen readers may announce
differently, and which a naive re-search for "the same-looking X" would miss without checking codepoints)
turned up ~15 more unlabeled buttons, plus `‹`/`›` month-navigation arrows (Household/Team/Calendar, 6
instances) and bare `+`/`＋` add buttons (BookSettings ×3, ContactModal, PriorityList) that the original
sweep's glyph list never covered at all — about 24 more instances across 11 files, all fixed with the
same "read the onClick target, pick the accurate label" discipline as the original sweep.

**Verified the inventory is now actually complete**, not just "probably fine": re-ran the `comm`-diff
checks after all fixes landed — `useEscapeToClose`/`useFocusTrap`/`useScrollLock` each now present in
exactly 35 files (previously 32/32/8), and a final broad-glyph regex sweep (`✕×✖⊗☒✎⠿‹›«»↗↑↓←→` plus bare
`+`/`＋`) across every `.jsx` file in the app returns zero remaining unlabeled icon-only buttons.

`eslint --max-warnings 0` across the full `src` tree and `vite build` both clean throughout this pass.

## Unrelated bug: Settings page overflowing horizontally on mobile

Owner: "the settings page on mobile is overflowing to the right because its not truncating the setting
descriptions." A classic flexbox gotcha, not a truncation problem per se: a flex item's automatic
minimum width defaults to its content's full (unwrapped) size unless explicitly overridden with
`min-w-0` — several settings rows had a bare `<div>` (label + description) as a direct child of a
`flex items-center justify-between` row, with no `min-w-0`, so a long description forced the whole row
— and the page — wider than the viewport instead of wrapping.

Found by grepping every `flex items-center justify-between` in `pages/settings/` and checking each for
the same shape (an unconstrained div immediately followed by a fixed-size control) rather than guessing
from the one reported page. Real instances, all fixed (`min-w-0` on the text wrapper, `shrink-0` on the
fixed-size control, `gap` between them):
- `Notifications.jsx`'s built-in Proactive Suggestions rows (daily digest, overdue alert, etc.) — the
  one actually reported. The Custom Suggestions rows right below already had `min-w-0` correctly, which
  is what made the bug obvious once spotted: the same shape, fixed in one place and not the other.
- `admin/General.jsx`'s "Open registration" and "Auto-update" toggle rows.
- `admin/NewUser.jsx`'s linked-contact chip — a genuinely variable-length contact name that could
  realistically overflow, not just a short fixed string; also added `truncate` here rather than letting
  it wrap, since it's a single-line chip.

Checked the rest of `pages/settings/`'s own `justify-between` rows individually rather than assuming
they all needed the same fix — most are just a short title next to a small badge/button with the actual
description living on its own full-width line below (safe by construction), or already correctly
truncating on the flex item itself (`overflow:hidden`, which independently exempts that item from the
automatic-minimum-width rule — no `min-w-0` needed there either). Only fixed the ones with the real
shape of the bug, not every row that merely used the same layout utility class.

`eslint --max-warnings 0` and `vite build` both clean. Not live-verified on a real phone — same
environment limitation as everything else this session — but the mechanism is well-understood and the
fix mirrors patterns already working correctly elsewhere in this exact codebase (MenuRow.jsx, the
Custom Suggestions rows), not a novel approach.

## Follow-up: still overflowing — the main Settings page itself, not the sub-pages just fixed

Owner: "that didnt fix it. on the page when first click settings its still overflowing. cause the
setting description of shortcuts is too long. it needs truncated." A real miss on my part — the previous
pass fixed `Notifications.jsx`/`admin/General.jsx`/`admin/NewUser.jsx` (one level deeper in Settings'
own navigation) and cited `MenuRow.jsx` as an *already-correct* reference pattern without actually
checking its longest real-world case — the main Settings landing page (`pages/Settings.jsx`) itself,
which is what a user sees immediately on tapping Settings, uses `MenuRow` for every row and was never
actually broken (or fixed) by anything so far.

Measured every `MenuRow` `subtitle=` string in the app by length: Shortcuts' ("Pin modules to the
mobile bottom bar and the desktop sidebar", 61 characters) was the longest in the entire codebase by a
real margin over the second-longest (Admin's "Registration, workspaces, session length, updates", 51).
`MenuRow.jsx`'s own truncation setup (`flex-1 min-w-0` wrapper + `truncate` on the label/subtitle spans)
reads correctly by the same reasoning that held up for every other fix this session — but evidently
still wasn't enough to survive real mobile rendering for the single longest string in the app, the same
kind of narrow, hard-to-reason-about-in-the-abstract mobile-rendering gap `docs/MEMORY.md` already has
precedent for (the documented iOS-Safari-specific `min-h-0` cascade note left in `Layout.jsx`'s own
"Main content" comment). Rather than keep re-deriving the CSS from first principles with no way to
render and measure it, fixed this two ways at once:
1. Added `overflow-hidden` directly to `MenuRow.jsx`'s own row container (the `<Link>`/`<button>`) — a
   second, independent containment boundary that stops the row's own box from ever forcing the page
   wider, regardless of how the nested flex-shrink/min-width computation resolves in any given browser.
2. Shortened the Shortcuts subtitle itself to "Pin modules to the bottom bar & sidebar" (40 characters)
   — removes the immediate trigger outright, independent of the CSS fix holding up.

Neither fix depends on the other being right — either one alone should stop the page from overflowing;
together they're redundant on purpose. `eslint --max-warnings 0` and `vite build` clean. Still not
live-verified — flagged plainly rather than claimed fixed with false confidence, since the previous
"this should already work" reasoning turned out to be wrong once — the owner should confirm on their
own phone that the Settings landing page no longer scrolls sideways.

## Closing out the last accessibility item, then moving to plan mode

Owner: "fix the remaining accesibility things then enter plan mode for the two major ones." The
"remaining" item was the one specifically named in the plan/backlog: arrow-key navigation on custom
tab-switcher widgets (Goals' ME/pool tabs, Finance's view tabs), plus the open-ended "broader page audit."

New shared `lib/tabListKeyboard.js` — `handleTabListKeyDown(e, {tabs, activeIndex, onActivate, refs})`,
a plain function (not a hook, since each caller already owns its own active-tab state and ref array) —
Left/Right moves AND activates the adjacent tab, Home/End jump to the ends, matching the ARIA
Authoring Practices' standard tablist interaction. Applied the full pattern (`role="tablist"`/`"tab"`,
`aria-selected`, roving `tabIndex` so only the active tab sits in the normal Tab order) to every
*genuine* content-switching tab widget found in the app, not just the two named examples — a `grep` for
the shared `bg-charcoal-100 ... rounded-lg p-1` segmented-control styling turned up 3 more of the
identical shape: Household's/Team's own Calendar/Tasks tab bar (explicitly commented "Tab bar" in the
source), and the admin User Detail page's Personal/Business workspace switcher for module restrictions.

**Deliberately excluded, and why it matters**: the same styling class is also used by several rows that
are NOT tabs — Tasks'/Contacts' filter-chip rows, Finance's Sort-mode picker, and TransactionModal's/
ContactModal's Expense-Income-Transfer and Person-Company selectors (both literally form fields, set
via `set('type', t)` inside a `<form>`). Giving these `role="tab"` would have been actively wrong, not
just unnecessary — a screen reader would announce a filter or a form field as if it were switching
between content panels, which it isn't. Fixing everything that shares a CSS class regardless of what it
semantically *is* would have been the over-scoped, sloppier version of this fix.

Also swept every `<img>` in the app for a missing `alt` (a real, common, easy-to-check a11y gap not yet
covered by anything this session had done) — both real (non-decorative) images already had one
(`assetDisplay.jsx`'s lightbox uses the filename, `Login.jsx`'s background banner is correctly
`alt=""` + `aria-hidden="true"` since it's purely decorative). Nothing to fix there.

This closes every item in the UX Polish Batch's accessibility line — nothing further is open. Full
backend/frontend UX Polish Batch work is now done except the two explicitly-deferred items (bulk
actions, soft-delete/trash bin), which the owner asked to move into plan mode for next.

## Plan mode: designing #5 Trash and #4 Bulk actions, then starting the build

Re-entered plan mode for the two deferred items. Read the existing plan file's owner-vetted design
decisions in full first (Trash/ Brain path, 30-day window, all-modules-at-once, every human AND AI
delete path routes through it), then ran an Explore pass to get a precise, current-code inventory
(file:line) of every module's actual delete function and AI delete tool — the plan predates the Mod
Store conversion finishing, so exact locations needed re-verifying rather than trusting older
assumptions. Real gaps surfaced: team has zero AI tools of any kind; calendar/contacts/finance/journal/
whole-dashboard delete have no AI delete tool to intercept at all; `read_brain_file` doesn't consult the
`_brain_skip()` mechanism the way `list_brain_files`/`search_brain` do.

A Plan agent turned that inventory into a concrete build spec. Two real scope questions went to the
owner rather than being decided silently: whether sub-records (asset comments/attachments/templates,
contact interactions/deals) warrant their own trash entries, and whether dashboard block removal needs
one built from scratch. Owner picked the narrower scope both times (sub-records stay hard-deleted;
block removal stays out of scope) — recorded in `docs/MEMORY.md`'s 2026-09-05 entry and the plan file.
Plan approved via `ExitPlanMode`; owner then asked to start building now rather than stopping at the
plan, beginning with #5's backend core.

**Built this session**: `services/trash_service.py` (soft_delete/restore/purge_one/purge_expired/
list_trash_for_user/allowed_stores_for — index-before-move ordering, per-module dispatch via a new
`module_registry.trash_dispatch()`/`owned_trash_types`); both AI Brain skip-list fixes (`_brain_skip()`
gained `"Trash"`, and `read_brain_file` now actually checks the skip set — a real pre-existing gap, not
new); `routers/brain.py`'s `_ALWAYS_SKIP` also got `"Trash"`; `job_trash_purge` (`scheduler.py`, 02:00);
new `routers/trash.py` (list/restore/purge/bulk, each validating a caller-supplied `store_user` against
`allowed_stores_for()` before touching anything — closes what would otherwise be a real cross-user
access hole). Tasks is the first module wired fully end-to-end (service + router + AI tool + Household/
Team pool call sites) as the worked example other modules will follow. New tests: `test_trash_service.py`
(8), `test_trash_router.py` (7), `module_packages/tasks/tests/test_trash_handlers.py` (6), plus extended
`test_task_service.py`'s delete test. Full backend suite: 1373 passed. `black`/`isort` clean.

Remaining for #5: the other 8 delete-owning modules' own `trash_handlers.py` + call-site wiring (Notes,
Assets, Contacts, Finance's 10 delete functions across 3 services, Goals, Journal, Calendar/events,
Dashboards), then `pages/Trash.jsx`. #4 Bulk actions comes after, since its bulk-delete depends on #5.

`eslint --max-warnings 0` across the full `src` tree and `vite build` both clean.

## Continuing #5: all remaining modules wired, plus an owner-requested permission split

Owner: "keep going also probably a good idea to split the trash bin into two tabs depending on
permissions. say admins have a personal and household/team tabs and non admins only see their own
items." Implemented the split first since it touches the same core function every module's wiring
depends on: `trash_service.allowed_stores_for()` now gates the household/team pool store on
`role=="admin"` outright (not the finer `pool_edit` grant other pool mutations use — reviewing another
member's deleted history is a more sensitive concern than day-to-day editing), and each entry
`list_trash_for_user()` returns now carries a `scope: "personal"|"pool"` field so the eventual
`pages/Trash.jsx` can split into tabs on already-correctly-scoped data rather than filtering client-side.

Then wired the remaining 8 delete-owning modules, following the Tasks pattern from earlier — each gets a
`deleted_by` kwarg threaded through its service function(s), a new `module_packages/<id>/backend/
trash_handlers.py` (describe/restore/access_check), an `owned_trash_types` manifest entry, and every
router/AI-tool call site updated:

- **Calendar** (`services/events_service.py`) — one function covers Calendar + Household + Team events
  for free, since all three already called it against different store_users.
- **Goals** (`module_packages/goals/backend/service.py`) — the interesting case: a cascade delete can
  remove a whole subtree of goals in one call, so it now snapshots every doomed goal BEFORE the actual
  removal (needed the full subtree read first anyway) and writes one trash entry per goal, not one per
  call.
- **Journal** (`module_packages/journal/backend/service.py`) — first module with no JSON payload at all;
  the whole record is the moved `.md` file plus its tags (snapshotted since the existing code already
  clears them unconditionally on delete).
- **Notes** (`services/notes_service.py`) — two record types (`note`, `folder`); a folder delete snapshots
  every nested note's tags under that prefix so a folder restore brings its whole subtree's tags back,
  not just the folder itself. Confirmed and left alone, per the plan's own note: the note/folder's
  `_shares.json` entry was never cleaned up on delete before this feature existed either, so restoring
  the file back to its original path makes that dangling entry apply again "correctly by accident" — not
  something this pass fixes or needs to.
- **Assets** (`services/assets_service.py`) — reused the exact same stale-reference tolerance Tasks
  already has for a vanished linked Goal: if a restored asset's `parent_id` no longer resolves (the
  parent was itself purged), clear it and restore top-level with a `_warning`, rather than hard-failing
  the whole restore.
- **Contacts** (`services/contacts_service.py`) — the sub-record case from the plan's Open Question #1:
  interactions/deals are snapshotted into the payload, but ONLY so `describe()` can put "3 interactions +
  1 deal not restorable" in the Trash row's subtitle and a restore's response can carry the same warning
  — they're never actually brought back, since the owner already decided sub-records stay hard-deleted.
- **Finance** (`services/finance_service.py` + `finance_planning_service.py` + `finance_invoice_service.py`)
  — the big one, 10 delete functions across 3 services in one `trash_handlers.py` dispatching by
  `record_type`. `delete_book`'s whole book directory (shards + rules + receipts) moves as one unit — the
  same "move a directory between two Brain paths" primitive `trash_service.py` already had from Notes'
  folder case — and `delete_transaction` moves its own receipts subdirectory the same way. The other 8
  (account/receipt/rule/recurring/planned/client/invoice/payment) are pure JSON-array entries.
  `delete_payment`'s restore re-runs the same paid/sent status-flip `record_payment` already does, so a
  restored payment doesn't leave an invoice's status stale relative to its real balance.
- **Dashboards** (`services/dashboards_service.py`) — whole-dashboard delete only, confirming the plan's
  scope call: dashboard block removal still has no dedicated delete function today (both the human UI and
  the AI's `remove_dashboard_block` tool just save a shorter blocks list), so it stays untouched.

New tests per module (`test_<module>_trash_handlers.py`, one file per module after a real pytest
collection collision forced a rename — `module_packages/<id>/tests/` directories have no `__init__.py`,
so two same-named `test_trash_handlers.py` files across different packages import-collide under pytest's
default prepend import mode; every module's file is now uniquely named). Full backend suite: 1432 passed
(up from 1373), `black`/`isort` clean. All 9 delete-owning modules are now fully wired — the only piece of
#5 left is `pages/Trash.jsx` itself, then #4 Bulk actions.

Also noted, not fixed (pre-existing, unrelated to this work): `tests/test_events_service.py` stubs
`services.auth_service` with a `MagicMock` via `sys.modules.setdefault(...)` at import time — if that
file happens to be the first in a given pytest invocation to touch `auth_service`, the mock poisons
`sys.modules` for the rest of that process, breaking any later test file that needs the real module. Only
bites narrow ad-hoc test selections that put this file first; the full suite (and any run where something
else imports `auth_service` first) is unaffected — confirmed by running the "failing" combination both
ways.

## Frontend: pages/Trash.jsx, plus a placement decision

Owner asked where Trash should live in the nav before building it. Answered as a real tradeoff, not a
foregone conclusion: Brain (the closest existing precedent for "core, non-module infrastructure") is
buried inside Settings → Account next to Export, with no nav presence of its own — but Trash exists
specifically for the "I just deleted the wrong thing" moment, a more time-sensitive need than Brain's
occasional raw-file browsing, so burying it the same way seemed like the wrong call to carry over
automatically. Recommended a dedicated row on the top-level Settings page instead. Owner's actual
instruction: "let's put the main access for it in settings. and have a quick link button to it in all
modules that use it" — settled the placement question and added a second requirement (per-module quick
links) in the same breath.

Ran an Explore pass first to avoid guessing at conventions: confirmed `MenuRow`'s prop shape, the exact
current Settings.jsx row list, App.jsx's core-route pattern (`/brain` as the model — plain `<Protected>`,
no `ModuleRoute` wrapper), and — critically — that `HelpButton.jsx`'s small round ⓘ-next-to-the-title
pattern is the only Link-style icon button already used consistently across all 9 target module pages
(only Finance/Dashboard have a `⚙ Settings`-style text button; the other 7 have no header icon button at
all), making it the natural pattern to copy rather than inventing a new one.

**Built**: `pages/Trash.jsx` (two-tab admin split — Personal / Household-or-Team by workspace, hidden
entirely for non-admins or when the pool module isn't available, full ARIA tablist treatment via the
existing `lib/tabListKeyboard.js` from earlier this session's a11y pass; reads `?module=<id>` to
pre-filter; grouped-by-module rows with Restore/Delete-permanently, the latter behind the shared
`ConfirmDialog`; pull-to-refresh, empty state, toast feedback on both actions); a new `trash` client in
`lib/api.js`; a new shared `components/TrashLink.jsx` (mirrors `HelpButton.jsx` exactly, 🗑 instead of ⓘ)
dropped into all 9 delete-owning modules' own page headers (Tasks, Calendar, Goals, Journal, Notes,
Assets, Contacts, Finance, Dashboard); the Settings.jsx row and the `/trash` route in App.jsx.

`npm run build` and `eslint --max-warnings 0` both clean across the whole `src` tree. Not live-verified —
no browser automation available in this environment, flagged plainly per this project's own standard
rather than claimed working from a clean build alone.

**UX Polish Batch #5 (soft-delete/trash bin) is now fully shipped, backend and frontend.** `docs/TASKS.md`
updated to remove it (detail lives here, in `docs/MEMORY.md`, and in `CHANGELOG.md`'s Unreleased/Added
section now) — only #4 Bulk actions remains open in the batch, and it can now actually be built since its
bulk-delete depends on Trash existing.

## #4 Bulk actions — the last item in the batch, shipped the same day

Owner: "keep going." Built the shared frontend mechanism first (`lib/useBulkSelect.js`, `components/
SelectCheckbox.jsx`, `components/BulkActionBar.jsx`) since Trash's own eventual bulk bar was always going
to need it too, then 4 new backend endpoints, then wired all 4 list views.

**Backend** — one `POST /{module}/bulk-delete` per module rather than one generic cross-module endpoint
(rejected: it would need to understand 4 different identifier shapes and 4 different per-item access
checks that already exist correctly per-module). Each loops its own already-soft-delete-routed single
delete, returning `{deleted, failed}` — one item's failure never aborts the batch. Two real per-module
subtleties: Assets sorts the request by tree depth descending first, since `delete_asset()` has no
cascade at all and a same-request parent+child selection only succeeds if the child goes first; Notes
takes `{path, type}` pairs (needs the type to dispatch to `delete_note` vs `delete_folder`) and drops any
selected item nested under another selected folder before processing, both client- and server-side.

**A real bug caught before it shipped**: `BulkActionBar`'s mobile bar was originally plain `fixed
bottom-0` JSX, not portaled — the exact same "backdrop-filter/transform on an ancestor breaks `position:
fixed`" bug class `ConfirmDialog.jsx` already has a whole documented incident about. Caught it by pattern-
matching against that existing comment before ever clicking anything, not by reproducing the bug live —
fixed by portaling to `document.body` the same way Toast/ConfirmDialog already do, before this ever went
into a page.

**Another small fix while wiring Contacts**: `ContactRow`'s root element was a `<button>` — nesting the
new checkbox `<input>` inside it would have been invalid, non-nested-interactive HTML (and a real
accessibility/focus bug, not just a lint nitpick). Restructured to a `<div>` root wrapping an inner
`<button>` for the click-to-open target; the other three row components already used a `<div>` root, so
only Contacts needed this.

Full backend suite: 1443 passed. `npm run build` and `eslint --max-warnings 0` both clean across the whole
`src` tree. Not live-verified in a browser (no automation available here) — flagged plainly, same as
every other frontend claim this session.

**The entire UX Polish Batch — all 4 tiers, the original 34-item backlog minus what was explicitly
dropped/deferred/declined during triage — is now fully shipped.** Nothing committed this session; the
owner has not asked for a commit.
