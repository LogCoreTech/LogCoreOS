# 2026-07-08 — Daily Note

## Session — Assets v2 Phase 1 (owner testing feedback: 12 issues)

Planned in plan mode (3 Q&A rounds); owner picked: Phase 1 = crash + bug fixes + UX polish, Phase 2 = full template redesign (logged in TASKS "Now"). Also settled: archive "only this" floats children to top level; moves same-owner only; **build the share index now**; member selector = names only (upgrade later); attachment-in-Brain disk growth accepted as a known limit.

**Crash (item 3).** Docker logs were unreachable from the session (no `sg`/sudo), so root-caused from code + the owner's two symptoms:
- **Login-kick** = `api.js` `request()` hard-logged-out on ANY 401, so a stray/background 401 (Dashboard AssetsWidget poll, /me poll) ejected the user. Fixed: re-verify the session via `/auth/me` before clearing; a lone 401 now rejects to the caller's catch.
- **Blank "…not valid" (ErrorBoundary)** = history render destructured every change as `[o,n]`; non-array/legacy data threw "not iterable". Fixed with a tolerant `formatChanges`; ErrorBoundary now logs the real message and offers Reload.
- Still want the owner's exact error text + `docker logs logcore-app` to fully confirm, but both fragilities are fixed regardless.

**Backend.** Per-node archive (`_is_archived` no longer walks ancestors) + `set_archived(cascade)` + `count_active_descendants`; owners can delete own personal assets (`find_asset.can_delete`); new **`services/assets_index.py`** derived share-routing cache (`_system/assets_share_index.json`) rebuilt at startup (`_warm_share_index`) + incrementally, so `list_visible`/`find_asset` read only sharers' files; `GET /assets/members`; AI `search_assets` + `move_asset` (+cascade on archive). Reads asset files directly to avoid an import cycle. 8 new tests → suite **257 green**; black/isort clean.

**Frontend.** Move button + `MovePicker` tree-picker (same-owner); search bar + filter dropdown replace pills (flat results while searching); create modal shows what unlocks after save and auto-flips to edit; 3-choice archive prompt; share/hide member **selectors**; reusable **TagInput** (GitHub-topics chips) + **EmojiPicker**; personal delete button; mobile **safe-area** modals (`viewport-fit=cover` + shared `.modal-overlay`/`.modal-card` in index.css, applied to all 4 modals). Build clean.

**Verified live** (relaunched): health OK, `/assets` deep-link serves SPA, `/assets/members` 401 unauth, `/static` 200, share index warmed at startup.

**Commits:** `53282d9` (backend), `82507e3` (frontend). Docs updated in this turn.

## Follow-up

- Owner: UI walkthrough of the polish (move, search, 3-choice archive, selectors, emoji picker, mobile modals); if the crash recurs, grab the exact ErrorBoundary text + `docker logs logcore-app`.
- Push the two commits; watch CI (local venv is 3.14, CI 3.11).
- Next: **Assets Phase 2** (template redesign — per-user templates, role/user sharing, accept/decline notification handshake) — now top of TASKS "Now".
