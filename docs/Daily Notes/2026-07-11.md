# 2026-07-11 — Daily Note

## Session — Phase 2 bug fixes (owner testing)

- **Clarified deployment**: `app.logcoretech.com` = the local app via the `logcore-tunnel` Cloudflare tunnel (up 13 days), NOT a separate box. My relaunches reach it. (I had wrongly said the "deployed backend was behind" — corrected in the 07-09 note.)
- **Asset share did nothing** — root cause found in the logs: sharing an asset lived behind a *separate* "Save access" button; users hit the footer **Save** (which did `PATCH /assets/{id}` for name/fields only), so no `PUT /assets/{id}/access` was ever sent → no notification, nothing on the recipient. Fix: the main Save now also persists sharing/hiding (`AssetModal.submit` calls `updateAccess` when `canManage`); removed the confusing extra button.
- **Leaving a share didn't remove the name** — decline/leave only cleared the viewer from `accepted`, leaving the `{target}` entry, so the owner still listed them. Fix: `_respond_shares` drops the entry entirely for a **per-user** target on decline/leave (group/role shares still just drop the viewer from `accepted`). Applies to assets + templates. Tests assert the owner's `shared_with` no longer lists a member who left.
- Template share/accept/leave itself was already working in the logs (PUT access 200, respond 200, leave 204) — only the "still listed after leave" part was wrong.
- Suite **275 green**, black clean, frontend build clean, relaunched (health OK). Committed `26dd752`.

## Follow-up

- Owner: re-test the two-account flow — share an asset, hit **Save** (one button now), recipient gets the bell request → Accept → visible; recipient Leaves → gone from the owner's share list too.
- Optional still open: optimize login-banner.PNG (→WebP).
