# 2026-08-12

## Fixed the ai_usage_service timezone-bucketing test bug (deferred from 2026-08-10)

Owner confirmed the other three post-release items were fine and asked to fix this one now.

Root cause (full diagnosis history in the 2026-08-10 note): `test_record_tokens_accumulates_personal_and_business`
and `test_record_message_only_bumps_messages` use `USER = "TestUser"` without ever registering it via
`create_user()`, so `auth_service.get_user_timezone` finds no account and falls back to `"UTC"` —
`today_for_user` then buckets by the UTC calendar date, while the assertions independently computed
bare `date.today()` (local/server timezone). The two only diverge for a several-hour window every
day, whenever UTC has already crossed into the next date but local hasn't — real and recurring, not
a calendar-day coincidence.

Fixed by making both tests compute their expected bucket key via `today_for_user(USER)` — the same
function `ai_usage_service._bump_and_escalate` actually uses — instead of bare `date.today()`, so the
test's assumption matches the service's real contract regardless of what timezone the machine running
it is in. No production code changed — `today_for_user`'s per-user-timezone bucketing (UTC fallback
included) is correct as designed; this was purely a test-assertion bug.

Verified: `test_ai_usage_service.py` 21/21 green, full backend suite **596/596** — first fully clean
run this cycle. `black --check .` and `isort --check-only .` both still clean.

### Files updated

- `app/backend/tests/test_ai_usage_service.py`
- `docs/TASKS.md` (bug entry marked done)

Uncommitted — owner hasn't asked for a commit this session yet.

## TASKS.md full reorganization (interactive, via AskUserQuestion)

File had grown to 620 lines and was genuinely unwieldy — a 65+ entry, ~2.5-month "Done" log with
paragraph-length write-ups, several already-completed items still sitting in "Now" instead of "Done"
(Custom Dashboards saga, Host Fix, user-deletion bug, AI usage counter), and a 219-idea backlog scored
P0-P3 with inconsistent thematic subsections. Owner explicitly asked to use AskUserQuestion to drive the
reorg rather than have it done unilaterally — went through several rounds:

**Removed on request**: the What's-New auto-catch-up CHECK item, the Finance owner-test-pass item
(walked through and confirmed okay), and the Household/Team task-modal-categories bug (believed already
fixed) — all deleted from Now.

**Structural decisions** (owner picked, each via AskUserQuestion): delete every completed `[x]` item
across the whole file rather than archive it — detail already lives in `CHANGELOG.md`/git
log/Daily Notes, so TASKS.md shouldn't duplicate it; delete the entire "Done" section outright, same
reasoning; keep the Idea Backlog inline in TASKS.md (not split to a separate file) but **reorganize it
by module/theme instead of P0-P3 impact score**, dropping the score tags entirely once categorized;
merge "Security audit remediation" (top of file, down to 2 open items) and "Security follow-ups"
(further down, 5 more) into one "Security" section placed right under Now.

**Prioritization pass** (also via AskUserQuestion, item-by-item): the 3 remaining Now bugs and 3 Now
biz/ops items — no strong preference either way, kept in original relative order; the 5 Security "CHECK"
findings — treated as equally low-priority; of Security's 3 bigger efforts (fail-closed CORS fix vs
app-level 2FA vs infra deploy verification) — **2FA explicitly prioritized**, surfaced ahead of the other
two in the new Security section; Launch surface's existing legal→demo-safety→polish order — no change;
of Features that unblock scale's 15 un-ordered items — **finishing the Chat UX redesign's last 2 items
was picked as genuinely next**, now the first bullet in that section.

**Idea Backlog recategorization**: confirmed a 13-category taxonomy (Growth & Revenue; Trust, Security &
Reliability; AI & Chat Agent; Cross-App UX & Polish; Technical/Architecture/DevOps; Finance; Assets;
Contacts/CRM; Notes & Journal; Automations/n8n; Notifications; Calendar & Tasks; New Module Ideas) before
sorting, specifically to avoid re-sorting all ~219 ideas twice. Every idea from the original P0-P3 list
was preserved and moved into exactly one category (roughly highest-value-first within each), P0-P3 tags
dropped per owner's choice — category placement is now the only signal.

**Consequence, also fixed today**: the doc's own "mark tasks done" convention (in `docs/AGENTS.md`'s
End-of-Turn Memory Check and mirrored in `docs/hooks/docs_reminder.sh`'s Stop-hook checklist text) would
have immediately started re-bloating TASKS.md with `[x]` entries again next session. Both updated to
"remove it when done" instead, matching the new format note at the top/bottom of TASKS.md itself.

Net effect: TASKS.md active sections (Now/Security/Launch/Features/AI-findings/Product-Backlog) now
contain only genuinely open work, with the two most-actionable sections (Now, Security) reflecting real
owner priority signal instead of arbitrary doc order.

### Files updated

- `docs/TASKS.md` (full rewrite)
- `docs/AGENTS.md` (End-of-Turn Memory Check wording)
- `docs/hooks/docs_reminder.sh` (Stop-hook checklist wording)

Uncommitted — owner hasn't asked for a commit this session.
