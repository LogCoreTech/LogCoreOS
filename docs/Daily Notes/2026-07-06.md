# 2026-07-06 — Daily Note

*(Product work only — this note was rewritten 2026-07-06 when non-product content moved to a separate private log.)*

## Session — docs_reminder.sh sync nudge

- `docs/hooks/docs_reminder.sh` — TASKS.md-change detection added to the early-exit branch; core-docs mtime check extended.
- `docs/AGENTS.md` — memory table row added for the sync rule.

## Session — AI provider live on dev

- Anthropic API key configured via Admin → AI Provider on the dev instance; agent confirmed working end-to-end (tool use, Brain reads/writes).
- Open registration confirmed OFF on the dev instance before the key went live.
- Decision: the public demo instance (launch-surface work) ships without an AI key until the per-user daily message cap is built.

## Session — roadmap folded into planning docs

- Product roadmap/vision session processed. Product decisions locked:
  - **RAG index design rule:** when RAG over the Brain is built, the vector index must be a disposable derived cache — embedded file-backed store (e.g. Chroma), rebuildable from Brain files at any time, never a source of truth, no new stateful service (compatible with the no-database philosophy; PROJECT.md already permits derived indexes).
  - **Product milestones v0.2 → v1.0 adopted** (v0.2 credibility + RAG; v0.3 digest/capture upgrades; v0.4 AI-built n8n automations; v1.0 trust stack gates the Show HN post).
  - Backlogged: quick capture, offline-first sync, journal insight loops.
  - Corrections vs the roadmap doc (it was stale): proactive digest engine, per-workspace AI context, basic recurring tasks, and workspaces/Team/HA/n8n are already shipped.

## Session — CI repair: format fix, stale tests, badge

- GitHub About section filled (description + ~16 topics).
- **Discovery: CI existed since Jun 27 and failed all 92 runs** on the black format check (frontend job always passed). black + isort applied via python:3.11 Docker (`cca9210`).
- Test run then showed 4 failures — **stale tests, not bugs**: they encoded the old immediate-archive task lifecycle. Rewritten to the documented nightly-archive behavior (backdate `completed_at` + run `process_user()`); companion test added asserting today's completions are untouched. CI badge added to README (`ba261eb`).
- Suite verified green in a Python 3.11 container: **186 passed**.

## Session — chat agent bugs logged (log-only)

- From owner testing: (1) agent writes raw first names into `assigned_to` without resolving against pool members; (2) saved chat archives lose the main AI response, keeping only the lead-in sentence (suspect auto-save/step-trace assembly); (3) feature: default "approve edits" chat mode (per-write approval). All logged in TASKS.md under active build work.
- Shared notes with per-user permissions logged to Product Backlog.

## Session — v0.1.0 released, CI green

- **Actions green for the first time in 93 runs**; badge live. CHANGELOG stamped `[0.1.0] — 2026-07-06`; **GitHub Release v0.1.0 published**. Built-in updater now has a live target.

## Session — agent member-name resolution fix (bug 1 of 2)

- `services/agent_service.py`: new `_resolve_member_name()` — resolves a raw name against `auth_service.list_users()` names: exact full name → exact first name → first-name prefix, all case-insensitive. Ambiguous or unknown → returns an error listing candidates and telling the agent to ask the user, not guess.
- `add_shared_task` / `update_shared_task` tool branches now resolve `assigned_to` before writing; explicit `null` still clears an assignment. Tool schema descriptions updated to steer the model.
- New `list_household_members` agent tool (admin, read-only, included in research mode) mirroring `GET /shared/members`.
- Tests: `tests/test_agent_member_resolution.py` — 16 tests covering the resolver and executor integration. Full suite **202 passed**; black + isort clean on changed files.
- Environment note: local venv on Python 3.14 needed unpinned deps + `bcrypt==4.0.1` (passlib incompat) — CI on 3.11 remains source of truth. Docker unavailable to this session (user not in docker group; sudo denied).

## Session — chat archive truncation fix (bug 2 of 2)

- Root cause was **not** the step-trace assembly (original suspicion) — the backend saves full content. The archive **parse** was lossy: `parseSavedChat()` in `Chat.jsx` kept only lines starting with `**You**:`/`**AI**:` and dropped all continuation lines of multi-line messages. Continuing a saved chat loaded the truncated parse, and the 1.5 s auto-save then **overwrote the same archive** with it — permanent loss, lead-in line kept. Parser now accumulates continuation lines into the current message.
- Saved-chat viewer drawer had the same per-line rendering (`return null` on continuation lines) — now renders bubbles from `parseSavedChat()` with `whitespace-pre-wrap`.
- Adjacent cause: `HistoryMessage.content` cap of 5000 chars made auto-save 422 silently for long agent/research responses (catch is deliberately silent). Raised to 30,000 across `/chat`, `/chat/save`, `/chat/save-memory` so long responses save AND continued chats containing them pass validation. User input stays capped at 5000 via `ChatRequest.message`.
- Verified: frontend builds; Node round-trip script (serialize → parse → serialize → parse, multi-line + multi-paragraph messages) all-preserved; backend suite 202 passed; black/isort clean.
- **New bug found + logged in TASKS.md:** proactive notification injection makes chat history start with an assistant message → `/chat` alternation validator 422s; injection also triggers a junk auto-save archive.
- Bug 1 (member resolution) committed as `ba07707`; push to origin blocked by session permissions — owner to push or approve.

## Session — status sync (owner, outside dev session)

- Both bug-fix commits (`ba07707`, `95d4875`) pushed by owner; **CI passed** on Python 3.11.
- GitHub Discussions: already enabled — marked done in TASKS.md.
- LinkedIn company page: **blocked** — LinkedIn requires a registered company (LLC); revisit after LLC formation (business-side).

## Session — notification-injection chat bug fixed

- Proactive notifications injected into the chat thread are now **display-only**. New module-level `toApiHistory()` in `Chat.jsx` filters `_proactive` messages and trims history to the `/chat` validator's required shape (drop leading non-user, drop trailing non-assistant) — this also heals older archives that were saved with leading notification messages when continued.
- Auto-save now filters proactive messages and skips entirely when the thread contains nothing else — no more junk archives from a notification landing on mount.
- Frontend rebuilt clean. Backend untouched (no test rerun needed).

## Session — approve-edits chat mode (new default)

- New `approve` chat mode, now the default (backend `ChatRequest.mode`, frontend `chatMode` state + `api.js` default). Reads run freely; the first response containing a write tool call breaks the agent loop with status `awaiting_approval` and `pending_write` steps — nothing executes that turn.
- UI: new ApprovalCard (mirrors ProposalCard) lists pending writes with humanized tool name + inputs; Approve re-sends "Yes, go ahead." as a **one-turn auto** request (`send()` gained a `modeOverride` param); Deny replies "No — don't make those changes." staying in approve mode. Mode drawer gains "✓ Approve" as first entry.
- Write-vs-read decided by new `_READ_TOOLS` allowlist (= `_RESEARCH_TOOLS` + `get_home_state`); anything unlisted is approval-gated, so future tools fail safe. Guarded by `test_new_tools_are_write_gated_by_default`.
- System prompt gets an approve-mode block: call tools normally, keep lead-ins short, don't retry after a decline.
- Tests: `tests/test_agent_approve_mode.py` (5 tests, mocked `agent_completion`). Full suite **207 passed**; black/isort applied; frontend builds.
- **Assets module logged** in TASKS.md ("Features that unblock scale") — owner flagged REQUIRED BEFORE PILOT CLIENT; scope needs a definition session.

- **Verified live** — stack relaunched (`launch.sh --skip-build`, backend image rebuilt); owner confirmed approve mode works end-to-end in the app (ApprovalCard shows, Approve executes, Deny declines).

## Follow-up

- Next build work: Automation Inbox, then land-lead workflow (see TASKS.md "Now" section).
- Assets module scope definition with owner before pilot client.
