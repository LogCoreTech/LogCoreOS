# 2026-07-04 — Daily Note

*(Product/infra content only — non-product content moved to a separate private log 2026-07-06.)*

## Work Done

Planning session — no code changes.

**Infrastructure planning:** assessed running 2 LogCore instances on the dev machine (HP Pavilion 500-205T, 6 GB RAM capped at 8 GB max — not enough). Decision: use Hetzner CX22 VPS (~€4.50/mo) for the demo instance; personal instance on a future used mini PC or also a VPS. EU latency (~80–100 ms) is acceptable for a web app.

**Hosting strategy:**
- `demo.logcoretech.com` → Hetzner CX22, full stack, open registration
- `app.logcoretech.com` → personal instance (VPS or home mini PC later)
- Cloudflare Tunnel handles routing for both, no separate domain needed
- Infisical: no need to self-host — use Infisical Cloud free tier when needed; not needed for personal or demo

## Decisions Made

- Separate personal and demo onto separate boxes — security boundary, not just RAM
- Demo instance needs AI cost protection (Haiku model or daily message cap) before going live — demo users will burn the Anthropic API budget otherwise
- Automation Inbox moved up in priority — workflows need somewhere to surface results
- TASKS.md reorganized into sections
- Privacy policy + ToS added as blockers before the demo goes public
- Self-hosting an LLM deferred — Anthropic API is far cheaper and simpler at this stage

## Follow-up

Build order: Automation Inbox → lead workflow. Public demo launch runs in parallel.
