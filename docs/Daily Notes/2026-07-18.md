# 2026-07-18 — Daily Note

## Session — managed beta instance: tunnel + launch.sh root-clone fix

Continuation of the 2026-07-17 deploy (tenant/infra details in the private Business repo).

- **Cloudflare Tunnel connected** on the beta instance: token set in `docker/.env`
  (`CLOUDFLARE_TUNNEL_TOKEN`), tunnel container recreated, 4 connections registered. **Public
  hostname not routed yet** — owner still needs to add the Public Hostname mapping
  (hostname → `http://localhost:8000`) in Cloudflare Zero Trust → Tunnels.
- **Fixed the root-clone permission bug in `launch.sh`** (found on yesterday's clean-VPS deploy):
  new `fix_brain_ownership()` runs before `launch_containers` — chowns `brain/` to 1000:1000
  (app container user) when the script runs as root; warns with the exact command on any other
  uid mismatch. `bash -n` clean. CHANGELOG (Unreleased → Fixed), MEMORY.md Known Gotchas entry,
  TASKS.md bug → Done. **Not committed** — owner hasn't asked; ready to commit+push on request.
- **Added `launch.sh --tunnel-token <token>`** (owner asked how a new instance gets its tunnel
  token when the app is localhost-only pre-tunnel): the flag writes `CLOUDFLARE_TUNNEL_TOKEN`
  into `docker/.env` (wins over `brain/hosting.json`), making a fresh VPS fully reachable in two
  commands — `git clone …` + `bash launch.sh --install-deps --tunnel-token <tok>`. Missing-value
  and unknown-flag errors tested; AGENTS.md flag list + CHANGELOG updated.
- Gotcha from the token write: setting a long secret over SSH via an inline sed/printf fallback
  corrupted the value (typo in the duplicated string). Correct pattern: pipe the secret via stdin
  (`printf '%s' "$TOK" | ssh host 'tok=$(cat); sed -i ...'`) and verify with md5sum on both ends.

Remaining for the instance (tracked in Business repo): Cloudflare public hostname mapping,
dedicated Anthropic API key + spend alert, admin + tenant accounts, Admin → Hosting hardening
(cookie_secure, trust_proxy_headers, domain_url), backups toggle + cron verification.
