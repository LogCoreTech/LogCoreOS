# 2026-08-29

## Goals Round 2 — 11 owner-reported issues fixed in the same still-uncommitted changeset as the 2026-08-28 module build

The owner tried the Goals module built the previous day and came back with an 11-item list from
actually using it — bugs, real UX complaints, and two feature requests. This is that list, worked
through in one sitting, still inside the same uncommitted changeset as the original build (no commit
happened between the two rounds). Grouped below by kind rather than in report order, since several
items turned out to share a root cause once investigated.

### Bugs fixed

**Nested-modal clipping — a real, pre-existing latent bug, not something this build introduced.**
`.modal-card` (in `index.css`) applies `.card`, which uses `backdrop-blur-sm` — CSS `backdrop-filter`
on an ancestor element creates a new containing block for any `position: fixed` descendant, so a
modal that's nested INSIDE another modal's own DOM sizes and clips against the OUTER modal's box
instead of the real browser viewport. Two real instances existed: `GoalModal.jsx`'s own "+
Subgoal"/"+ Task" flows (GoalModal reopening itself, or opening TaskModal, from inside an already-open
GoalModal), and `AssetModal.jsx`'s pre-existing nested `<TaskModal>` (its own "+ Task" quick-create).
Fixed via `createPortal(..., document.body)` — not a new pattern, `components/assetDisplay.jsx`'s
photo viewer already uses the exact same trick for the exact same reason, confirmed by reading it
before writing the fix rather than reinventing one. `GoalModal.jsx` now wraps its own top-level
return in a small local `Portal` helper component, so nesting is self-solving at every depth — the
recursive "+Subgoal" GoalModal-inside-GoalModal case portals itself automatically without any special
casing — and separately portals its delete-confirmation popup. `AssetModal.jsx`'s nested `<TaskModal>`
got the identical `createPortal` treatment applied directly at its own call site. `AssetModal.jsx` has
at least 2 OTHER instances of the identical pattern (`archivePrompt`, `SaveAsTemplateModal`) —
deliberately left unfixed this round, since only the TaskModal nesting matched what was actually
reported; filed as its own small `docs/TASKS.md` Product Backlog item rather than silently expanding
scope.

**Pool task-linking now targets the correct store**, closing Round 1's own explicitly-flagged known
gap. `GoalModal`'s "+ Task"/"Link existing task" actions previously always went through the personal
`tasksApi` regardless of whether the goal being viewed was a household/team pool goal. Fixed with a
new `poolTaskApi(workspace)` helper in `GoalModal.jsx` that dynamically imports
`module_packages/household/frontend/api.js`'s `shared` client (personal workspace) or
`module_packages/team/frontend/api.js`'s `team` client (business workspace) depending on which pool
the goal belongs to. `TaskModal`'s existing `saveApi` prop and a new `listFn` prop added to
`TaskPicker.jsx` (defaults to the personal `tasksApi.list`, override-able) both thread this through.
Two small local wrapper components inside `GoalModal.jsx`, `TaskLinkPicker` and
`PoolAwareTaskCreateModal`, exist purely to resolve the async pool-client import before rendering
`TaskPicker`/`TaskModal` — both of those need a real function reference up front, not a promise, so a
tiny bit of `useState`/`useEffect` plumbing sits in between the dynamic `import()` and the actual
render.

**Appointments were already fully linkable to goals — confirmed, no code change needed.** One of the
11 items turned out not to be a bug at all: `goal_id` was added to Task unconditionally in Round 1
with no restriction by task type, and `TaskPicker.jsx` never filtered by type either, so an
appointment-type task could always be linked to a goal exactly like any other task. Verified by
direct read of both files before reporting back, rather than assuming the report was accurate.

**Weight metric gap.** The root cause: `height_cm`/`weight_kg` are built-in, always-private fields
directly on the Contact record itself (`services/contacts_service.py`'s `_PRIVATE_SHORT_FIELDS`),
invisible to Round 1's `contacts:number_field` provider, which only ever reads `contact["custom"]`.
Fixed with a new, separate metric provider, `contacts:weight` (`module_packages/contacts/manifest.py`'s
`_resolve_weight`), always resolving the caller's own self-contact — no `contact_id` override, since
the field is private by design and only ever readable by its own owner. Owner explicitly asked for
weight only, not height, so no `contacts:height` provider was built alongside it.

**A real math bug caught and fixed BEFORE it shipped, not after.** While building the weight provider,
realized every metric provider built in Round 1 (Finance's `budget_pct` aside, which is a utilization
percentage, not a goal-completion one) silently assumed "higher current value = more progress toward
target." That assumption is backwards for a weight-LOSS goal: you start ABOVE target and progress
means the current value going DOWN, so the original `current/target*100` formula would show 120%
complete on day one and get WORSE the more weight you actually lost. Fixed generically rather than as
a weight-only special case: a new shared `module_registry.directional_pct(current, target,
direction="increase"|"decrease", start_value=None)` function, living in `module_registry.py` rather
than inside `module_packages/goals/` specifically so a module registering its own metric provider —
like Contacts — doesn't have to import from a sibling module's package just for this formula.
"increase" (the default) preserves the original behavior exactly; "decrease" computes progress as
`(start - current) / (start - target)`, which needs an explicit starting point (`start_value`) since a
weight reading, unlike "pages read," has no natural zero baseline to measure from. Both Goals' own
"manual" provider and Contacts' `number_field`/`weight` providers now expose a `direction` + a
`start_value` config field (a `select` + an optional `number`) alongside their existing `target_value`,
and `goals_service.py`'s `_resolve_metric` manual branch was updated to call this shared helper instead
of its own inline `current*100/target`. Weight's own config schema gets a distinct
`_WEIGHT_DIRECTION_FIELD` rather than reusing the shared `_DIRECTION_FIELD` constant — the shared one
lists "increase" first, and the frontend `MetricPicker.jsx` pre-selects `options[0]` whenever a config
has no explicit value yet, which would have shown "Increase" pre-selected in the UI while the backend
was already silently computing "decrease" behind the scenes the moment a `target_value` was set — a
real visual/effective mismatch caught by re-reading the picker's own selection logic before shipping,
not discovered by a user afterward.

### UX rework (real design decisions from the interview, not just bug fixes)

**Tab restructure: ownership replaces depth as the tab axis.** Round 1's "ME" tab (root-level goals
only) is GONE — "ME" now shows all of the caller's own personal goals, at any depth, which is what
the old flat "Goals" tab used to show. A second tab, labeled "Household" or "Team" depending on the
active workspace (not a generic "Pool"), shows that pool's own goals — previously pool goals were
invisibly merged into the same single list, identifiable only by an `_owner` field nobody was actually
looking at, which is exactly what the owner reported as "no idea where the pool goals are." The pool
tab is hidden ENTIRELY via `useAuth()`'s `activeModuleIds`/`user.disabledModules` — the same check
`App.jsx`'s own `ModuleRoute` already uses — when Household/Team isn't installed/enabled for the
viewer; a member without the `pool_edit` write grant still sees the tab, read-only, the same as pool
tasks/notes already work today — it only disappears entirely when the underlying pool module itself
is off. This was an explicit, deliberate choice confirmed during the interview, not a default assumed
here, and it matches every other pool-visible feature's existing behavior in the app.

**Category-grouping headers removed** from the Goals list — each goal already shows its own category
badge on the card itself, so the separate grouping headers were redundant, per the owner's own
observation while looking at the page. The descriptive text that used to sit under the tab switcher
was removed at the same time, for the same reason — it no longer explained anything the UI itself
didn't already make obvious.

**Linking an existing goal as a subgoal** — previously only "+ Subgoal" (create a brand new one)
existed as an option. New `GoalPicker.jsx` (123 lines) mirrors `TaskPicker.jsx`'s own
search-autocomplete shape exactly. Its own `subtreeIds()` helper is a client-side port of the
backend's `collect_subtree_ids` cycle-guard, used to exclude a goal and its own descendants from the
picker's results — re-parenting a goal under its own descendant is guaranteed to 400 server-side
already, so there's no point offering an option that can't work. The picker explicitly ALLOWS
re-parenting any goal, including one that already has a different parent — it shows a match's current
parent title inline ("— currently under 'X'") specifically so moving an already-nested goal isn't a
surprise to whoever's doing it. No backend changes were needed at all for this —
`goals_service.update_goal`'s existing cycle guard from Round 1 already covers the re-parenting case
correctly; this was purely a missing frontend affordance, confirmed by re-reading the router/service
before assuming a backend change was required.

### New features

**Recurring-task completion log + automatic rate-based rollup contribution.** Every recurring Task
now accumulates a capped, dated completion log (`completion_log: [{"date": "YYYY-MM-DD"}]`, capped at
90 entries via `_COMPLETION_LOG_CAP` in `services/task_service.py`) — appended in `update_task()`'s
existing recurring-completion branch, the same place `streak_count`/`last_completed_date` already get
updated, with symmetric removal when a completion is undone on the same day it was logged.
`goals_service.compute_progress()`'s rollup loop (`module_packages/goals/backend/service.py`) now
checks whether a linked task is `type == "recurring"`, and if so calls a new
`_recurring_completion_rate(task)` helper — a count of `completion_log` entries falling in the
trailing 30 days, divided by 30, clamped 0-100 (`_COMPLETION_RATE_WINDOW_DAYS = 30`, a fixed default
rather than a per-goal configuration option, matching the owner's explicit "automatic, no extra
configuration step" ask) — instead of the old binary 100/0 done-check. A non-recurring linked task is
completely unaffected by any of this. Verified live via a real HTTP smoke test rather than only unit
tests: a recurring task seeded with 10 completions spread across a 30-day window correctly rolled up
to its parent goal's own displayed 33% (10/30).

**Shared tag vocabulary for Goals + Tasks.** New core `services/tags_service.py` (44 lines) —
deliberately core, not inside `module_packages/goals/`, since both Goals' own package and core
`task_service.py` need it, the same "a real external consumer keeps a service in core" test this whole
project has used consistently throughout every module conversion this session. `Tags/tags.json` per
store (personal or pool) is a real, persisted, GROWING vocabulary — not live-scanned off existing
records — since the owner specifically wanted the tag search/autocomplete to be driven by a real
accumulating list, so a tag stays suggestible even after the last record carrying it gets deleted.
`register_tags()` is a case-insensitive-dedup, case-preserving union-add (`_VOCAB_CAP = 500`, a
generous ceiling rather than a realistic limit), wired into both `task_service.add_task`/`update_task`
and `goals_service.create_goal`/`update_goal` whenever a tag not already in the vocabulary shows up.
New core `routers/tags.py` (30 lines, `GET /tags?pool=`) mirrors `routers/priorities.py`'s own
login-required-no-module-gate shape exactly, plus a new `tags` export on core `lib/api.js`.
`TaskCreateBase`/`TaskUpdateBase` (`routers/_task_models.py`) and Goals' own `GoalCreate`/`GoalUpdate`
models all gained `tags: list[str] | None`. Frontend reuses the EXISTING `components/TagInput.jsx`
component completely as-is — no changes needed there at all — its `suggestions`-provided, non-strict
mode was already exactly "shows existing tags, typing filters to the closest match, Enter/comma adds
a new one if nothing matches," which is precisely what the owner asked for, confirmed by reading the
component before assuming it would need work. Used in both `GoalModal.jsx`'s edit form and
`components/TaskModal.jsx`'s form (which gained a Tags field entirely — it didn't have one before).
Tag PILL display (not comma-separated text) plus click-to-filter was added to `GoalModal.jsx`'s view
mode, `Goals.jsx`'s `GoalCard`, and `module_packages/tasks/frontend/Tasks.jsx`'s `TaskCard` — clicking
a pill sets a client-side `tagFilter` state that filters the current list, with a dismissable filter
chip shown above the list in both places.

Deliberately scoped to Goals+Tasks only this round: the owner's own broader vision — a header-level
magnifying-glass search bar across the WHOLE app, with tags spanning every module — was pushed back
on as real, separate, much bigger scope (Notes/Assets/Contacts/Journal/Calendar have zero tag fields
today, and a real app-wide search bar is new UI plus a new backend fan-out query, not a filter on one
already-loaded list) and logged as its own detailed Product Backlog entry instead — enriching the
ALREADY-EXISTING "Global search across the whole Brain" backlog line with these specifics (the search
bar concept, tags-as-a-facet), rather than replacing it or creating a duplicate entry alongside it.
**The owner explicitly asked to be reminded to start on this once Round 2 is committed** — noted both
on the enriched backlog line itself and as its own pointer item at the top of `docs/TASKS.md`'s "Now"
section, since this repo doesn't have an existing strict convention for "the owner wants to be asked
about this next" and a visible spot at the top of the active-work queue seemed like the clearest
option available.

### A real, incidental dead-code cleanup

Found while touching `module_packages/tasks/frontend/Tasks.jsx` for the tag-pill work:
`allTasks`'s list comprehension had a leftover `.filter(t => t.type !== 'goal')` from BEFORE Round 1
removed `type=="goal"` from Task entirely — since that type value can never occur on a real Task
anymore, the filter had become a permanent no-op, silently doing nothing on every render. Removed,
along with its now-stale comment ("Goal-type tasks live on the Goals page only").

### Files

Backend: `module_registry.py` (new `directional_pct()`). `module_packages/goals/backend/service.py`
(`_resolve_metric`'s manual branch now calls `directional_pct`; `compute_progress`'s rollup loop gained
`_recurring_completion_rate`; `create_goal`/`update_goal` gained `tags` handling via
`tags_service.register_tags`). `module_packages/contacts/manifest.py` (new `contacts:weight` provider
+ `_WEIGHT_DIRECTION_FIELD`/`_START_VALUE_FIELD` config constants). `services/task_service.py`
(`completion_log` field + `_COMPLETION_LOG_CAP`, `tags` field + vocabulary registration).
New core `services/tags_service.py`, `routers/tags.py` (mounted in `main.py` right after
`priorities.py`, same `/api/v1/tags` shape). `routers/_task_models.py` (`tags` field on
`TaskCreateBase`/`TaskUpdateBase`). Tests: `tests/test_tags_service.py` (6), `tests/test_tags_router.py`
(4), `module_packages/contacts/tests/test_contacts_metric_providers.py` (4, covers both
`_resolve_weight` and confirms `_resolve_number_field` still works with the new `direction` param), 7
new `directional_pct` tests appended to `tests/test_module_registry.py` (now 15 total), 5 new
completion-log/tags tests appended to `tests/test_task_service.py` (now 20 total), 7 new tests
appended to `module_packages/goals/tests/test_goals_service.py` (now 30 total — recurring-rate rollup
×3, reparenting-via-link ×2, tags ×2).

Frontend: `module_packages/goals/frontend/GoalModal.jsx` (new local `Portal` helper wrapping the whole
component + the delete popup, `poolTaskApi()`, `TaskLinkPicker`/`PoolAwareTaskCreateModal` wrapper
components, Tags field + pill display, uses new `GoalPicker.jsx` for "Link existing" subgoal linking).
New `module_packages/goals/frontend/GoalPicker.jsx` (123 lines). `module_packages/goals/frontend/Goals.jsx`
(tab restructure from depth-based Goals/ME to ownership-based ME/pool, category-grouping headers and
tab-switcher description text removed, tag pill + click-to-filter on `GoalCard`).
`module_packages/assets/frontend/AssetModal.jsx` (its nested `<TaskModal>` now renders through
`createPortal`). `components/TaskPicker.jsx` (new `listFn` prop). `components/TaskModal.jsx` (new Tags
field via `components/TagInput.jsx`, `saveApi`-derived pool-vs-personal tag-vocabulary lookup).
`module_packages/tasks/frontend/Tasks.jsx` (dead `.filter(t => t.type !== 'goal')` removed, tag pill +
click-to-filter + dismissable filter chip added to `TaskCard`). `lib/api.js` (new `tags` export).

### Verified

A real end-to-end HTTP smoke test (not just unit tests) verified: goal+task creation with tags
(vocabulary union-add confirmed on both the goal's and the task's own store), the
recurring-completion-rate rollup (10 completions in a 30-day window → 33% on the parent goal, exactly
as expected), and the weight metric's decrease-direction math (started at 95kg, target 80kg, currently
90kg → 33%, matching `(start-current)/(start-target)` by hand-calculation before trusting the live
number). Full backend suite: 1112 passed (up from 1079 before this round — +33 net).
`eslint --max-warnings 0` clean, `vite build` clean — the `Goals-*.js` chunk grew from roughly 19.7kB
to roughly 24.8kB, reflecting the new `GoalPicker.jsx`/tag UI/portal code, with no new orphaned chunks
produced.

**Still not verified**: a real browser click-through — the same gap as every module built this whole
session, no real login credentials available for interactive verification in this environment. This
round in particular changed enough visible UI (the tab restructure, tag pills, the reparent picker) on
top of Round 1's own already-large surface that it deserves real on-device verification before the
owner trusts any of it fully. **Not yet committed** — this documentation pass happens before the
commit, same as every prior module's own build → test → document → commit pattern this session has
followed.

See `docs/MEMORY.md`'s 2026-08-29 entry for the same writeup in that file's own Key Decisions Log
format, and `docs/Daily Notes/2026-08-28.md`'s own Goals section for the full Round 1 design this
round builds directly on top of.

## Goals Round 3 — 2 more owner-reported issues, same still-uncommitted changeset

The owner came back with two more items after trying Round 2's own fixes, verbatim: "recurring
tasks still arent selectable in the link tasks picker. and i want it optional for the recurring
tasks to take actual effect to a goal or only show the completion metric and not effect rate."
The first is a real bug Round 2 didn't actually close — Round 2 shipped `TaskPicker.jsx`'s
`listFn` prop so a pool goal searches the right task store, but never touched the picker's own
cap/ranking logic, so the underlying symptom was still there. The second is a genuinely new
opt-in mechanism, not a bug fix.

### Bug fixed: TaskPicker.jsx's hidden result cap

**Root cause, found only by live reproduction — static reading of `TaskPicker.jsx`,
`GoalModal.jsx`, and every backend file touching task-goal linking found nothing wrong.** The
search-autocomplete dropdown filtered matches by substring, in raw array order (task creation
order), then applied a hardcoded `.slice(0, 8)`. A user with many pre-existing one-off tasks
sharing a common word with a recurring task's title would never see the recurring task in the
dropdown — even searching its exact title — because it got crowded past position 8 by older
matches. Reproduced in a fully isolated, disposable local test instance (port 8001, a scratch
brain directory seeded only with generic template files, obviously-fake local-only secrets),
driving a real headless Chromium browser (`playwright-core`) through the actual UI: registered a
test account, installed Goals/Household directly via a Python one-liner against the test brain
(a module install needs a process restart to take effect, since `register_routers()` only runs
once at boot), then seeded 12 non-recurring "Reading log N" tasks followed by one recurring
"Reading" task created LAST — confirmed the recurring task was completely absent from the
dropdown before the fix, and present (top-ranked) after. Nothing on the owner's real running
instance (port 8000, real data) was touched.

**Fix, both in `TaskPicker.jsx`:** a new module-level `_matchRank(title, q)` helper (~line 4-9)
ranks an exact title match `0`, a starts-with match `1`, any other substring match `2`; the
`matches` computation (~line 56-70) sorts by that rank before display. The `.slice(0, 8)` cap was
removed entirely, since the dropdown already had `max-h-56 overflow-y-auto` (a real scroll
container) from the original build — the cap had simply been defeating that scroll container's
purpose the whole time, an arbitrary leftover from when the component was first written, not an
intentional decision. The owner's own follow-up question — why cap at 8 instead of scrolling
through all of them? — is what escalated the fix from "add ranking, still cap at 8" to "remove the
cap for the filtered case." A first pass left the cap in place for the unfiltered browse view
(empty search text) reasoning it was an unranked "first N" list — same day, the owner pushed back
("i want the empty query field... to still scroll through all tasks. its a convenience factor for
users"), so that cap was dropped too; `matches` is now simply `q ? (filtered+ranked) : all`.

Verified live: searching "Reading" against 26 accumulated matching tasks (2 recurring + 24 old
one-offs, across test runs) rendered all 26 in the scrollable dropdown, both recurring
exact-matches sorted to the top. No backend change — purely `TaskPicker.jsx`, one file.

### New feature: optional recurring-task rollup contribution (`counts_toward_goal`)

**A linked recurring task's contribution to its parent goal's rollup percentage is now optional
per link, not automatic** — directly satisfies "only show the completion metric and not effect
rate." Owner chose via `AskUserQuestion`: newly-linked recurring tasks default to opt-in OFF (not
counting) until explicitly toggled on — not the reverse (opt-out/default-on) — so a recurring task
can be linked purely for visibility/tracking (its own rate still displays) without silently moving
the goal's overall percentage.

New Task field `counts_toward_goal: bool | None` on `TaskCreateBase`/`TaskUpdateBase`
(`routers/_task_models.py`, lines 42 and 73). Default logic lives in exactly two places in
`services/task_service.py`, not duplicated per entry point:

- `add_task()` (lines 47-54) — sets `False` when a task is created already-linked (recurring type
  + `goal_id` set at creation, e.g. the "+ Task" flow inside a goal) and the caller didn't
  explicitly pass the field.
- `update_task()` (lines 82-90) — sets `False` when an update newly sets `goal_id` (a value
  different from the task's current one) on a recurring task, and that same update payload didn't
  explicitly include the field. This single choke point covers every entry path uniformly: the
  frontend's "Link existing task" flow, the "+ Task inside a goal" creation flow, AND the AI's
  `link_task_to_goal` tool (`module_packages/goals/backend/agent_tools.py`), which already calls
  `task_service.update_task(..., {"goal_id": ...})` with no other fields and picks up the same
  default automatically — zero `agent_tools.py` changes needed.

**Backward compatibility**, matching this project's own stated care about migration correctness:
a recurring task linked BEFORE this feature shipped has no `counts_toward_goal` key in its stored
JSON at all. `goals_service.compute_progress()`'s rollup loop (`module_packages/goals/backend/service.py`,
line 345) treats a missing key as `True` via `task.get("counts_toward_goal", True)`, so existing
goals' already-computed progress percentages don't silently change the moment this ships — the
opt-in-off-by-default behavior only applies to newly linked recurring tasks going forward,
confirmed by `test_recurring_task_default_missing_field_still_counts`, which strips the field back
off a task record to simulate pre-feature legacy data.

`_recurring_completion_rate` was renamed to `recurring_completion_rate` (dropped the leading
underscore, `service.py` line 305) since `router.py`'s `GET /goals/{id}` handler (line 212) now
calls it too, not just `compute_progress()` internally — each recurring linked task in that
endpoint's `linked_tasks` response gets an attached `recurring_rate` field (rounded 0-100), so the
frontend can show a recurring task's own 30-day rate even when it's not counting toward the goal's
percentage. `compute_progress()`'s rollup loop (lines 341-348) now only appends a recurring linked
task's rate to `children_pct` when `task.get("counts_toward_goal", True)` is true — skipped
entirely when explicitly `False`; if it was the only linked item, the goal's progress source falls
back through the existing chain (metric wins, else weighted rollup of children, else manual
pending/done toggle) with one fewer contributing child, potentially zero.

**Frontend (`GoalModal.jsx`):** new `toggleTaskCounts(taskId, current)` (line 163, mirrors the
existing `unlinkTask`'s pool-aware API resolution pattern) — calls
`api.update(taskId, { counts_toward_goal: !current })`. The linked-tasks list (previously a
single-line `<li>` per task) was restructured: each recurring task now shows
`(recurring — N% / 30d)` next to its title (line 335, using the new `recurring_rate` field), plus
a checkbox row below it, "Counts toward this goal's progress" (lines 339-348) —
`checked={t.counts_toward_goal !== false}`, so a MISSING field renders as checked/on, matching the
backend's own `True` fallback. **A real bug caught and fixed before shipping, not after**: an
earlier draft used `t.counts_toward_goal ?? false`, which would have shown every pre-existing
linked recurring task as unchecked in the UI even though it still counted server-side — a visual
lie. Non-recurring linked tasks are unaffected — the checkbox and rate badge only render
`{t.type === 'recurring' && ...}`.

### Tests added this round

- `app/backend/tests/test_task_service.py`: 6 new tests —
  `test_recurring_task_created_already_linked_defaults_to_not_counting`,
  `test_recurring_task_created_unlinked_has_no_counts_toward_goal_field`,
  `test_non_recurring_task_created_linked_has_no_counts_toward_goal_field`,
  `test_newly_linking_recurring_task_via_update_defaults_to_not_counting`,
  `test_relinking_same_goal_does_not_reset_explicit_counts_toward_goal`,
  `test_explicit_counts_toward_goal_respected_on_link` (20 → 26 total).
- `app/backend/module_packages/goals/tests/test_goals_service.py`: 2 new tests —
  `test_recurring_task_not_counting_toward_goal_is_excluded_from_rollup`,
  `test_recurring_task_default_missing_field_still_counts` (the legacy-data-simulation test
  described above) (30 → 32 total). Two PRE-EXISTING Round 2 tests
  (`test_recurring_linked_task_contributes_completion_rate_not_binary`,
  `test_recurring_linked_task_with_no_completions_contributes_zero`) needed a one-line change
  each — adding `"counts_toward_goal": True` to their `add_task()` calls — since without it, the
  new opt-in-off-by-default logic would have made them stop testing what they were meant to test
  (the rate computation itself, not the opt-in default).

### Files

Backend: `app/backend/routers/_task_models.py` (`counts_toward_goal` on
`TaskCreateBase`/`TaskUpdateBase`). `app/backend/services/task_service.py` (`add_task()`/
`update_task()` default logic). `app/backend/module_packages/goals/backend/service.py`
(`_recurring_completion_rate` → `recurring_completion_rate`, `compute_progress()`'s rollup loop
gated on `counts_toward_goal`). `app/backend/module_packages/goals/backend/router.py`
(`GET /goals/{id}` attaches `recurring_rate` to each recurring linked task). No changes needed to
`agent_tools.py`.

Frontend: `app/frontend/src/components/TaskPicker.jsx` (new `_matchRank` helper, ranked +
uncapped filtered matches). `app/frontend/src/module_packages/goals/frontend/GoalModal.jsx` (new
`toggleTaskCounts`, restructured linked-task list item with rate badge + checkbox).

### Verified

Both fixes verified live in the same isolated browser test environment (port 8001, disposable
scratch brain, obviously-fake secrets) — not on the owner's real instance. Picker fix: searching
"Reading" against 26 matching tasks rendered all 26, recurring exact-matches top-ranked. Opt-in
toggle: linking a fresh recurring task showed the checkbox unchecked by default; clicking it sent
a successful `PATCH` and flipped it to checked; a recurring task linked under the OLD code (during
earlier testing, before this feature shipped) correctly showed checked by default, confirming
backward compatibility empirically, not just via unit test. Full backend suite: 1120 passed (up
from 1112, +8 net). `eslint --max-warnings 0` clean, `vite build` clean. **Still not committed** —
same one big changeset as Round 1 and Round 2.

See `docs/MEMORY.md`'s 2026-08-29 Round 3 entry for the same writeup in that file's own Key
Decisions Log format.

## Goals Round 1-3 committed; app-wide search bar + universal tags started (Increment 1)

Round 1-3 committed as `608c799` after the owner's explicit "commit" — 79 files, one commit,
still un-pushed (only asked to commit, not push). Immediately after, moved to the reminder logged
at the top of this file's own `docs/TASKS.md` "Now" section: the owner's app-wide search bar +
universal tags vision. Scoped through a full interview (module coverage, tags scope, search
architecture, UI shape) — owner chose the maximal option on every axis: all modules, tags added
everywhere including a module (Finance) that already has an overlapping `category` concept, live
fan-out (no persisted index), an icon-triggered modal. Full plan written to
`/home/logcore/.claude/plans/i-want-you-to-composed-scone.md` (also readable there for the full
per-module breakdown and 8-increment build order) after two parallel Explore agents (existing
tag/search infra + frontend header/layout structure) and one Plan agent produced the initial
draft, independently verified against real code before being trusted.

**Increment 1 — mechanism + UI shell, Tasks + Goals only — shipped and verified live.** New
`SearchProviderSpec` dataclass in `module_registry.py`, the exact shape `MetricProviderSpec`
already established (declared on the OWNING module's own `manifest.py`, discovered centrally,
called generically, never raises): `key`, `label`, `resolve(query, tags, user, workspace) ->
list[dict]`. `ModuleManifest` gained `owned_search_providers`; a new `search_providers()`
discovery function namespaces by `f"{module_id}:{key}"`, identical to `metric_providers()`.

New core `services/search_service.py` — `search(query, tags, user, workspace)` fans out to every
active provider, skipping any module in `user["disabled_modules"]` BEFORE calling `resolve()`
(never after), and degrading a single broken provider to zero results without crashing the whole
response (`try/except` around each call, logged not raised). A real simplification found during
implementation, not anticipated in the plan: `routers/auth.py`'s `get_current_user()` already
resolves `user["disabled_modules"]` to the fully-effective set (role + per-user + not-installed)
BEFORE any router ever sees the request — confirmed by direct read (`auth.py:117-121`) — so
`search_service.search()` needed no second call to `get_effective_disabled()` at all, just read
the already-resolved list straight off the user dict, the same shortcut `agent_service.py`'s own
`_brain_skip()` already takes.

New `routers/search.py` (`GET /search?q=&tags=`), mirrors `routers/tags.py` exactly — login-only,
no module gate, wired into `main.py` next to `tags.router`. Tasks' and Goals' own manifests each
gained a `_search_x` provider function (private, defined directly in the manifest — the same
placement `_resolve_number_field`/`_resolve_budget_pct` already established for
`MetricProviderSpec.resolve`), matching title/notes/category substring + tag-intersect.

Frontend: new `components/GlobalSearch.jsx`, modeled directly on
`module_packages/dashboard/frontend/DashboardSwitcher.jsx`'s own `.modal-overlay`/`.modal-card`
shape (already mobile-safe, dark-mode themed) — an `autoFocus` text input, a `TagInput` in
`strict` mode (zero changes to that component) fed by the union of `tags.list(false)`/
`tags.list(true)`, a 250ms debounce before calling the live `search.query()` API (the one
genuinely new behavior vs. `DashboardSwitcher`'s in-memory filter), results grouped by module with
each module's own icon/label pulled straight from `ALL_MODULES`, click a result → `navigate(
deepLinkUrl(module, recordId))` then close — the exact mechanism `NotifBell`'s own
`navTarget()`/`openTarget()` already uses. New `search` export in `lib/api.js`, next to the
existing `tags` export. `Layout.jsx` gained a new `SearchButton` icon component (same style as
`NotifBell`'s own trigger) placed next to `NotifBell` in all three spots it renders (desktop
sidebar collapsed/expanded, mobile header), plus a new Ctrl/Cmd+K `keydown` listener placed next
to the existing `?`-opens-Help effect — deliberately looser than that guard (fires even while
typing elsewhere on the page, standard convention, not just when nothing has focus).

**Verified live**, isolated test instance, real browser: search icon click AND Ctrl+K both open
the modal; a mixed Tasks+Goals result set groups correctly under two headers with the right
icons; clicking a Goals result navigated to `/goals?goal=<id>` (`deepLinkUrl` round-trip
confirmed, not just unit-tested); a tag-only search (no text query) correctly surfaced both an
"urgent"-tagged task and an "urgent"-tagged goal together; created a second, non-admin test user,
had the admin disable Goals for that user specifically (admins can't restrict their own access —
had to use a second account to test this), confirmed their own Task result still appeared while
NO "Goals" group rendered at all — the disabled-module filtering working end-to-end, not just
passing a unit test with a mocked disabled-set.

New `tests/test_search_service.py` (7 tests: provider discovery for an installed vs. not-installed
module, empty-query-and-empty-tags short-circuit, a real title match, disabled-module skip, tag
filtering, a broken provider degrading to `[]` without crashing). `eslint --max-warnings 0` clean,
`vite build` clean.

7 increments remain (Household/Team pool providers; Contacts; Assets; Calendar; Finance; Notes;
Journal — the last two needing new sidecar files for tags since they're markdown, not JSON
records, and Journal separately needs a `recordParam` deep-link fix it's never had). Continuing
incrementally, same rhythm as the 13-module Mod Store rollout.

## App-wide search bar + universal tags — Increments 2-8, all shipped

Continuing straight on from Increment 1 above, same session. Each increment below was built,
covered by new backend pytest coverage, and verified live in the same isolated test instance/real
browser this whole rollout has used throughout — never the owner's own running instance. Full plan
at `/home/logcore/.claude/plans/i-want-you-to-composed-scone.md`.

### Increment 2 — Household + Team pool search providers

`module_packages/household/manifest.py` and `module_packages/team/manifest.py` each declare 3
`SearchProviderSpec` entries (`tasks`/`goals`/`events`, e.g. `household:tasks`) — thin wrappers
around the same `task_service.list_tasks("_household", "personal")` /
`goals_service.list_goals("_household", "personal")` / `events_service.list_events("_household",
"personal")` reads `household/backend/dashboard_block.py`'s own resolvers already make (Team
mirrors this with `"_team"`). This proves out the load-bearing architectural bet Increment 1's own
writeup flagged but hadn't built yet: pool data is searched via a provider OWNED by the POOL
module, not bolted onto Tasks'/Goals'/Calendar's own providers — Tasks' own `/tasks` list only
ever surfaces pool items *assigned to* the viewer, and `search_providers()` namespaces by the
OWNING manifest's module_id, so a household provider is automatically skipped when Household is
disabled for a user with zero extra logic in `search_service.py`.

New shared helper landed here, not in Increment 1: `module_registry.search_match(query, tags,
haystack, own_tags) -> bool` — the substring + tag-intersect logic every provider needs, factored
out for the same reason `directional_pct()` already lives in `module_registry.py` rather than
inside one module's own package. Tasks' and Goals' own Increment-1 providers (`_search_tasks`/
`_search_goals`) were refactored to call it too, replacing their original inline duplicate code.

New tests: `test_household_providers_discovered_when_installed`,
`test_search_finds_household_pool_task`, `test_search_respects_disabled_household_module`
(confirms disabling Household hides pool results while the caller's own personal Tasks/Goals
results still show), `test_household_and_personal_tasks_dont_leak_into_each_other`.

### Increment 3 — Contacts

`create_contact()`/`update_contact()` (`services/contacts_service.py`) gained `register_tags()`
calls — closing a real, grep-confirmed gap: Contacts already had a `tags` field (used only in
`ContactModal.jsx`'s own client-side substring filter) that had never once called into the shared
vocabulary Goals/Tasks already use. New `_search_contacts` provider matches name/notes/address/
core_values, deliberately NOT reusing `contacts_service.search_contacts()` — that function already
has its own shipped consumer (an AI tool), and widening its contract for an unrelated caller risked
an unrelated behavior change. No new frontend UI needed — the tag input already existed; this
increment only wires its output into the shared system.

**Verified live**: created a contact with `tags: ["vendor-tag"]` against the real API in the
isolated test instance and confirmed it landed correctly in `_household/Tags/tags.json` on disk —
which surfaced a real, non-obvious existing default worth documenting along the way, not a bug:
`ContactCreate.pool` defaults to `True`, so an unqualified create lands in the household pool store
by default, not the personal one.

New tests: `test_create_contact_registers_tags_into_shared_vocabulary`,
`test_create_contact_with_no_tags_registers_nothing`, `test_update_contact_registers_new_tags`
(`tests/test_contacts.py`), plus `test_contacts_provider_finds_matching_contact` in
`test_search_service.py`.

### Increment 4 — Assets

New `tags: list[str] | None` on `AssetCreate`/`AssetUpdate`
(`module_packages/assets/backend/router.py`). `create_asset()` (`services/assets_service.py`)
stores a cleaned tags list and registers them; `update_asset()` follows the same before/after
`changes`-tracking pattern the function already uses for `name`/`notes`, so a tags edit shows up in
the asset's own change-history log like any other field edit. New `_search_assets` provider
searches name/notes/`fields{}` values, extending the existing `search_assets` AI tool's own
haystack shape rather than duplicating it. No migration — a pre-existing asset simply has no `tags`
key, read identically to `[]` via the same `.get("tags") or []` defensive pattern used throughout
this codebase.

Frontend: `AssetModal.jsx` — `tags` added to form state, a `tagSuggestions` effect keyed on
`[isPool, form.owner]`, a `TagInput` field after Notes, `tags: form.tags` on both create/update
payloads; `AssetView.jsx` — read-mode tag pill display added right after the Notes display block.

New tests: `test_create_asset_with_tags_registers_shared_vocabulary`,
`test_create_asset_without_tags_has_empty_list`, `test_update_asset_registers_new_tags`
(`tests/test_assets_service.py`), plus `test_assets_provider_finds_matching_asset`.

### Increment 5 — Calendar

New `tags: list[str] | None` on the shared `EventCreate`/`EventUpdate` models
(`routers/_event_models.py` — the one CORE file this rollout touches) — shared by `calendar.py`
AND Household's/Team's own pool event endpoints, so pool events get the field for free with zero
changes needed there. `add_event()`/`update_event()` (`services/events_service.py`) gained tags
handling + `register_tags`. New `_search_calendar` provider searches title/notes.

Frontend: `components/EventModal.jsx` — also core, used for personal AND pool events via its
`saveApi`/`poolSaveApi`/`isHouseholdEvent` props — gained `tags` in form state (flows into the save
payload automatically via the existing `{...form, ...}` spread, no separate payload wiring needed
unlike Assets), a `tagSuggestions` effect keyed on `[isHouseholdEvent, shareToPool]`, a `TagInput`
field after Notes.

New tests: `test_add_event_with_tags_registers_shared_vocabulary`,
`test_add_event_without_tags_has_empty_list`, `test_update_event_registers_new_tags`
(`tests/test_events_service.py`), plus `test_calendar_provider_finds_matching_event`.

### Increment 6 — Finance (the most architecturally involved increment)

New `tags: list[str] | None` on `TransactionCreate`/`TransactionUpdate` ONLY — a deliberate,
owner-confirmed scope call, documented in the manifest's own docstring: Finance already has a
closed, per-book-validated `category` concept overlapping semantically with tags, so the owner was
asked specifically whether Finance should still get real tags anyway; confirmed yes "for full
consistency," so only Transactions (the highest-volume, most naturally taggable record) got the
field — Invoices/Clients/Budgets didn't, keeping the increment bounded. `add_transaction()`/
`update_transaction()` (`services/finance_service.py`) gained tags handling + `register_tags`.

`_search_finance` is the most complex of all 8 providers: it iterates every book the caller can see
via `list_visible_books()`, resolves each book's real store user via a new local helper
`_book_store()` (reads the book's own denormalized `_owner` field, mirroring the exact lookup
`dashboard_blocks/_finance.py`'s own resolvers already do), then searches that book's transactions
(tag-aware) plus invoices and clients (full-text only — a tag-only search explicitly `continue`s
past both, an honest non-match rather than silently ignoring the facet, since neither has a tags
field).

**A real, load-bearing fix made during implementation, not anticipated in the plan**: `Finance.jsx`
only ever reads `?book=`/`?view=` — there is no per-transaction/invoice/client deep-link route
anywhere in the frontend, confirmed by reading its `useSearchParams` handling directly. Returning a
matched record's own id as `record_id` would have produced a silently-broken click-through
(`/finance?book=<transaction-id>`, no book with that id). Fixed by using the matched record's own
BOOK id as `record_id` for every Finance result regardless of which record type matched — the most
precise real navigation this frontend currently supports — locked in by
`test_finance_provider_finds_matching_transaction_and_returns_book_id`, which explicitly asserts
`record_id == book["id"]`, not the transaction's own id.

Frontend: `TransactionModal.jsx` — `tags` state, a `tagSuggestions` effect keyed on `book._owner`,
a `TagInput` field after Notes, `tags` added to the save payload.

New tests: `test_add_transaction_with_tags_registers_shared_vocabulary`,
`test_add_transaction_without_tags_has_empty_list`, `test_update_transaction_registers_new_tags`
(`tests/test_finance_service.py`), plus the book-id-assertion test above.

### Increment 7 — Notes

The first increment needing genuinely new storage infra, since a note is a Markdown FILE with no
structured-field mechanism at all (confirmed: no frontmatter parsing exists anywhere in this
codebase). New sidecar `Notes/_tags.json`, path-keyed, an exact mirror of the existing
`_shares.json`/`_archive.json` sidecar pattern already in `services/notes_service.py` —
`_tags_file()`/`load_note_tags()`/`get_note_tags()`/`set_note_tags()` (the last also calling
`register_tags`), plus `_drop_note_tags_entry()`/`_drop_note_tags_prefix()` wired into
`delete_note()`/`delete_folder()` at the exact same call sites `_drop_archived_entry()`/
`_drop_archived_prefix()` already use. `move_item()` doesn't carry archive status forward on a
rename either (confirmed by reading it), so not carrying tags forward on move matches this sidecar
pattern's own pre-existing behavior rather than opening a new gap.

`list_notes()` now denormalizes `tags` onto every item the same way `archived` already is (one
batched `load_note_tags()` lookup after the tree walk, not a per-item file read); `get_note()`
gained `tags` too. New `PUT /notes/tags` (`TagsRequest{path, tags}`), deliberately gated at
"contribute" access rather than "edit" — tagging a shared note reads closer to a content edit than
a structural action like move/delete/reshare.

`_search_notes` is the first provider to search real CONTENT, not just titles — via a new shared
`module_registry.search_snippet(text, query) -> str | None` (±100/+200-char excerpt around the
first match), extracted from `agent_service.py`'s own `search_brain` tool, which had the identical
logic as a local closure and so couldn't be imported as-is — a clean extraction, not a duplicate.
Resolves each note's real store via the EXISTING `notes_service.store_for_owner(owner, viewer)` —
found during implementation to already do exactly what Finance's own provider had to hand-write as
`_book_store()`, so Notes' version came out simpler than Finance's.

Frontend: `notes/frontend/api.js` gained `setTags`; `Notes.jsx` — a `tagSuggestions` effect keyed
on `note?.path`, an editable `TagInput` row shown when the viewer has write access, a read-only
tag-pill row shown otherwise, `saveTags()` calling the new endpoint with an optimistic local
update.

New tests (`tests/test_notes_service.py`, 6 — not the 7 the original plan guessed):
`test_set_note_tags_registers_shared_vocabulary`, `test_note_with_no_tags_has_empty_list`,
`test_set_note_tags_appears_in_list_notes`, `test_set_note_tags_to_empty_removes_entry`,
`test_delete_note_clears_tags_entry`, `test_delete_folder_clears_nested_tags_entries`, plus
`test_notes_provider_finds_matching_note_content`.

### Increment 8 — Journal

Genuinely new infra a second time — unlike Notes, Journal had NO JSON store at all before this, not
even an existing sidecar to mirror within the same file, so `_tags_file()`/`load_entry_tags()`/
`get_entry_tags()`/`set_entry_tags()` (`module_packages/journal/backend/service.py`) are the first
JSON-backed storage this module has ever had; deliberately stays INSIDE the package rather than
promoted to core, since no external file imports `journal/backend/service.py` today. `list_entries()`/
`get_entry()` now include `tags`; `delete_entry()` cleans up its own sidecar entry via
`set_entry_tags(..., [])`. New `PUT /journal/{date}/tags`; new `_search_journal` provider reuses
`search_snippet()` the same way Notes' own does.

**A real, separate, load-bearing gap fixed as part of this increment, not left as a dead click**:
`app/frontend/src/module_packages/journal/manifest.js` had NO `recordParam` field at all —
confirmed by direct read, the only converted module in the entire app with no way to deep-link to a
specific record at all. Fixed by adding `recordParam: 'date'`; `Journal.jsx` now reads `?date=` via
`useSearchParams` to set its initial selected date on mount, instead of always defaulting to today
with no way to land on a specific past entry via URL.

**Verified live in the isolated test browser** — flagged in the original plan as "a real open
question, not a formality" since it was untested territory: created a journal entry, searched for
it via the new search bar, clicked the result, and confirmed the browser actually navigated to
`/journal?date=2026-05-01` AND displayed that exact entry's real content, not just landed on the
Journal page in general — the single most load-bearing live verification of the whole 8-increment
rollout, the one increment where "does the click-through even functionally work" was a genuine
unknown rather than a mechanical repeat of six already-proven cases.

Frontend: `journal/frontend/api.js` gained `setTags`; `Journal.jsx` — initial `date` state now
reads from `searchParams.get('date')`, a `tagSuggestions` effect (Journal is personal-workspace-only
per its own manifest, so no pool-vocabulary branching needed, unlike every other module's tag UI),
a `TagInput` row added to the entry editor, `saveTags()`.

New tests (`module_packages/journal/tests/test_journal_service.py`, 5):
`test_set_entry_tags_registers_shared_vocabulary`, `test_entry_with_no_tags_has_empty_list`,
`test_get_entry_includes_tags`, `test_list_entries_includes_tags`,
`test_delete_entry_clears_tags_entry`, plus `test_journal_provider_finds_matching_entry_content`.

### Cross-increment final verification

After all 8 increments were built, one last pass in the same isolated test instance/browser used
throughout this whole rollout: created one distinctly-named record in EACH of Tasks, Notes,
Journal, Contacts, Assets, and Calendar (all sharing the word "zephyr"), opened the search modal
with a single Ctrl+K, typed "zephyr" once, and confirmed all 6 modules' results appeared correctly
grouped under their own module header with the right icon — a real, combined, cross-module fan-out
working end-to-end in one screenshot, not 6 separate isolated checks. Then specifically
re-verified the Journal click-through in isolation (see Increment 8 above).

**All 8 increments are now shipped — the rollout is complete.** Full backend suite: 1160 passed (up
from 1127 after Increment 1, +33 net across Increments 2-8). `eslint --max-warnings 0` clean, `vite
build` clean on every frontend-touching increment (2, 4-8).

See `docs/MEMORY.md`'s 2026-08-29 entry (same App-wide search bar + universal tags Key Decisions Log
entry Increment 1 lives in) for the same writeup in that file's own format.
