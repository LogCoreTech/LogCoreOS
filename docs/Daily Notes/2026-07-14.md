# 2026-07-14 — Daily Note

## Session — 20-item feedback batch + Contacts (CRM) module (3 phases)

Owner delivered a 20-item feedback/feature list; interviewed him across 9 rounds (plan mode) before building. Full plan: `~/.claude/plans/alright-i-have-a-fluffy-lemon.md`. Built in three phases, each ending green.

### Phase 1 — quick wins (suite → 406 green)
1. **Default priority "God" → "Religion"** — renamed in every auto-generation source (`profile_service.DEFAULT_PRIORITY_ORDER`, `Setup.jsx`, `Profile.jsx`, `_template/Profile.md`, `_household/profile.json`, skill docs) + added a `Religion` key to `CATEGORY_COLORS` (kept `God` for back-compat). Existing users' saved priorities untouched.
4. **Show-archived-books toggle** in Finance (+ Unarchive). Backend already supported `?include_archived`; only the UI was missing.
5/9. **Workspace-aware category + tax-bucket defaults** — `finance_service.default_categories(ws)` / `default_tax_categories(ws)`; personal keeps Groceries+Salary (tests depend on it), business gets accounting buckets; tax buckets Schedule-C (business) / common-deduction (personal).
6. **Payee label** flips "Paid to" (expense) / "Pay from" (income).
10/11. **Deductible + tax bucket on recurring & planned items** — fields added to `finance_planning_service`; match_bill propagates the flags onto the matched tx.
12. **Un-check completed goals** — `toggleDone` in Goals.jsx.
17. **Chat greeting** → "Hi, <name>, what can I help you with today?"

### Phase 2 — medium (suite → 398 green)
13. **Goals overhaul** — goals excluded from the nightly sweep (`recurring_service`); **"Clear completed"** button archives them to history (`task_service.cleanup_done_goals`, `POST /tasks/goals/cleanup`); **timeline filter** (Today/Week/Month/Quarter/Year/Undated by target date) with the progress "X of Y" reflecting the window; **date required** to create a goal (TaskModal + `_task_models` validator).
14. **Setup Life Priorities: ↑/↓ reorder buttons + Skip** (the beta-tester blocker — HTML5 drag didn't work on mobile).
15. **Agent priority-awareness** — `chat._build_context` injects the workspace-correct + pool priority lists; tool_guidance tells the agent to consult priorities before tasks/goals and require a date for goals; new `get_priorities(scope)` read tool.
16. **Chat toolbar icons** — memory buttons → 🧠/⏳/📚 icon-only + tooltips; mode selector fixed-width, drops the word "Mode", Plan gets 📋 (row no longer reflows).
19. **Notes drag-and-drop** — pointer-based (mouse threshold + touch long-press), drop onto a folder to move; menu fallback kept.

### Phase 3 — large (suite → 417 green)
- **Contacts (CRM) module** (`id: contacts`, both workspaces, member-on/guest-off via m008): `contacts_service.py` (contacts/interactions/deals/custom-fields/pipeline + asset-style sharing) + `contacts_index.py` (share-routing cache) + `routers/contacts.py` (CRUD, sharing handshake, CSV import/export, **write-focused n8n automation API — no bulk export**, contact money view scoped to the viewer's finance access) + `pages/Contacts.jsx` (list/detail/interactions/deals + settings/import) + 6 agent tools (reads free, writes approval-gated, **dedup search on create**) + follow-up reminders (`run_followup_reminders`, nightly `job_contacts_followups` at 08:00). Sharing mirrors finance/assets (read/contribute/edit, by-name>group, hidden_from beats, handshake for personal / contributors for pool). 13 tests.
  - **7. Payees are Contact-linked** — transactions carry `payee_contact_id`; `ContactPicker` (search-first, quick-create) in TransactionModal; bank/CSV import auto-suggests a payee contact (`_suggest_payee_contact`); per-payee totals via `GET /contacts/{id}/finance`.
  - **8. Invoice client form replaced** by the Contact picker (find-or-create a book client linking `contact_id`; ClientModal removed). No legacy migration (finance still owner-testing).
- **2. Pool bank accounts** — clarified/surfaced: `＋ Account` affordance on the (pool-labelled) Balance card; account management stays admin + finance contributor-edit (finance never consults pool_edit).
- **3. n8n lifecycle** — `n8n_service.reconcile()` starts the bundled container on the first stored workflow, stops it when idle + no external, stops it when an external instance is attached; Admin **force-on** toggle; triggers on config save / workflow import / delete + boot reconcile (`job_n8n_reconcile`).
- **18. Notes sharing** — sidecar `Notes/_shares.json` index (keeps .md portable) + `notes_index.py`; folder shares cascade to the subtree; household/team pool notes; read/contribute/edit + handshake; router routes reads/writes through access resolution; frontend share modal + Leave + read-only editor + owner badges. 6 tests.

### Verified
- Backend **417 passing** (`.venv/bin/pytest`), black clean; frontend build clean (80 modules). New migration **m008** disables Contacts for the guest role on existing installs. Share indexes (contacts/notes) warmed at boot in `main._warm_share_index`.

### NOT deployed
Backend baked into the image — owner must run `bash launch.sh --skip-build` (m008 runs on relaunch).

### Follow-ups
- Owner test pass: Contacts CRUD + share-contribute handshake (curl-verify server-side), payee autocomplete + contact money card, invoice→contact picker, goal timeline/cleanup/mandatory-date, setup ↑/↓+Skip on mobile, notes share/leave + pool notes, n8n auto start/stop.
- Deferred polish: deal→invoice deep-prefill (currently links to /finance?view=invoices), contacts kanban drag (stage select works), cross-workspace single-contact toggle (owner shelved — separate stores for now), pool contact pipeline admin editor.
