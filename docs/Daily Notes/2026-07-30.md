# 2026-07-30

## HOST FIX task closed out — all three hosts confirmed fixed, not a GitHub outage

Owner asked to run the documented stranded-updater fix (`docs/TASKS.md` "Now" section) on the demo VPS. Verified and fixed across all three hosts named in that task:

- **Demo VPS** (`188.245.44.87`, `logcore-demo1`) — already on the HTTPS remote, already at v0.4.3. Ran `git fetch` + `bash docker/update.sh` anyway to confirm: correctly reported "already up to date," no changes, all containers healthy. Noted in passing: HEAD sits a few commits ahead of the v0.4.3 tag (docs/idea-backlog commits + the mobile-footer-PWA fix) — harmless, but a minor deviation from the intended "pinned to tagged release" model.
- **Beta/Loves server** (`[redacted]`) — owner checked it independently, confirmed already on HTTPS remote and v0.4.3.
- **Owner's PC** (`/home/logcore/LogCoreDEV/LogCoreOS`, this host) — actually still broken: `origin` was still the SSH remote (`git@github.com:...`), and `update_status.json` showed a stale "success" stamped at `version: "0.4.0"` from a failed cron run on 2026-07-29 — the exact documented bug signature. Owner manually rebuilt to get the container back on 0.4.3. Root cause confirmed directly: a plain interactive `git fetch` succeeds fine (shell has SSH key/agent access) but cron's stripped environment can't authenticate — reproduced by fetching under `env -i` (no inherited SSH agent), which also succeeded once the remote was HTTPS, confirming the fix. Switched `origin` to `https://github.com/LogCoreTech/LogCoreOS.git`.

Owner floated a theory that the original incident might've been a GitHub-side outage rather than the SSH-remote/no-cron-key cause already written up in `docs/MEMORY.md`. Pushed back with evidence: an outage would've hit all three hosts identically and wouldn't have been resolved by a remote-URL change — but that's exactly what fixed it here, live, in real time.

**Residual, now closed:** couldn't verify the root-crontab duplicate check myself — `sudo crontab -l` is blocked by the auto-mode permission classifier in this environment even with the password supplied (tried `sudo -n` and a direct read of `/var/spool/cron/crontabs/`, both correctly blocked by permissions/policy rather than a missing password). Owner ran it by hand: no duplicate root entry, only the one `logcore` user cron. Task fully closed.

### Files updated
- `docs/TASKS.md` — HOST FIX task marked done with the per-host outcome

## AI usage counter + caps shipped (TOP PRIORITY task)

Owner picked this as the next thing to build. Scoped via Q&A before writing code: skip automation/n8n tracking (confirmed via code research it's architecturally invisible — n8n calls providers directly with its own credential, never through Python); count all 3 real AI call sites, not just `/chat` as the original TASKS.md note said (chat, save-memory, and custom AI-powered suggestions — the last one reachable from the scheduler, the "run now" button, AND nested inside a chat turn via the agent's own `run_suggestion` tool); tokens-only display, no $ estimate; build hard/soft cap enforcement in the same pass, not just visibility. Mid-session the owner added two more requirements: per-user selectable cap period (daily/weekly/monthly, not just monthly) and a live usage-percentage indicator in both Admin and the Chat module itself.

**Architecture**: instrumented at the single choke point — `ai_provider.py`'s `chat_completion()`/`agent_completion()` — rather than at each of the 3 call sites, so token recording can never be missed by a future caller. Storage is daily buckets (`brain/_system/ai_usage.json`, never in a user's portable Brain folder) with daily/weekly/monthly totals derived on read by summing the right date range, mirroring Finance's "derived values computed on read" rule — this means the per-user cap period is freely switchable with no migration. New `services/ai_usage_service.py`, new `routers/ai_usage.py` (`/api/v1/ai-usage/*`, admin-only except `GET /me`), new `pages/AiUsage.jsx` at `/admin/ai-usage` (linked via a button on the AI Provider card), new usage-% badge + "Continue anyway?" confirm card in `Chat.jsx`.

Full design questions were resolved via `AskUserQuestion` before implementation (limit scope = global default + per-user override; limit metric = admin's choice of messages/tokens/both per user; hard-vs-soft mode = per-user, not instance-wide; soft-cap acceptance holds for the rest of the current period).

**Verified**: 20 new backend unit tests (`test_ai_usage_service.py`) plus the full 494-test suite green; a throwaway end-to-end script drove the real FastAPI app through `TestClient` (register → login → set a hard cap → confirm chat #2 is blocked with zero AI calls made → switch to soft → confirm the confirm-required response → accept overage → confirm subsequent calls go through) — this is full HTTP-level verification of the actual wiring, not just the service layer. Frontend `vite build` and `eslint` both clean. No browser-driven visual check was possible in this environment (no Docker access, no browser tool) — the Admin page and Chat badge have not been visually confirmed on a running instance; owner should eyeball both before considering this fully done.

### Files updated
- `app/backend/services/ai_provider.py` — captures token usage from Anthropic/OpenAI responses, threads `user_name`/`workspace` through `chat_completion`/`agent_completion`, records via `ai_usage_service` after every real call
- `app/backend/services/ai_usage_service.py` — new: daily-bucketed usage storage, period resolution, cap checks, warn/over notifications, admin overview/rows/defaults/limits
- `app/backend/services/agent_service.py` — passes `user_name`/`workspace` into `agent_completion`
- `app/backend/routers/chat.py` — usage gating + recording on `/chat` and `/chat/save-memory`, new `accept_overage` field
- `app/backend/services/suggestions_service.py` — usage gating + recording in `_run_custom_async` (covers scheduler, on-demand, and nested chat-tool triggers in one place)
- `app/backend/routers/ai_usage.py` — new admin + self-service endpoints; registered in `main.py`
- `app/backend/tests/test_ai_usage_service.py` — new, 20 tests; `tests/test_agent_approve_mode.py` — fixed a mock signature broken by the `agent_completion` signature change
- `app/frontend/src/lib/api.js` — new `aiUsage` export, `accept_overage` on `chat.send`
- `app/frontend/src/pages/Admin.jsx` — "AI Usage & Limits →" button on the AI Provider card
- `app/frontend/src/pages/AiUsage.jsx` — new admin page
- `app/frontend/src/pages/Chat.jsx` — usage-% badge, `UsageConfirmCard`
- `app/frontend/src/App.jsx` — new `/admin/ai-usage` route
- `app/backend/content/help.json` — Admin section howto bullet
- `docs/TASKS.md` — task marked done with actual shipped scope; demo daily-cap task annotated (the cap half is now just config)
- `docs/MEMORY.md` — new Key Decisions Log entry (architecture + automation-invisibility rationale)
- `docs/MAP.md`, `docs/API.md`, `CHANGELOG.md` — new files/endpoints documented
