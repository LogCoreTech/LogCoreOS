# 2026-09-04

## AI Settings picker: fixed a real 502 the owner hit switching providers before saving

Owner reported, from real manual testing of the 2026-09-02 AI Settings overhaul: "after i hit load
models for anthropic it loaded them then after i switched to provider on the picker any other attempt
to load returns a 502 error."

Confirmed the root cause with the existing test suite before touching code, rather than guessing —
`test_load_models_never_leaks_a_different_providers_saved_key` already proved the exact failure shape:
switching the picker's provider without saving first, then clicking "Load Models" before typing a
fresh key, makes the cross-provider key-leak guard correctly withhold the old provider's key, but the
code then fell through to the openai-compatible branch's `key or "ollama"` fallback unconditionally —
meant only for genuine keyless local runners, but applied to every provider. A real cloud provider
(whichever one the owner switched to) rejects that fake `"ollama"` key, and the resulting auth failure
got caught by the broad `except Exception` and turned into an opaque 502 with no hint a key was ever
missing.

**Fix**: `load_models()` in `routers/ai_settings.py` now checks the catalog's `list_requires_key` field
right after resolving the key — `False` only for providers with a genuinely public/keyless model list
(local runners, OpenRouter) — and 400s with a clear "type an API key for X first" message instead of
ever attempting the request when no real key is available. Deliberately checked `list_requires_key`,
not `needs_api_key`: OpenRouter needs a key to chat but not to list models, so gating on the wrong field
would have broken its legitimately-keyless fetch. Applies to the Anthropic branch too, not just the
openai-compatible one.

Existing key-leak-guard test updated to assert the new 400 (a stronger proof than before — the fake
client is now never even constructed when no real key exists). Two new tests: one reproduces the exact
reported scenario end-to-end, one confirms genuine local runners (Ollama) are unaffected and still get
the placeholder fallback. 31 AI-settings tests passing, full backend suite reconfirmed clean, `black`/
`isort` clean. No frontend changes needed — the clearer message reaches the UI through the existing
`data.detail` → error-message plumbing already in place. See `docs/MEMORY.md`'s 2026-09-04 entry.

## AI Settings: 400 message clarified to say it's the provider's own requirement

Owner asked whether keyless model fetching was possible at all for providers that need auth, then:
"can you make that clear in the error for those providers that they require it not us." Confirmed first
(Q&A) that this was already provider-by-provider correct — OpenRouter and every local runner already
fetch without a key via `list_requires_key=False`; everything else needs one because that provider's own
`models.list()` endpoint is authenticated, not because of any policy LogCoreOS imposes.

Reworded the 400 from `routers/ai_settings.py`'s `load_models()`: now reads "{label} requires an API
key to list its models — that's a requirement of their API, not ours. Type one in above..." instead of
the more neutral "No API key available for {label}." Tests still pass unchanged (the existing assertion
only checks the provider's label appears in the message). `black`/`isort` clean.

Not yet committed or pushed.

## UX Polish Batch — full triage of the "Cross-App UX & Polish" Idea Backlog (34 items)

Owner asked for the Cross-App UX & Polish idea list explained item-by-item, then ran a structured
interview dispositioning all 34 — approve/respec/drop/defer — followed by a second round closing out
every item that didn't get a clean answer the first pass. Full spec for all 30 approved/specced items
now lives in `docs/TASKS.md`'s new **UX Polish Batch** section; nothing has started building yet, this
was triage only.

**Real research findings during the interview, not just owner preference:**
- Self-contact is deliberately stored in the `_household` pool, not the user's own Brain folder
  (`contacts_service.py:230-247`) — survives account deletion and stays cross-workspace visible for
  free, but breaks if Contacts is uninstalled. Surfaced while scoping the "user avatar" item, which
  grew into a real architectural question (owner wants Profile and Contact to eventually be the same
  record, with per-field privacy) — deliberately parked until the rest of this batch ships. Full
  writeup in `docs/MEMORY.md`'s 2026-09-04 entry.
- Bare `?` already opens Help globally (`Layout.jsx:280`) — the planned keyboard-shortcuts-overlay item
  needed a different trigger, landed on Cmd/Ctrl+/.
- Two items in the original backlog were stale on inspection: priority already renders as a text badge
  (`Tasks.jsx:344`), not color-only as the backlog claimed — retired, nothing to fix. `autoComplete` on
  Login.jsx was confirmed genuinely missing — kept as a real item.
- The chat agent's `capabilities_index()`/`get_help` (`module_packages/chat/backend/router.py`) already
  implements the "AI as feature-discovery layer" half of the progressive-disclosure idea — confirmed
  live and wired into every chat system prompt, not aspirational.

**Dropped/retired outright:** native app-store wrapper (Capacitor — doesn't remove the server
dependency, owner's call not worth building), non-color priority signal (stale premise), split-pasted-
tags in TagInput (owner declined).

**Notably respec'd, not just approved as originally worded:** "this week at a glance" became a
notification + dashboard block instead of a dashboard widget; onboarding became "one example item per
module at account creation" instead of a sample-data toggle; "welcome back" became a middle-screen
popup with an optional AI summary of what changed while away (merging with the existing "AI change-log"
Idea Backlog item, now removed as a duplicate); "today at a glance" became a real dashboard block
instead of a fixed header element.

**Note on session environment:** earlier in this same conversation, prior turns (the feature-discovery
design-decision work, and code verification for the "is it possible to integrate this idea" question)
ran against `/home/user/LogCoreOS`, a path that turned out not to exist in this environment — none of
that reached the real repo. Re-verified every fact this session actually relies on (self-contact
storage, the `?` shortcut, autoComplete, the priority badge, `capabilities_index` wiring) directly
against `/home/logcore/LogCoreDEV/LogCoreOS` before writing anything to these real docs.

Not yet committed or pushed. Next: a second interview round scoping finer implementation detail for
each approved item, in plan mode.

## UX Polish Batch — Tier 1 + Tier 2 built, tested, and verified

Second interview round (plan mode) scoped every approved item into 4 dependency-ordered tiers plus a
mobile/desktop pass and a security/module-boundary review (full plan: the session's own plan-mode
output). Then built and verified, tier by tier, with real backend/frontend tests at each step —
not just clean builds. Full backend suite: 1315 passed (up from 1291), 0 failed. Frontend
`eslint --max-warnings 0` and `vite build` clean throughout.

**Tier 1 (#7 ConfirmDialog, #26 Toast, #8 Escape-to-close):**
- New `components/ConfirmDialog.jsx` — replaced all 34 raw `confirm()`/`window.confirm()` sites across
  20 files (verified count, not the backlog's assumed 14). Plain Yes/No/Cancel, except a
  `requireTypedText="DELETE"` variant built for account self-deletion — no caller yet, since that
  feature doesn't exist; wires in automatically once it does.
- New `lib/toast.jsx` (`ToastProvider`/`useToast`) — success auto-dismisses (~4s), errors persist,
  stacks, portaled above the mobile bottom nav bar. Mounted in App.jsx; not yet adopted by any specific
  feature (infrastructure-complete, ready to use).
- New `lib/useEscapeToClose.js`, retrofitted into ~40 existing modal instances across ~29 files
  app-wide (two background agents, each self-verified + spot-checked), plus the 2 new components.
  Real edge cases handled: view-mode-delegates-to-a-sibling-component guards (TaskModal→TaskView,
  AssetModal→AssetView), a `fullPage`-mode guard (ContactDetail/ContactModal double as plain pages via
  Profile.jsx), and independently-stacked popups (confirm/archive/mute-over-an-open-modal) each getting
  their own hook call.
- Migration work split across 3 background agents following one hand-built reference implementation
  (`AssetModal.jsx`) — two genuinely tricky cases handled correctly: `RoleDefinitions.jsx`'s
  async-computed warning message (kept the `roleUsers` fetch, only the confirm() call itself moved),
  and `ModStore.jsx`'s sequential positive (non-negated) conflict-retry confirm.

**Tier 2:**
- **#12 (fresh-install baseline)** — real bug-shaped fix: `test_fresh_install_leaves_optional_modules_uninstalled`
  proved a fresh instance started with only Tasks/Chat/Dashboards installed. Removed the
  `features.json`-existence guard from journal's m015/calendar's m020/notes' m026/goals' m031 (each
  migration now runs unconditionally, once, matching how the upgrade path already worked) — Journal,
  Calendar, Notes, and Goals now join the baseline. Found and fixed 4 now-stale per-module conversion
  tests that still asserted the old behavior (`test_{journal,calendar,notes,goals}_module_conversion.py`).
- **#25 (welcome-back popup)** — `services/welcome_back_service.py`, `routers/welcome_back.py`,
  `components/WelcomeBackPopup.jsx`. Reuses `presence_service`'s existing ping (not a new per-request
  write). First genuinely AI-generated (not template-text) proactive summary in this codebase.
  Real ordering bug avoided, not just handled: `GET /welcome-back/check` reads `should_show()` BEFORE
  touching presence, so the request's own ping can't race the away-check it's answering — verified with
  a real "second call in the same visit doesn't re-fire" test.
- **#6 (this-week digest)** — real architecture correction mid-build (see `docs/MEMORY.md`'s matching
  2026-09-04 entry): moved off ad-hoc `auth.json` fields onto `suggestions_service.py`'s existing
  `_BUILTIN_DEFAULTS` registry once its "Proactive Suggestions" UI was found already generic enough to
  need only one new array entry. New `job_this_week_digest` in `scheduler.py`, per-user cadence checked
  inside a single daily cron registration rather than needing two.

**Docs**: `docs/MAP.md` updated for every new file; `content/help.json`'s Notifications section gained
real howto lines for both new features (not a `whats_new` entry — no release/version cut happened this
session).

**Not done yet, explicitly**: Tier 4 (soft-delete/trash bin — the largest item, touches every module's
delete path including AI tools — bulk actions, persist-filters, duplicate, save-and-add-another, a11y,
empty states, shortcuts overlay, offline banner, swipe gestures, custom PWA icon, pull-to-refresh +
diff-based refresh). Continuing in this same session.

## UX Polish Batch — Tier 3 built, tested, and verified

Full backend suite: 1334 passed (up from 1291 at the start of this session), 0 failed. Frontend
`eslint --max-warnings 0` and `vite build` clean throughout.

- **#11 (onboarding seed data)** — new `services/seed_data_service.py`, one function per module,
  wired into 8 manifests' `on_new_user()` (7 new, journal's existing one extended rather than
  replaced). Real signature-shaped bug caught by its own test before it mattered: Notes' create
  function takes a filesystem *path* as its "title" argument, not a display string — the shared
  `"Example: "` prefix's colon isn't a valid path character, so Notes gets its own `"Example - "`
  spelling instead of silently breaking. On a fresh instance (given #12's new baseline) a new user
  gets seed data for whichever of the 8 modules are actually installed — Assets/Contacts/Finance
  seeds only fire for users who sign up after an admin installs those via Mod Store, same as any
  other `on_new_user()` hook.
- **#30 ("today at a glance" dashboard block)** — new core `services/dashboard_blocks/_today_glance.py`
  (`today_glance`, `module="dashboard"`-gated, reusing `scoped_target()`), new `TodayGlanceBlock`
  frontend component. Opt-in via Add Block only, no seed-template changes.
- **#10 (search fast-follow)** — real compatibility issue caught and designed around, not just fixed
  after the fact: `search_service.search()` already had 17 passing tests asserting a plain
  `list[dict]` return; the naive "return a richer dict" redesign would have broken every one of them.
  Landed instead as backward-compatible keyword-only additions (`cross_workspace`, `provider`,
  `with_totals` — all default to the pre-existing behavior, verified by re-running the untouched old
  test file). Each result now also carries `_provider` (the exact namespaced key, not just
  `_module`) since Household/Team each own 3 providers under one module — needed for "show more" to
  target the right one when a module's results span more than one provider. `GlobalSearch.jsx`
  reused the just-built `useToast()` as its first real caller (confirming the cross-workspace switch)
  and the existing `useEscapeToClose()`.

## Tier 4 — starting the smaller items

- **#18/#21 (show/hide password + autoComplete)** — `pages/Login.jsx` only, both trivial and
  low-risk.
- **#22 (persist filter/sort)** — real finding first: almost nothing in the app has *unpersisted*
  filter/sort state to migrate. Tasks' own sort mode already survived navigation via `localStorage`;
  grepped every module's frontend for filter/sort `useState` and found no other real candidate.
  Scoped down honestly to the one concrete case (Tasks' `filter`/`sortMode`), upgraded from
  localStorage-only to server-side (`tasks_filter`/`tasks_sort_mode` on `auth.json`) per the decided
  architecture, rather than inventing a generic mechanism with nothing left to apply it to.
  Also closed a real, pre-existing gap found while adding tests: no dedicated test file for
  `PATCH/GET /auth/me` existed at all before this (`tests/test_auth_me_settings.py`, new) — which
  caught a second real thing, a genuinely dead validation branch: the hand-written
  `command_palette_actions` list/string check added in Tier 2 can never actually run, since Pydantic's
  own `list[str]` typing already rejects the wrong shape before `update_me()` is ever called — removed
  as dead code once the test proved it unreachable, rather than left in place unused.
- **#23 (Duplicate)** — pure frontend, no backend changes: each of `TaskView.jsx`/`EventModal.jsx`/
  `TransactionModal.jsx` builds the same payload its own submit already sends, minus completion/streak
  fields, and posts it to the same create endpoint. Confirmed each already scopes to the same
  personal-vs-pool store the original opened through (`saveApi`), so "stays in the same scope" needed
  no new logic.
- **#24 (Save & add another)** — scoped to Transactions only (the spec's own named example), not
  extended to every create modal. `TransactionModal.jsx` gained a `keepOpen` submit path (guarded to
  new, non-transfer transactions only) that resets identifying fields and calls a new
  `onSavedKeepOpen` prop instead of the closing `onSaved` — `Finance.jsx`'s existing `key={txModal.tx?.id
  || 'new'}` already keeps the same component instance mounted across repeated saves, so the reset
  state actually shows rather than being wiped by a remount.
- **#2 (empty states)** — new `components/EmptyState.jsx`, wired into Tasks/Notes/Contacts. Deliberately
  did NOT touch Assets — it already has a genuinely better purpose-built empty state (two CTAs: "New
  Asset" and "Create a template", with conditional copy) than the generic component would provide;
  forcing it through the shared component would have been a regression, not an improvement.
- **#28 (role-aware messaging)** — real bug found, not just a nice-to-have: `GettingStarted.jsx`'s 4-step
  checklist was never actually role-aware — it linked every user to `/tasks` and `/chat` unconditionally,
  a dead link for any restricted feature-role user with those modules disabled. Fixed by filtering steps
  against `user.disabledModules`. Also added a real empty-blocks message to `Dashboard.jsx` itself
  (distinguishing "limited module access" from a plain new-dashboard empty state), closing both halves
  of the original backlog item.
- **#16 (offline banner)** — new `components/OfflineBanner.jsx`, wired into `Layout.jsx`. Combines
  `navigator.onLine`/`online`/`offline` events with a 15s-interval `GET /api/v1/health` check, since
  `navigator.onLine` alone stays `true` even when DNS or a proxy is actually broken, not just the
  network interface itself.
- **#31 (custom PWA icon per accent color)** — new backend dependency, Pillow, added deliberately: the
  shipped `icon-192.png`/`icon-512.png` are a flat orange "C" (the same brand `#f97316` as the CSS
  default) on a dark badge with a white "L". New `services/icon_service.py` converts to HSV and rotates
  only the hue of pixels near that source orange (~24°, sampled directly from the real file) toward the
  user's `accent_color`, keeping saturation/value untouched so the glyph's own shading survives — the
  dark badge (~213° hue) and the white "L" (near-zero saturation, hue-invariant regardless of rotation)
  fall outside the ±45° hue window and are never touched. Verified visually against both real icon files
  with a blue and a green target before wiring up the router.
  New `routers/pwa.py`: `GET /pwa/manifest.webmanifest` is cookie-authenticated (`get_current_user`,
  mirrors `GET /auth/me/background`'s own precedent — native `<link>` requests can't attach the app's
  `Authorization` header, only the httpOnly cookie) so it can read the caller's real `accent_color` and
  bake it into the icon URLs it returns; `GET /pwa/icon-{size}.png?accent=` stays public/unauthenticated
  on purpose — it's just a public brand asset recolored by a regex-validated hex string
  (`_ACCENT_COLOR_RE`, reused from `routers/auth.py`), nothing sensitive gated there, and no auth
  mechanism could reach it from a native `<link>`/`<img>` request anyway.
  **Real bug caught by the test suite before shipping, not after**: the manifest's icon `src` values
  initially embedded the accent raw (`?accent=#22c55e`) — a bare `#` in a URL starts a fragment, so the
  browser would silently drop everything after it and the accent would never actually reach the server.
  Fixed with `urllib.parse.quote()` server-side (manifest generation) and `encodeURIComponent()`
  client-side (`theme.js`'s `apple-touch-icon` href, for iOS — which reads that link directly and
  ignores the manifest's own `icons` array entirely, so both links need swapping, not just one).
  `theme.js`'s `applyAccentColor()` is the single hook point already called on login/refreshUser/logout
  — swaps both link hrefs to the dynamic endpoints on login, back to the static `/manifest.json`/
  `/icon-192.png` defaults on logout, no new call sites needed anywhere else.
  10 new backend tests (`test_icon_service.py`, `test_pwa_router.py`). `black`/`isort`/`eslint --max-warnings
  0`/`vite build` all clean.
