# 2026-07-25

## Idea generation & prioritization pass

Ran a repeated search → generate → compare → update → organize → document cycle to surface product/business/polish ideas not yet in `docs/TASKS.md`, and to give the existing backlog a consistent priority framing. Working on branch `claude/prioritize-ideas-tasks-skn9bd`.

**What was added:** a new **Idea Backlog** section in `docs/TASKS.md` (after Product Backlog, before Done), scored on `Impact` (business effect, 1–5) × `Polish` (perceived-quality effect, 1–5) → tier 🔴P0/🟠P1/🟡P2/⚪P3. Explicitly unvetted — nothing here is promoted into the active sections without owner triage. De-duped against everything already tracked (Security remediation, Now, Launch surface, Features that unblock scale, Product Backlog).

**Cycles completed this session:**
1. Growth & Monetization — pricing page, in-app self-hosted→managed upsell, referral program, comparison pages, feature-gate design ahead of Stripe
2. Polish, UX & Accessibility — a11y pass (previously entirely absent from the backlog), empty states, command palette, bulk actions, trash/undo
3. Trust, Reliability & Observability — error monitoring (Sentry/GlitchTip), Playwright E2E suite (currently zero frontend/integration test coverage), public status page, backup-restore drill
4. Product Differentiation & AI — AI activity digest, cross-module proactive suggestions (surfacing the already-built Deal/Asset/Invoice linking as insight instead of passive data), RAG-lite stepping stone
5. Managed Hosting Business Ops — multi-tenant admin console, tenant onboarding SOP, support intake, data-export/offboarding commitment
6. Per-Module Polish Sweep — Dashboard summary strip, Calendar drag-to-reschedule, Notes version history, Journal mood picker, promoted the shared-shopping-list idea out of `PROJECT.md` Phase 5 (was only in the architecture doc, not the actual backlog)

**Notable gaps found by the search step** (i.e. genuinely absent from any existing doc before this pass): accessibility/a11y, frontend/E2E test coverage, structured error monitoring, and a public status page. These are flagged 🔴/🟠 in the new section as the highest-leverage additions.

**Cycle 7 (synthesis):** added a "Start here — P0 items across all cycles" shortlist at the top of the Idea Backlog, pulling all 7 score-9 ideas into one place with suggested sequencing (a11y + E2E tests + error monitoring as foundational risk-reduction; pricing page + in-app upsell as the cheapest real revenue levers; AI activity digest as the standout differentiator; multi-tenant admin console as urgent-once-it-scales).

**Cycle 8 (i18n, compliance & data portability):** search step confirmed via grep that there is **zero i18n framework** anywhere in `app/frontend/src` (English-only today, previously unflagged). Also surfaced a real correctness bug while reading `finance_reports.py`: `net_worth()` sums `total_cents` across every visible book without checking each book's `currency` field, so a mixed USD/EUR household gets a meaningless blended total — logged directly as a **BUG** under "Now — active build work" (not the idea backlog, since it's a confirmed code-level finding, not speculative) rather than buried in the idea backlog. Added compliance ideas (DPA template, ToS acceptance tracking, EU data residency, admin audit log) and the i18n/multi-currency feature ideas that generalize past that bug.

**Cycle 9 (technical debt & architecture):** line-count + duplication search found real structural debt: `services/agent_service.py` (2,368 lines, the whole AI tool registry in one file), `routers/auth.py` (1,009 lines mixing 5 unrelated concerns), and — most notably — four separately hand-rolled share/access-resolution implementations (assets/finance/contacts/notes index services) that the Done log shows have independently produced the *same class* of specificity-ladder bug more than once for Assets alone. Also found `GET /health` is a hardcoded `{"status":"ok"}` with no actual checks (`routers/health.py:8`). No `TODO`/`FIXME` comments exist anywhere in the backend — good hygiene otherwise.

**Cycle 10 (docs & dev experience):** counted actual router endpoints (298, via grep across `app/backend/routers/`) against `docs/API.md`'s documented endpoints (116) — only ~39% coverage on a hand-maintained 1,182-line file. Also found the FastAPI app's `/docs` Swagger UI is enabled by default with no explicit decision recorded, and its hardcoded `version="0.1.0"` (`main.py:171`) is stale against the real 0.4.3+ release — added to the API/doc-coverage ideas.

**Cycle 11 (community & ecosystem):** found the "GitHub issue templates" Launch Surface checklist item was stale — `.github/ISSUE_TEMPLATE/bug_report.yml` + `feature_request.yml` already exist and are substantive, so it's now checked off. No `CODE_OF_CONDUCT.md` exists at the repo root — added as the top idea this cycle (near-zero effort, expected-by-default for OSS, worth having before any Show HN/r/selfhosted push).

**Cycle 12 (synthesis):** cross-cycle duplicate scan across all 78 idea-backlog rows (cycles 1–11) found zero duplicates. Expanded the "Start here" section with a second-tier 🟠P1 skim list grouped by what each item unblocks (revenue / polish / trust / AI differentiation / hosting ops / architecture / docs), plus a summary line: 78 scored ideas total, 2 concrete findings actioned directly rather than left speculative (the Finance net-worth currency bug, the stale issue-templates checklist entry).

**Cycle 13 (security as a business asset):** confirmed `SECURITY.md` is already solid (scope, response-time commitment, credit policy). Looked past the existing audit-remediation work toward security as sales/trust collateral for managed hosting: pre-revenue pentest, a public trust page (in-app version already shipped but nothing pre-signup), SOC 2 gap assessment, bug bounty, and the trivial `security.txt`.

**Cycle 14 (mobile-specific):** read `app/frontend/public/sw.js` — the PWA service worker is already well-built (app-shell caching, network-first with offline fallback, push handling), confirming the big backlogged "Offline-first PWA sync" item is correctly scoped as a data-sync project rather than a from-scratch build. Added smaller mobile-native-feel gaps: biometric/PIN app-lock (no re-auth gate today on a life/finance data app), a Capacitor native wrapper for app-store discoverability, and swipe gestures.

**Cycle 15 (pricing & packaging):** turned the abstract Cycle-1 feature-gate idea into a concrete tier proposal mapped directly onto the existing workspace/module enforcement points (no new architecture needed). Notably: "bring your own AI key" as a discount tier — the AI-provider abstraction already supports this technically (self-hosters do it today), so it's a pricing lever built on already-shipped tech, not a new feature.

**Cycle 16 (activation & onboarding funnel):** confirmed via grep that the app has **zero outbound email capability anywhere** in the backend — `docs/PROJECT.md` already says as much ("email is used for login only"), but this quietly constrains three separately-backlogged items (Help feedback delivery, Admin password reset, v0.3 email digests) to workarounds instead of a real fix. Flagged baseline transactional-email infra as the shared unblocker. Also added an invite-your-household prompt, a demo→signup bridge, and chat quick-start prompts (surfacing the example commands already listed as working in PROJECT.md but undiscoverable in the actual chat UI).

**Cycle 17 (internal product analytics):** every idea explicitly constrained to opt-in/anonymized/aggregate-only, given "data ownership first" is the actual product promise — flagged that violating it would contradict what's being sold. Noted there's currently zero usage/feature-adoption signal anywhere (self-hosted or managed) beyond the in-progress AI usage counter, so roadmap module-investment decisions are currently unmeasured guesses. Led with managed-hosting-only engagement tracking (consent already implicit there) before the harder self-hosted opt-in telemetry idea.

**Cycle 18 (final synthesis):** added an Executive Summary at the top of the Idea Backlog (under 300 words) covering all 17 generation cycles + this synthesis pass, updated the backlog-size line to the final total (107 scored ideas), and ran a last cross-cycle duplicate scan on cycles 13–17 — clean, nothing overlapping.

**Session wrap for this pass.** Total output: 18 cycles, 107 scored + categorized ideas in a new `docs/TASKS.md` "Idea Backlog" section (fully unvetted, awaiting owner triage — nothing here overrides existing priorities), 1 real bug found and logged (Finance net-worth currency aggregation), 1 stale checklist item corrected (GitHub issue templates were already shipped). The single clearest cross-cutting finding: several of the highest-value items weren't feature ideas at all but silent gaps — no accessibility support, no frontend/E2E tests, no error monitoring, no outbound email capability, an API reference covering only 39% of endpoints, and four independently-duplicated access-control implementations that keep reproducing the same bug class. All were previously unflagged anywhere in the project's docs.

## Files updated
- `docs/TASKS.md` — new Idea Backlog section (cycles 1–18: 17 generation cycles + 2 synthesis passes), one new BUG under Now, one stale checklist item corrected
