# 2026-06-28 — Daily Note

## Work Done

- Replicated LogCoreAgency docs structure into LogCoreOS:
  - Created: README.md, SOUL.md, TASKS.md, MEMORY.md, MAP.md
  - Renamed PROJECT_DOCS.md → PROJECT.md
  - Rewrote AGENTS.md with boot protocol preamble + dev reference
  - Deleted BACKLOG.md (content moved to TASKS.md)
  - Created docs/Daily Notes/ and docs/hooks/

- Created all 4 hooks (docs_loader, docs_reminder, commit_reminder, safety_check) adapted from LogCoreAgency

- Wired hooks into:
  - LogCoreOS/.claude/settings.json (for LogCoreOS-specific sessions)
  - LogCoreDEV/.claude/settings.json (for LogCoreDEV-rooted sessions — active now)

- Ran full codebase audit and updated docs for accuracy:
  - MAP.md: fixed Dashboard.jsx (was Home.jsx), added Goals.jsx / Brain.jsx / Profile.jsx, all 5 components, 8 missing services, missing routers, brain _system files
  - AGENTS.md: scheduler updated from 4 → 7 jobs, chat modes (plan/auto/research), Web Push, Export prefix, Suggestions sections added
  - MEMORY.md: added security rules 9–10 (rate limits, security headers), full env vars table

## Decisions Made

- hooks live in both .claude/ locations so they fire regardless of which directory Claude Code is opened from
- docs_loader.sh injects AGENTS.md in addition to SOUL + MEMORY + TASKS (user request)

## Follow-up

- 13+ uncommitted changes — commit when ready
