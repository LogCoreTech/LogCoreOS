# 2026-08-24

## Mod Store: universal module system, phase 1 (registry infrastructure)

Long design conversation (interviewed extensively, plan at
`/home/logcore/.claude/plans/i-want-you-to-composed-scone.md`) landed on a much bigger scope than
the original "browsing-only Mod Store catalog" from earlier sessions: every module in the app —
including foundational ones like Tasks, Chat, and Dashboards — will eventually convert into a
uniform `module_packages/<id>/` format, each with its own manifest/version/changelog. A locked
module just carries `uninstallable=True` rather than being excluded from the system. Real
infrastructure (`dashboard_blocks/`, `agent_service.py`'s tool-orchestration engine) is explicitly
excluded — no router of their own, imported by every module, never a module themselves.

Today's actual code is the foundation only: the registry mechanism (`module_registry.py`), the
Mod Store service/router (`mod_store_service.py`/`routers/mod_store.py`), and all four
enforcement-gap fixes (Brain browser, AI agent tools, dashboard blocks, export-as-deliberate-no-op)
wired generically off a module's manifest — built and fully tested with **zero real modules
converted yet** (`module_packages/` is empty except `__init__.py`). Journal's actual conversion
(increment 1 in the plan) is still pending — tracked in `docs/TASKS.md`.

### Real bugs caught by writing tests before trusting the design

1. `discover_manifests()` was blindly importing every directory under `module_packages/`,
   including `__pycache__` once Python compiled bytecode for it. Fixed: require an actual
   `manifest.py` file, skip `_`-prefixed dirs.
2. Route ordering: the SPA catch-all was registered via `@app.get` at plain import time, but module
   routers can only register inside `lifespan()` *after* migrations run (a locked module's
   install-state depends on its own upgrade migration). Starlette matches by insertion order, so
   the catch-all had to move into `lifespan()` too, positioned last — otherwise it would have
   shadowed every module route. Verified via a real `TestClient` lifespan run, not just imports.
3. A locked module's registration failure needed to crash boot loudly, not log-and-skip like an
   optional module — the lenient path would leave the app looking healthy while something as
   load-bearing as Tasks was silently missing.

### Design corrections made mid-build (caught by reasoning through edge cases, not just coding)

- Brain-browser/agent-tool module gating is **per-user** (checked against the caller's
  `disabled_modules`), not instance-wide — a module can be installed for the instance but disabled
  for one role/user, and that case must hide its folder too. First draft only handled "never
  installed at all."
- Dashboard block gating threads the module check through **whichever identity `render_block()` is
  actually resolving as at that pass** (viewer at pass 1, owner at pass 2's
  `share_underlying_data` exception) — not a flat "check the owner" shortcut. Verified with a test
  where the viewer has the module off but the owner has it on (resolves as owner) and one where
  both have it off (stays locked).
- `features_service.ALL_MODULE_IDS` → `all_module_ids()` touched 4 files, not the 2 originally
  assumed — `routers/auth.py` had a second validator (`WorkspaceModulesRequest`) using the same
  name that a first pass missed.

### Files

New: `app/backend/module_registry.py`, `app/backend/services/mod_store_service.py`,
`app/backend/routers/mod_store.py`, `app/backend/content/mod_store_catalog.json`,
`app/backend/module_packages/__init__.py`, 7 new test files (40 tests), `fake_module` fixture in
`tests/conftest.py` (writes a real `module_packages/<id>/` package to disk for one test — real
importlib discovery, not mocked — and cleans up after).

Modified: `main.py`, `migrations/runner.py`, `routers/{auth,brain,chat,dashboards,features}.py`,
`services/{agent_service,features_service,help_service}.py`,
`services/dashboard_blocks/{registry,render}.py`.

Branch: `feat/mod-store-registry-infra`. Full suite: 832 passed (792 existing + 40 new), zero
regressions — confirmed genuinely zero-behavior-change with no modules converted yet.

No CHANGELOG entry — nothing user-visible shipped this turn (no UI, no observable behavior change
for anyone using the app today). That comes with journal's actual conversion.

## Journal conversion, backend half (increment 1 continued)

Session limit reset, continued from the plan. Moved journal for real this time — into
`module_packages/journal/` per the pattern above, backend only (frontend still pending). Verified
with actual end-to-end checks, not just unit tests: a real `TestClient` lifespan boot with journal
marked installed correctly registers its router at `/api/v1/journal`, its `journal_entry` dashboard
block (with `module="journal"` set), and its 3 agent tools — and a live `write_journal_entry` →
`read_journal_entry` round-trip through the moved service actually works.

### Real gap found mid-conversion

`write_journal_entry` wasn't in `_RESEARCH_TOOLS`/`_READ_TOOLS` — core's "new tools are write-gated
by default" rule — but the phase-1 module-tool plumbing had no way for a module to declare which of
its own tools are read-only. Without a fix, `read_journal_entry` would have silently stopped
working in research mode and started requiring approval in approve mode, a real regression from
today's behavior. Fixed: `ModuleManifest` gained `read_only_agent_tools`; both `agent_service.py`
call sites now union it in via `module_registry.read_only_agent_tool_names()`.

### Test infrastructure fix (affects every future module conversion, not just journal)

A module's own tests need the same fixtures as `app/backend/tests/`, but conftest.py fixtures only
inherit down a directory tree, not across sibling ones. Moved the shared fixtures to a new
`app/backend/conftest.py` (the true common ancestor of `tests/` and `module_packages/`), deleted
the old `tests/conftest.py`, and added `testpaths = ["tests", "module_packages"]` to
`pyproject.toml` so a bare `pytest`/`pytest -v` covers both trees. `pytest tests/ -v` still narrows
to core-only when that's what's wanted (an explicit path on the command line overrides testpaths).

### Files

New: `app/backend/module_packages/journal/**` (manifest + backend/{router,service,dashboard_block,
agent_tools}.py + tests/test_journal_service.py), `app/backend/conftest.py`,
`tests/test_journal_module_conversion.py` (6 tests: migration fresh/upgrade split, backfill hooks,
full install/uninstall/reinstall round-trip).

Deleted: `routers/journal.py`, `services/journal_service.py`, `services/dashboard_blocks/_journal.py`,
`tests/test_journal_service.py` (old location), `tests/conftest.py` (moved up), journal's section in
`content/help.json`, `brain/USERS/_template/Journal/` (tracked in git — removed via `git rm`).

Modified: `main.py`, `features_service.py`, `agent_service.py`, `dashboard_blocks/registry.py`,
`routers/setup.py` (new `on_new_user` hook call), `pyproject.toml` (testpaths).

Not moved, deliberately (frontend work, separate remaining task): `lib/api.js`'s journal client,
`deepLinks.js`'s entry, `pages/Journal.jsx`, `JournalEntryBlock` component.

Full suite: 838 passed (832 + 6 new). Branch: `feat/mod-store-registry-infra`, committed per
explicit session-scoped authorization ("commit when you are finished with a portion as planned").

## Journal conversion, frontend half (increment 1 complete)

Finished journal's conversion end-to-end. `src/lib/moduleRegistry.js` (`import.meta.glob`) auto-
discovers every converted module's frontend manifest at build time — `constants.js`, `deepLinks.js`,
`blockRegistry.js` all merge it in generically now, no per-module edit needed going forward. Moved
`pages/Journal.jsx`, its `JournalEntryBlock` component, and its `lib/api.js` client into
`module_packages/journal/frontend/`. `lib/api.js`'s `get`/`post`/`put`/`patch`/`del` are now
exported so a module's own client can build on the shared session/error-handling logic instead of
reimplementing it.

New `activeModuleIds` in `AuthProvider` (from `GET /mod-store/active`, same poll cadence as the
user), closing a real gap `disabledModules` alone can't: between clicking Install and clicking
Restart Now, the marker says installed but the backend hasn't registered the router yet.
`ModuleRoute` now requires both signals for a converted module's route. Nav itself needed zero
changes — `Layout.jsx`'s existing `disabledIds` filter already worked correctly once `ALL_MODULES`
included the merged-in entries.

### Verified against a real running instance, not just pytest

Started an actual `uvicorn` dev server against a real Brain (copied from the real setup template),
registered a real user through the real `/auth/register` flow, then ran the full cycle over real
HTTP: catalog → install → confirmed `/mod-store/active` still empty and `disabled_modules` already
cleared (marker flips immediately, router doesn't) → restarted the process → confirmed `/mod-store/
active` now includes journal and `GET /api/v1/journal` returns real `application/json` (a genuine
gotcha found here: an unregistered path doesn't 404, it falls through to the SPA catch-all's
`index.html`, which is also a 200 — a bare status-code check can't tell the two apart, content-type
has to be checked) → wrote and read back a real entry → uninstalled → `disabled_modules` immediately
included journal again, before any restart → confirmed the entry file was still on disk, byte-
identical. `npm run build` confirmed the actual production code-splitting: `Journal-*.js` and
`JournalEntryBlock-*.js` are genuinely separate chunks, not folded into the main bundle.

Real gap caught by this live check specifically (not by unit tests): journal was fully wired up but
had no `content/mod_store_catalog.json` entry — `get_catalog()` only ever iterates catalog entries,
so it would never have appeared in the Mod Store UI at all. Added it (`status: "available"`).

**Honest limitation**: no browser-automation tooling (Playwright/chromium-cli) is available in this
environment, so the actual React rendering was never visually verified — what's confirmed is the
real API cycle and the build output, not a screenshot-checked click-through. The Mod Store admin UI
itself (the page that would let an admin do this by clicking instead of curl) isn't built yet —
tracked in `docs/TASKS.md`.

**Safety note**: a broad `pkill -f "uvicorn main:app"` during manual testing nearly matched a real,
long-running instance on port 8000 under the same OS user — survived only because that specific PID
denied the kill signal. Switched to tracking and killing my own test instance by exact PID for the
rest of the session; the real instance was confirmed healthy throughout.

### Files

New: `app/frontend/src/lib/moduleRegistry.js`, `app/frontend/src/module_packages/journal/**`
(manifest.js + frontend/{Journal,JournalEntryBlock,api}).

Deleted: `app/frontend/src/pages/Journal.jsx`, `JournalEntryBlock` out of `blocks.jsx`.

Modified: `App.jsx` (lazy-route loop, `ModuleRoute`'s `activeModuleIds` check), `lib/constants.js`
(`ALL_MODULES` merge), `lib/deepLinks.js` (`MODULE_ROUTES` merge), `lib/api.js` (exported HTTP
helpers, removed journal's client, added `modStore` client), `lib/auth.jsx` (`activeModuleIds`),
`components/dashboard/blockRegistry.js` (module-package block merge),
`app/backend/content/mod_store_catalog.json` (added journal's real entry).

Full backend suite: 838 passed (unchanged from the backend-half commit — this was frontend-only
work). Journal is now a complete, working, fully-tested example of the conversion pattern every
other module will follow. Branch: `feat/mod-store-registry-infra`.

## Mod Store admin UI (increment 1, truly complete)

Built `pages/settings/admin/ModStore.jsx` — the piece that makes everything built today actually
usable by a human, not just curl. Wired into `AdminMenu.jsx` + `App.jsx` at
`/settings/admin/mod-store`. Seven explicit per-row states (`rowState()`) crossing the `installed`
marker against the `active` (really-registered-right-now) signal; a page-level "Restart Now" banner
whenever anything's pending in either direction; confirm-before-uninstall and the online-users
warning both use plain `window.confirm()`, matching this codebase's existing convention rather than
a custom modal.

### Verified the actual shipped logic against real backend responses

Extracted `rowState()` straight from the committed file (not reimplemented) and ran it via Node
against real `GET /mod-store/catalog`/`/active` responses through each real state transition:
fresh → installed-not-restarted → restarted-and-active. Also verified the online-users conflict
path for real: created a second user via `POST /auth/admin/users` (self-registration closes after
the first user — hit this as a real gotcha mid-test, not a bug, just unfamiliarity with this
instance's own registration policy), logged them in, had them ping presence, then confirmed
`POST /mod-store/restart` returns exactly the `{detail: {message, online_users}}` shape
`modStore.restart()` expects, and that `force: true` correctly bypasses the check and proceeds to
the restart attempt itself (502s here only because there's no real Docker container in this
sandbox — expected, already documented).

This closes out Mod Store increment 1 completely. Three commits today on
`feat/mod-store-registry-infra`: the registry mechanism, journal's conversion, and now the UI to
drive it. Next up: increment 2 (Home), tracked in `docs/TASKS.md`.

Full backend suite: 838 passed, unaffected (this portion was frontend-only). Frontend build clean,
157 modules.

## Hotfix: reinstall crash + badge glitch, reported from the real instance

Owner report, after actually deploying and clicking through increment 1 on their real instance (not
this sandbox): uninstalled journal (worked), reinstalled it, got an error, and the whole app appeared
to crash ("said check logs"); separately, the `pending_uninstall` badge ("Uninstalled — restart to
finish") looked "glitched... half hidden."

**Root cause (restart crash)**: `POST /mod-store/restart` called `docker_sdk...containers.get(
"logcore-app").restart()` synchronously, inline in the handler, before returning — but that call
restarts the very container the request is running in, so Docker can kill the process before the
HTTP response finishes reaching the browser. This is the one Mod Store code path that was never
actually exercised for real anywhere this session (no Docker daemon in this sandbox; only ever
verified as a 502 "container not found," never as a genuine successful restart). See
`docs/MEMORY.md`'s 2026-08-24 entry for the full writeup.

**Fix**: the Docker connectivity/container-lookup call stays synchronous (a real config/permission
failure still 502s immediately to the client), but the actual `.restart()` call is now deferred via
FastAPI `BackgroundTasks` (runs only after the response has been sent) plus a 2s delay. New
`tests/test_mod_store_router.py` (9 tests, the router's first-ever direct test coverage — previously
only the service layer and journal's `on_install`/migration hooks were tested, never `install()`/
`uninstall()`/`restart()` themselves) — including a regression test asserting the fake container's
`.restart()` is *not* called by the time `restart()` returns, only once the deferred task runs.

**Badge fix**: `pending_install`/`pending_uninstall` labels were long unbroken strings inside a
`flex-wrap` row with no `whitespace-nowrap` — shortened both ("Restart to activate" / "Restart to
finish," the page-level banner already explains the full context) and pinned to one line.

**Verified**: full backend suite green (847 passed = 838 + 9 new), frontend `vite build` clean.
**Not verified**: the actual production-instance failure was never reproduced here (no real Docker/
socket-proxy in this sandbox) — the fix is based on a close read of the exact race condition, not a
confirmed repro. Asked the owner to grab real server logs if the restart still fails after this
lands, since this diagnosis, while well-evidenced, wasn't confirmed against their actual error text.

## Hotfix round 2: badge layout + restart never resolving, both from continued real-instance testing

Owner kept clicking through the (still pre-fix) real instance and sent more detail: on mobile the
row's badge+version "grey text" was "pushed down and overflow hidden" instead of sitting in the top
corner (their words — a design expectation, not just a bug description); separately, clicking
"Restart Now" flashed a green success message but the "pending restart" banner never went away,
pressing it again returned an error, and only closing/reopening the Settings page showed the correct
post-restart state. Suggested fix themselves: "should make it autorefresh that page."

**Badge/layout fix** — `ModStore.jsx`'s row previously put name + version + badge in one
`flex items-center gap-2 flex-wrap` line; on a narrow viewport the badge could wrap onto its own line
in a way that read as clipped/hidden rather than a normal second line. Restructured: the badge is now
a `shrink-0` sibling in its own `justify-between` row, pinned to the top-right corner and never
wrapping; the name+version block is what shrinks/truncates (`min-w-0`, `truncate`) if space is tight.
Matches exactly what the owner asked for ("should be in the top corner").

**Restart-never-resolving fix** — root cause: the backend's own restart is now deliberately deferred
(~2s delay, then the actual container stop/start/boot, all real time) per the hotfix above, but the
frontend was calling `load()` exactly once, immediately after the request returned — so it always
re-fetched the *same pending state* that existed before the restart even began, made worse (not
better) by the crash-hotfix's own deferral. `doRestart()` now polls (`catalog()`+`active()` every 2s,
60s deadline) until no row is still `pending_install`/`pending_uninstall`, keeping `restarting` (and
therefore the Restart Now button, which is `disabled={restarting}`) true for the *entire* window —
not just the initial request — so a second click can't land mid-restart, which is what turned into
the reported "pressing again returned an error." Fetch failures during the poll (the container really
is down for a few seconds between the old process dying and the new one answering) are expected and
silently retried (`load(true)`), not surfaced as an error — only a real 60s timeout flashes a message.
`load()` now returns the freshly-fetched `{modules, active}` directly rather than only setting state,
since reading React state back inside the same tick that just called its setter would see stale data.

**Verified**: `npx vite build` clean. **Not verified**: no live browser in this sandbox, so the
actual mobile rendering and the full poll-until-resolved cycle against a real container restart were
never watched happen — reasoned through from the exact reported symptoms and the mechanism's own
timing, same honest gap as the round-1 hotfix above. Asked the owner to re-verify on their real
instance once this lands.

## Hotfix round 3: row content clipped mid-transition, fixed on reload

Owner, still testing round 2's (pre-fix) build: "when the tag and text/buttons switch between
install/restart and uninstall... both sides of text and tags are cut short or bottoms of text or
tags are cut off. but it all returns to normal when page is reloaded." Researched first rather than
guessing blind — spawned an Explore agent to check whether this app has hit this bug class before.
Confirmed two real precedents: `ContactRow`'s presence-popover clipping (2026-08-18 entry above) and
`assetDisplay.jsx`'s lightbox, both caused by `.card`'s `backdrop-blur-sm` (creates a new stacking
context / containing block) — `backdrop-filter` is a genuinely fragile mechanism in this app's mobile
PWA context, not a first occurrence. Neither precedent's fix was a remount trick, though — both found
and fixed the specific stacking-context/containing-block mechanism directly.

**Diagnosis**: "correct on reload, broken on the in-place transition" is exactly the signature of a
stale GPU compositing layer — a fresh mount always paints cleanly; an in-place React DOM mutation is
what triggers the bug. `ModStore.jsx`'s row `<div>` (with `.card`'s `backdrop-blur-sm`) never
unmounts across a state transition — same `key={entry.id}` throughout install→pending→active→
pending_uninstall — so React mutates its children in place every time.

**Fix**: keyed the row on `${entry.id}:${state}:${busy}` instead of just `entry.id`, so React fully
unmounts and remounts the row's DOM subtree (and therefore its backdrop-blur compositing layer) on
every visually-relevant transition, forcing a clean paint every time — mechanism-agnostic (fixes the
symptom regardless of whether the true cause is exactly the same stale-layer-bounds issue or a
related compositing quirk), one line, no shared-CSS/design changes elsewhere in the app.

**Verified**: `npx vite build` clean. **Not verified**: still no live browser in this sandbox — same
honest gap as rounds 1 and 2. This is the third real bug this owner has caught by actually clicking
through the shipped UI on their own instance; none of the three were reachable by this session's
pytest/build-only verification.

Owner confirmed all three fixes working on the real instance ("it all works perfectly"), authorized
the commit, and said to move forward. Committed as `794db43` on `feat/mod-store-registry-infra`.

## Home converted into `module_packages/home/` — Mod Store increment 2 complete

Same treatment as journal, moved straight into implementation (pattern already proven, plan already
approved — no re-plan needed). Full file inventory first, same rigor as journal's own increment:
`routers/home.py`, `services/dashboard_blocks/_home.py`, 4 agent tool schemas + executor cases,
`pages/Home.jsx`, `HomeFavouritesBlock`, the `home` client in `lib/api.js`, `content/help.json`'s
section — all moved into `module_packages/home/`.

**The real complication journal didn't have**: Home's admin config form (HA URL/token) lives inside
`pages/settings/admin/Household.jsx` — a different, not-yet-converted module's page, not a dedicated
Home admin page. Two decisions this forced, both written up in full in `docs/MEMORY.md`'s 2026-08-24
(fourth) entry: `services/ha_service.py` stays in core (unlike journal's service, it has consumers
outside Home's own control surface — Household.jsx's form, agent_service.py's ha-configured gate);
`Household.jsx`'s Smart Home section is now gated on Home's installed state, the same stacked
installed+active check `ModuleRoute` already applies to a full route, just for one section of a page
that isn't itself converted.

Also found and fixed while touching this: `content/help.json` and `Home.jsx`'s own error text both
said "Admin → Smart Home," which was never actually true — the form has always lived at
Admin → Household. Corrected in both places (the help text moved into the manifest, so this is now
also what the AI would tell a confused user).

### Two non-obvious things preserved exactly, not carried by the generic module mechanism

- `m016`'s upgrade migration guards on `ha_config.json`'s existence *and* a real, non-blank url+token
  — Home was always opt-in, unlike journal's blanket "the instance existed before" assumption in
  `m015`. An instance that never connected HA starts correctly uninstalled on upgrade.
- `get_home_state` is read-only (no approval needed) but deliberately excluded from the narrower
  research-mode tool set — a real asymmetry predating this conversion. The generic
  `read_only_agent_tools` manifest union (built for journal) would have added it to both sets
  uniformly, silently widening research mode. Left it out of Home's manifest entirely and kept the
  exact hardcoded `_READ_TOOLS = _RESEARCH_TOOLS | {"get_home_state", "ask_user_question"}` line in
  `agent_service.py` untouched — it works identically regardless of which list a tool's schema comes
  from, since these sets only check names.

`features_service._BUSINESS_MEMBER` needed a comment, not a deletion: it's hand-authored (unlike
`_PERSONAL_MEMBER`, derived via comprehension), and `load_features()` defaults any module missing
from a role map to `True` — Home's `"home": False` had to stay explicit there or a fresh
business-profile instance would silently default Smart Home to enabled.

### Files

New: `app/backend/module_packages/home/**` (manifest + backend/{router,dashboard_block,agent_tools}.py
— no service.py, ha_service.py stays core), `tests/test_home_module_conversion.py` (4),
`tests/test_home_agent_tools.py` (6), `app/frontend/src/module_packages/home/**` (manifest.js +
frontend/{Home,HomeFavouritesBlock,api}).

Deleted: `routers/home.py`, `services/dashboard_blocks/_home.py`, `pages/Home.jsx`,
`HomeFavouritesBlock` out of `blocks.jsx`, home's `content/help.json` section, home's entries in
`constants.js`/`deepLinks.js`/`lib/api.js`/`App.jsx`/`blockRegistry.js`.

Modified: `main.py`, `features_service.py`, `agent_service.py` (`_get_tools()`'s ha_ok filter now
applies over the combined core+module tool list, not just `_USER_TOOLS`),
`dashboard_blocks/registry.py`, `pages/settings/admin/Household.jsx` (import + installed-state gate).

Also fixed while running `eslint` for the first time this session (not previously part of the
verification loop — only `vite build`+pytest had been checked): 2 pre-existing unescaped-apostrophe
errors in `ModStore.jsx` and `Household.jsx`, neither related to this turn's actual changes.

**Verified**: full backend suite green (857 passed = 847 + 10 new), `npm run build` clean, `eslint
src/ --max-warnings 0` clean, genuine code-splitting confirmed (`Home-*.js`/
`HomeFavouritesBlock-*.js` as separate chunks). `git grep "home"` outside `module_packages/home/`
turns up only the deliberate, commented exceptions above.

**Not verified**: same honest gap as every round today — no live browser or real Docker/socket-proxy
in this sandbox, so the actual click-through and a real restart cycle against Home specifically
haven't been watched happen here. The owner's own real-instance testing is what verified those
concerns for the mechanism generically (see the hotfix rounds above); this increment hasn't had its
own real-instance pass yet.

This closes Mod Store increment 2. Two modules fully converted (journal, home); ten to go, one at a
time. Next: Calendar (increment 3), tracked in `docs/TASKS.md`.

## Home Assistant: rename + config-form relocation, and a real mobile overflow fix

Owner feedback after clicking through increment 2 on their real instance, three items in one
message. Researched the first two before touching anything (owner explicitly asked for research +
AskUserQuestion, not an immediate implementation).

**Mobile card-width overflow on install**: the "Restart to activate" badge and the row's action
button appearing together was pushing the card wider than the mobile viewport — owner's own diagnosis
and proposed fix, both correct. `ModStore.jsx`'s action button now only renders for stable states
(`active`/`not_installed`); neither pending state shows one, reappearing once the row lands back on
a stable state.

**Config form relocation, researched before moving anything**: owner proposed moving the Home
Assistant URL/token form out of `Household.jsx` and asked "does that resolve all the issues?" —
partially. Moving the *frontend* form does fully resolve the Household.jsx entanglement documented
in this same file's increment-2 entry above. It does **not** change why `services/ha_service.py`
stays in core — that's about `agent_service.py` needing `is_configured()` directly, unrelated to
where the frontend form lives. Presented this distinction, then asked where the form should actually
land. Owner's answer: keep everything else in the `home` module, but put the config form specifically
in **Admin → Hosting**. Checked `Hosting.jsx` before agreeing — it already hosts n8n's connection
config (Automations hasn't converted either), a real, already-established precedent for "an
external-service integration's connection settings live here even though the owning module hasn't
converted." This is the first section there gated on a module's installed state (n8n/Automations
isn't, since it hasn't converted) — a template for when Automations does. Moved `SmartHomeSection`
(renamed `HomeAssistantSection`) + the installed-state gate from `Household.jsx` to `Hosting.jsx`
wholesale; `Household.jsx` now has zero Home Assistant references left at all.

**Rename**: `Home Assistant` is now the display name everywhere a user sees it (nav label, Mod Store
catalog entry, dashboard block picker, manifest `help_section`, Help page, admin menu subtitles) —
`id: "home"` stays unchanged (no migration; it's what disabled-modules lists/role maps/brain paths
already reference, changing it would be a real breaking change for zero benefit). Logo: owner asked
about using the real Home Assistant logo — flagged two real issues before agreeing to anything: it's
a registered trademark (Open Home Foundation), using it in a commercial product risks implying an
official partnership that doesn't exist; separately, the Mod Store catalog has no image-icon support
at all today (`entry.icon` is rendered as plain text) — a real logo would mean building that
mechanism first, not swapping a field. Owner agreed to keep the existing emoji icon (💡, unchanged).

Also fixed while touching this: `content/help.json` and `Home.jsx`'s own error/empty-state text both
said "Admin → Smart Home" originally, corrected to "Admin → Household" in the increment-2 commit
earlier today, now corrected *again* to "Admin → Hosting" to match where the form actually ended up
— three names for the same non-existent-until-today page across one day is a real reminder to
grep-verify a pointed-to location rather than assume it, even one I wrote myself hours earlier.

### Files

Modified: `pages/settings/admin/Household.jsx` (`HomeAssistantSection`/gate removed entirely, back
to pool-priorities-only), `pages/settings/admin/Hosting.jsx` (`HomeAssistantSection` +
`HomeAssistantNotInstalled` + the gate added), `pages/settings/AdminMenu.jsx` (subtitles),
`pages/settings/admin/ModStore.jsx` (action button hidden during pending states),
`module_packages/home/manifest.py` + `manifest.js` + `backend/dashboard_block.py` +
`frontend/Home.jsx` (rename + pointer text), `content/mod_store_catalog.json`, `content/help.json`,
`features_service.py` (comment only), plus `docs/API.md`/`MAP.md`/`AGENTS.md`/`PROJECT.md`/
`TASKS.md` for the rename and relocation (excluding this file and `MEMORY.md`'s own historical
entries, which correctly keep "Smart Home" as a record of what was true when written).

**Verified**: full backend suite green (857 passed, unchanged — this was all frontend/content/docs),
`vite build` clean, `eslint --max-warnings 0` clean. **Not verified**: same live-browser/real-restart
gap as everything else today.

## Home Assistant: full internal id rename, "even if it needs some migrations"

Owner, unprompted after the previous round: "can we change the internal parts for home to home
assistant? even if it needs some migrations." A real, materially bigger ask than the display-name
rename earlier today — this one touches an *already-shipped* module's id, meaning real state on the
owner's live instance (`installed_modules.json`, any per-role/per-user disabled-module overrides,
the actual Brain folder holding favourites) currently says "home" and needs to survive the rename,
not just the code.

Asked one clarifying question before touching anything, since it was the one piece with real
data-migration risk rather than a pure code rename: should the on-disk Brain folder
(`USERS/{name}/Home/`) rename too, given that means moving real files on the live instance, not just
a JSON-key rename? Recommended leaving it alone (nobody sees a raw Brain folder name; zero rename
benefit for real, if small, risk). Owner chose to rename it anyway — respected that as their explicit
call on their own data and implemented it carefully rather than re-arguing a decision they'd already
weighed.

**What actually renamed** (`home` → `home_assistant`): module id, both `module_packages/home/`
directories (backend + frontend, `git mv`), the API router prefix (`/api/v1/home` →
`/api/v1/home_assistant`, plus every frontend `api.js` call site), `owned_brain_paths` (`Home` →
`HomeAssistant`), the Mod Store catalog entry's id, `features_service.py`'s `_BUSINESS_MEMBER`
entry, and two real bugs caught only by grepping the renamed files for any remaining hardcoded
`"home"` string rather than assuming the rename was complete: `router.py`'s
`require_module("home")` gate and `dashboard_block.py`'s `BlockSpec(module="home")` — both would have
silently broken their own access-control/gating check post-rename (checking an id nothing would ever
match again) if left as `"home"`.

**What deliberately did NOT rename**, stated clearly rather than assumed: the frontend route
(`/home` — the URL a user actually navigates to/bookmarks, a materially more visible thing than an
internal id); the help section's own anchor id (`#home`); the 4 AI tool names (`get_home_state` etc.
— descriptive names for a "smart home" concept, not literally "the module's id"); the
`home_favourites` dashboard block type. None of these are the module's id in the sense the owner
asked about, and renaming them adds real blast radius (frontend routes, saved dashboard block
configs, chat-tool-calling behavior) for no real benefit.

### The migration itself — new m017, careful about a specific re-run trap

Added `home_assistant:m017_rename_home_id_to_home_assistant`, run once, alongside the existing m016.
Four independently-idempotent steps: rename the `"home"` key in `installed_modules.json`'s
`installed` dict; rename it in every role's map inside `features.json`; rename it inside every user's
`disabled_modules` in `auth.json` (handling both the flat-list and workspace-keyed-dict shapes
`get_effective_disabled()` documents); rename the real `USERS/{name}/Home/` folder to
`HomeAssistant/` via `Path.rename()` (a single atomic filesystem op, not copy-then-delete) for every
user who has one.

**A real trap avoided, not just handled**: m016's own migration *key* (`"home:m016_..."`) was kept
completely unchanged, even though it now lives inside the renamed `home_assistant/manifest.py` and
its internal call now writes `mark_installed("home_assistant", ...)`. Confirmed via
`module_registry.py`'s collision-check that a migration key only ever needs to be a unique string —
it never has to match the module's current id. Renaming m016's key too would have made it look
*unapplied* on the owner's real instance (which already ran it once under the old key), causing it to
silently re-run on next boot — and since m016's logic re-checks `ha_config.json` and calls
`mark_installed()` if configured, a stray re-run could have **silently re-installed a module the
owner had explicitly uninstalled since**, overriding their own action without any error or signal
that it happened.

### Files

Backend: `module_packages/home/` → `module_packages/home_assistant/` (git mv), `manifest.py`
(id/prefix/paths + new m017), `backend/router.py` + `backend/dashboard_block.py` (the two
hardcoded-`"home"` gate bugs), `content/mod_store_catalog.json`, `features_service.py`. Frontend:
`module_packages/home/` → `module_packages/home_assistant/` (git mv), `manifest.js` (id), `frontend/
api.js` (all paths), `pages/settings/admin/Hosting.jsx` (import path + gate strings). Tests:
`test_home_module_conversion.py` rewritten with 6 new tests covering each of m017's four steps
individually plus a run-twice idempotency check (10 tests total, was 4); `test_home_agent_tools.py`
updated to the new id (still 6 tests). Docs: `API.md`/`MAP.md`/`AGENTS.md`/`TESTING.md`/`MEMORY.md`'s
evergreen module-id list updated to the new id and path — `MEMORY.md`'s own dated historical entries
and this file's own earlier sections today were deliberately left saying "home", an accurate record
of what was true when written, not a live reference.

**Verified**: full backend suite green (863 passed = 857 + 6 new), including every migration step
tested directly against a real Brain fixture (real JSON files, a real folder rename, not mocked),
`eslint --max-warnings 0` clean, `vite build` clean with `Home-*.js`/`HomeFavouritesBlock-*.js` still
splitting correctly. `git grep '"home"'` across backend source (outside tests, the deliberately-kept
migration key/help-anchor/tool-names, and this file) turns up nothing left over.

**Not verified**: same honest gap as everything else today — no live browser or real instance in this
sandbox, so the actual migration was never run against the owner's real, already-populated
`installed_modules.json`/`auth.json`/Brain folder, only against synthetic pytest fixtures built to
match those exact shapes. Worth a real backup before this hits the live instance, given it's the
first migration this session that moves real files rather than only rewriting JSON.
