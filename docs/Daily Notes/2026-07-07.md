# 2026-07-07 — Daily Note

## Session — docs_reminder.sh: full doc checklist + test-coverage nudge

- `docs/hooks/docs_reminder.sh`:
  - Base checklist expanded from 3 to 6 items — now always includes `CHANGELOG.md`, `docs/PROJECT.md`, and `docs/TESTING.md` (the old conditional CHANGELOG item was removed as redundant). `TESTING.md` added to the core-docs mtime skip-check.
  - New conditional **TESTS MISSING** item: fires when `app/backend/` code (excluding `tests/` and `automations_stubs/`) changed since last stop but no file in `app/backend/tests/` did — points at `docs/TESTING.md` and says to run pytest before committing. This is the enforcement mechanism for "write tests for each new thing."
  - `.pytest_cache`, `/venv/`, `/.venv/` added to the changed-file exclusions — pytest runs were previously being detected as backend code changes.
- Verified with a sandboxed copy (state files redirected to scratchpad): nudge fires when backend code changes without test changes, stays silent when tests changed in the same window.
- `docs/AGENTS.md` §4 (End-of-Turn Memory Check) updated to the 6-item list + TESTS MISSING note; decision logged in `docs/MEMORY.md`.
- Concurrent session note: the Assets module MVP landed in a parallel session today (its MEMORY.md decision entry and AGENTS.md module-ID update were merged around, not touched).

## Session — Assets module MVP (REQUIRED BEFORE PILOT CLIENT)

Planned via plan mode with three owner Q&A rounds (Grok draft assessed: good bones, platform extras cut), then built end-to-end.

**Owner decisions locked** (full detail in MEMORY.md Key Decisions):
- Single object type, arbitrary nesting; **Templates** (owner's term) are admin-only, instance-level, ordered typed fields + defaults; bare-bones start (no seeds; optional example insert).
- Assets per-user per-workspace + subtree **sharing** (team/household/named user, read|edit) + **hidden_from** exclusions that beat shares — enforced server-side.
- **Pool conversion** (admin): physical move to `_household`/`_team` so objects survive account deletion; pool writes reuse `pool_edit`.
- **Archive-only** for members; hard delete admin + confirm, blocked with children. Per-asset **history** (cap 50).
- **Task linking both directions**; AI gets **full CRUD + template management** (writes approval-gated by approve mode); **n8n read+write in MVP** via `X-Automation-Token`.
- No rollups; CSV import deferred.

**Built:**
- Backend: `services/assets_service.py`, `services/automations_config.py`, `routers/assets.py` (+ registration, module id `assets`, `_BUSINESS_MEMBER`), 8 agent tools, task `asset_id` passthrough. 44 new tests → suite **249 passed**; black/isort clean.
- Frontend: `pages/Assets.jsx` (recursive tree, filters, archived toggle), `components/AssetModal.jsx` (dynamic template fields, attachments via blob URLs, access section, history, archive/convert, nested pre-linked TaskModal), `components/TemplateManager.jsx`, TaskModal asset picker, Dashboard `AssetsWidget`, Admin → n8n **Automation API token** reveal/rotate, api.js namespace + `requestBlob()` + `X-Workspace` on uploads.
- **R1 fix shipped**: Vite `assetsDir` → `static/`, backend mount `/assets` → `/static` (guarded) — the `/assets` page route was shadowed by the static mount in production; deep-link verified live.

**Verified live** on the relaunched stack: health OK, `/assets` deep link serves the SPA, `/static` bundle 200, automation API 401 without token, assets API 401 unauthenticated. Owner UI walkthrough still recommended (template → subdivision → parcel → share → convert → AI approve flow).

## Follow-up

- Owner: live UI walkthrough of the assets flow; push commits; watch CI (local venv is Python 3.14, CI is 3.11).
- Next build: **Automation Inbox** (reuses `automations_config.py` token), then land-lead workflow.
- Deferred assets follow-ups logged in TASKS.md ("Features that unblock scale").
