# 2026-07-12 — Daily Note

## Session — Employee access to Assets (contribute level + comments), planned 2026-07-11 evening

Owner's question: how should employees update assets (status changes, notes) — accounts or API calls? **Decision: humans are real users (identity, audit, revocation); the n8n automation token stays machine-only.** Planned in plan mode (2 Q&A rounds); owner added comment notifications w/ jump button, automation comments API, goals/tasks split, and role-based hide-from.

### Shipped (backend 294 green, black clean; frontend build clean)

- **Contribute access level** — `shared_with` entries (and new pool `contributors`) support `access:"contribute"` with per-entry `caps: {fields:[...], add:[comments|files|children]}`; owner configures via checkbox panel per share row (`CapsSelector` in `assetDisplay.jsx`). Router enforces: PATCH only capped field keys (name/parent/notes always 400), upload needs `files`, create-under needs `children`. `normalize_caps()` defaults to comment-only. Multiple matching entries union. Personal contribute shares keep the accept handshake; **pool contributors have no handshake** (pool already workspace-visible) — solves "crew updates team assets" without the coarse `pool_edit` grant.
- **Comments** — `comments` log per asset (cap 100, ≤2000 chars, attributed, oldest trimmed). Posting: edit-level or `comments` cap (read = view-only; stricter than the drafted "anyone visible" — consistent with caps philosophy). Delete: author/owner/pool-manager. **Notifications**: every edit-level user except the author gets in-app notif with `{type:"open_asset"}` action (NotifBell renders **View →**) + ntfy/web-push with `/assets?asset=<id>` deep link — new `suggestions_service.notify_user()` helper; Assets.jsx consumes `?asset=` and opens the read-first view.
- **Automation API** — `POST /assets/automation/assets/{id}/comments` (attributed "automation", same notifications); documented in API.md alongside the pre-existing PATCH edit endpoint (owner explicitly wanted both documented).
- **Role hide** — `hidden_from` accepts dynamic `role:<feature_role>` entries (validated against features.json; follows the role, unlike snapshot shares). `viewer_role` param threaded through `list_visible`/`find_asset` from routers **and agent tools** (so chat can't leak role-hidden pool assets). TagInput hide-from suggests `role:*` chips via `/assets/roles`. Note to owner: owner-owned assets are invisible-by-default — role hide mainly matters on pool assets.
- **AssetView is now the employee workspace** — quick status dropdown (edit users + contribute-with-status), inline FieldInputs for granted fields w/ one "Save updates" PATCH, comment list+box, capped upload; contribute users never see the editor (no Edit button). `onAssetUpdated` syncs modal state + editor form so a later Edit/Save can't revert inline changes. Tree row "＋ Add inside" now also shows for contribute-with-children (Move stays edit-only).
- **Goals/Tasks split** — `Tasks.jsx` filters `type==='goal'` from the merged list; Goals page unchanged; calendar/top3 intentionally still include goals (noted in CHANGELOG).
- `FieldInput` moved to `assetDisplay.jsx` (shared by editor + view).

### Tests
13 new (contribute PATCH gating incl. attribution, caps annotations, children-cap create + inheritance, pool contributors, contributors-rejected-on-personal, comment roundtrip/caps/limits, posting rights, delete permissions, notification audience, automation comment, role hide dynamic + unknown-role 400). Suite **294 green**.

### NOT deployed
Backend baked into image — owner must run `bash launch.sh --skip-build` (also still pending from yesterday's pool-asset fix + m006 Folder template; one relaunch covers all of it).

## Session 2 — owner testing feedback (caps bug + comment hardening + mutes + goals)

- **BUG (owner found, on "test tester"):** changed contribute caps didn't take effect — root cause: grant resolution UNIONED all matching entries, so a household/team-wide entry (old caps, or plain edit) blended with/overrode the per-user entry. **Fix: specificity resolution** (`_resolve_grant`) — a by-name entry fully overrides group/role entries, for both `shared_with` and pool `contributors`; union only across same-level contribute entries. This is what makes per-member restriction on household/team-shared assets possible at all.
- **Comments hardened per owner:** deletion is **admin-only** (audit log — was author/owner); owners/pool-managers instead get **"Hide for everyone"** (`comments_hidden` flag, `PUT /assets/{id}/comments/visibility` — posting blocked incl. automation, data kept, history records `comments_off`/`comments_on`).
- **🔔 comment-notification mute:** per-user opt-out at `USERS/{name}/Assets/comment_mutes.json`; muting a node silences its **whole subtree** (ancestor walk in `_muted_node` at delivery — new children auto-covered). Bell button in AssetView header (edit-level users only — they're the notification audience) → popup: mute/unmute, or "muted through <parent>" when inherited. `GET/PUT /assets/{id}/mute`.
- **Goals off the dashboard:** `priority_service._pending_non_goal()` excludes `type='goal'` from top3/scored (dashboard Top 3 + morning digest); Dashboard Due-Today filters client-side. Goals stay on /goals, the calendar (dated), and goal_drift. **Goals widget** logged into the customizable-dashboard backlog item (owner floated it; not built).
- 6 new tests (specificity ×3, comments-hidden gating, subtree mute, goals scoring); author-delete test flipped to admin-only. Suite **300 green**, black clean, build clean.
- Still **NOT deployed** — one `bash launch.sh --skip-build` covers everything since 07-11.

## Session 3 — caps bug round 2 (pool_edit) + comments toggle rework

- **Bug root-caused from live Brain data** (`auth.json` + `_household/Assets/assets.json`): "test tester" holds `pool_edit: ["household"]`, and "test asset" is a household pool asset with a correct by-name contributor entry. The pool branch computed `can_edit` from the blanket grant **before** consulting contributors → caps were dead code for any pool manager. (Verified the deployed backend was already on the session-2 code via the new /mute route → 401, so this was the remaining hole, not a stale deploy.)
- **Fix:** by-name contributor entry **downgrades a non-admin pool_edit manager** to contribute-with-those-caps on that asset (`_contributor_caps(named_only=True)` gates it in both `find_asset` and `list_visible`). Group/whole-team entries never downgrade managers; admins never restricted. Pool ladder: admin > by-name contributor > pool_edit > group contributor > read.
- **Comments toggles per owner intent:** (1) any user can **collapse** the section for themselves — plain AssetView state, reappears on reopen; (2) the **off-for-everyone** switch moved to the **edit page** (On/"Off for everyone" pill, immediate PUT) and its backend gate relaxed from can_manage → **can_edit**. View shows a small "manage it in ✎ Edit" note to edit users when off.
- 3 new tests (manager downgraded incl. PATCH gating, group entry doesn't downgrade, admin never downgraded). Suite **303 green**, black clean, build clean. **Deploy needed:** `bash launch.sh --skip-build`.

## Session 4 — Automation Inbox (planned + shipped)

Owner questioned the standalone-module idea himself — decided (plan mode, 3 Q&A): **fold the Inbox into the Automations module**, notifications through the existing bell/push. Additions from Q&A: **acting = admins + picked reviewers**; **named inboxes** (create inboxes, route workflow_keys to them, per-inbox notify list); both workspaces.

- **Backend**: new `services/automation_inbox_service.py` + inbox section in `routers/automations.py`. Named inboxes `{name, notify, reviewers, workflows}`; items `{workflow_key, external_id, title, summary, url, fields, status(new|interested|passed|offer_made|closed), status_by/at, note}`. Business store `_team` pool, personal own Brain. Dedup `(workflow_key, external_id)`; `GET /automations/inbox/seen` for pre-qualification skip; cap 500/scope (reviewed trimmed first); General inbox auto-created for unrouted keys; inbox delete 409s with items. Token endpoints reuse `X-Automation-Token`; JWT endpoints reuse `require_module("automations")` (business tab client-gated by `automations_business` — established pattern).
- **Notifications**: one batched `notify_user()` per recipient per POST — action `{type:"open_inbox", workspace, inbox_id}` + push url `/automations?view=inbox&inbox=<id>`. NotifBell generalized: `navTarget()` handles open_asset + open_inbox; open_inbox **switches workspace** before navigating. Automations.jsx consumes the query params.
- **Frontend**: Automations page gains **Workflows | Inbox (n new)** pills; inbox chips (when >1), status filter (New/Reviewed/All), item rows (expandable summary, field chips, ↗ link, action buttons when `_can_act`, attribution), ⚙ settings modal (name, workflows TagInput w/ seen-key suggestions, notify + reviewers member pickers via `GET /auth/users`), + New Inbox; empty state documents the POST contract.
- 13 new tests (routing, General-once, dedup+seen, validation, reviewer gating+attribution, personal owner acts/manages, admin-only business manage, name/reviewer validation, batched notification w/ action, personal implicit notify, trim order, delete 409, token 401). Suite **316 green**, black clean, build clean.
- **Deploy:** `bash launch.sh --skip-build` still pending (covers everything since 07-11).

Next: **land-lead workflow** (n8n) posting into this inbox — see TASKS.md "Now".

## Session 5 — inbox invisibility fix (owner testing)

- Owner created an inbox and saw nothing. Two causes found from disk: (1) inbox chips only rendered when `length > 1`, so the FIRST inbox with no items was invisible; (2) his "land leads" inbox landed in the **personal** store (`USERS/anthony bailey/.../inbox.json`) — he was in the personal workspace and nothing said which scope the modal creates into.
- Fixes (frontend-only, live via bind-mounted dist): chips always render (📥 prefix; single inbox shown selected); ⚙ Settings works with a lone inbox without selecting a chip; empty state names the ready inboxes; the create/settings modal states the scope ("Business inbox — shared with the team" / "Personal — switch to business workspace for a team inbox").
- Owner's stray personal "land leads" inbox left in place — he can delete it from the personal workspace (⚙ → Delete) and recreate in business.

## Session 6 — Finance module: interview, plan, Phase A shipped

Owner asked for a finance tracker before the business-workflow build — customizable, business/personal/pool trackers, bank-linked spending data, security-first ("no data leak, no getting robbed"). Interviewed him (2 rounds, plan mode) after explaining how other apps do bank linking (aggregators/OAuth read-only tokens, SimpleFIN, CSV; nobody stores bank passwords).

**Decisions (full detail in MEMORY.md + approved plan `~/.claude/plans/before-building-a-workflow-valiant-willow.md`):**
- SimpleFIN from day one, but **admin-managed**: user requests a connection → admin enters the setup token in a new Admin → Bank Connections card (owner's change during plan approval); token per-user, read-only, revocable
- MVP = everything: accounts/balances/net worth, budgets+alerts, recurring bills, reports+widget, projected balance ("should have on day X") **with deviation alerts** (= fraud detection), accounting now (invoices, clients/AR "who paid/behind", tax, receipts); **CRM stays a separate future module** (reserved `contact_id` link)
- Sharing: asset-style, BOTH levels (book + per-account overrides), contribute caps (employee = add expenses only / no balances / own entries)
- Build split into 5 phases A–E, each ends green

**Phase A shipped (backend 337 green, black clean, build clean):** `finance` module (both workspaces, guest-disabled via m007) — books/accounts/categories/transactions with signed-integer-cents amounts, per-book per-year shards (cross-year edits move shards), computed-on-read balances/monthly report/net worth, pool books (`_household`/`_team`; member read, admin edit), personal books invisible to admins, `_resolve_book_access()` single access gate (Phase E extends it, no refactor), 3 AI read tools (`list_finance_books`, `list_finance_transactions`, `get_finance_report`), Finance page (book chips → Overview | Transactions, TransactionModal, BookSettings) + Dashboard net-worth widget. 21 new tests.

**Phase B shipped (350 green, black clean, build clean):** SimpleFIN admin-managed connections — member "Request bank connection" → admins notified (new `open_admin_banking` NotifBell action → /admin) → admin claims setup token in new **Admin → Bank Connections card** (connect/replace/sync-now/reveal/disconnect per user; reveal is the only endpoint outputting the access URL, 3/hour); member maps bank accounts → book accounts in **Finance → 🏦 Bank** (`SimpleFinPanel`; pool targets admin-only); `simplefin_service.py` sync engine (7-day overlap window, `simplefin_id` dedup, uncategorized landing + payee-rule auto-categorization, `synced_balance_cents` recorded for Phase C deviation checks, error notifications throttled 1/day to user+admins); scheduler jobs boot+2min & every 12h; payee rules learned on categorize (PATCH tx hook); CSV import (`finance_import_service.py`, preview → column-mapped commit, `import_hash` dedup, parens/$/comma parsing) with UI in BookSettings. 13 new tests.

**Next:** Phase C (budgets + alerts, recurring bills, planned items, projection, deviation alerts). NOT deployed — backend needs `bash launch.sh --skip-build`.

## Follow-up

- **Owner: run `bash launch.sh --skip-build`** (deploys yesterday's fix + m006 + today's employee features + session-2 fixes).
- Re-test the test-tester caps scenario after relaunch: with both a group entry and their personal entry present, changing the personal caps must now take effect immediately (specific beats group). Also: try deleting a comment as non-admin (blocked), Hide-for-everyone toggle, and the 🔔 mute on a parent asset (comment on a child → no notification).
- Owner live walkthrough (two accounts): share contribute w/ status+comments → accept → employee opens asset (view), changes status via dropdown, posts comment → owner's bell shows **View →** → lands on the asset. Pool: add a Contributor on a team asset → employee updates without pool_edit. Hide-from `role:crew` on a pool asset → crew user can't see it. Tasks page: goals gone (still on /goals).
- Employee onboarding when ready: create `crew` feature role (disable all but assets+dashboard), business workspace only — documented in AGENTS.md.
- Consider later: comment threading, automation comment edit/delete, per-employee API tokens (only if a concrete need appears).
