# MAP.md — LogCoreOS Navigation Index

This is the navigation index for all files in this repo. Keep it updated when files or folders are added.

---

## Repository Layout

```
LogCoreOS/
│
├── CLAUDE.md                     → thin pointer to docs/AGENTS.md
├── VERSION                       → current version string (read by launch.sh and update.sh)
├── README.md                     → user-facing quick start (do not move — it's for humans)
├── CHANGELOG.md                  → user-facing version history (Keep a Changelog format)
├── SECURITY.md                   → vulnerability disclosure policy
├── LICENSE                       → project license
├── launch.sh                     → one-command startup: builds frontend, generates .env, starts Docker
├── requirements-dev.txt          → dev/test deps (pytest, etc.)
│
├── app/
│   ├── backend/
│   │   ├── main.py               → app factory, router registration, CORS + security headers middleware, static file serving; `lifespan()` runs migrations → `module_registry.register_routers()` (caches result on `app.state.active_module_ids`) → the SPA catch-all — that order matters, see module_registry.py
│   │   ├── config.py             → all env vars via Pydantic Settings (reads docker/.env)
│   │   ├── scheduler.py          → 9 APScheduler jobs (recurring, digest, overdue, weekly, goal progress snapshot, goal drift, goal due urgency, JTI cleanup, custom) — `job_goal_progress_snapshot`/`job_goal_due_urgency` added 2026-08-28 when Goals became a real module (snapshot runs at `overdue_check_hour:15`, before the reworked `goal_drift` at `:30`, then `goal_due_urgency` at `:35` — see module_packages/ entry below)
│   │   ├── module_registry.py    → Mod Store universal module system: `ModuleManifest` dataclass (incl. `admin_agent_tools`, added 2026-08-25 for household's admin-only shared-task tools — the first module to own one; `admin_agent_tool_names()` unions it across active modules, feeding `agent_service.py`'s `_get_tools()` the same way `read_only_agent_tools`/`read_only_agent_tool_names()` already do for the read/write distinction; `owned_metric_providers`, added 2026-08-28 for Goals — see below), `discover_manifests()`/`active_manifests()` (importlib-based, scans `module_packages/*/manifest.py`), `register_routers()` (asymmetric failure handling — optional modules log-and-skip, `uninstallable` ones crash boot loudly — confirmed for real, not just synthetically, by tasks' 2026-08-25 conversion), `brain_paths_for_disabled()` (shared with `agent_service.py`, since services never import from routers). New `MetricProviderSpec` dataclass (2026-08-28, key/label/`config_schema`/`resolve`) + `ModuleManifest.owned_metric_providers` + `metric_providers()` discovery function (unions every ACTIVE module's own registered providers, keyed `f"{module_id}:{provider.key}"`, mirroring `admin_agent_tool_names()`'s own discovery shape exactly) — a fully generic, extensible registry letting any module declare "I can compute a current/target/pct for something," built for Goals' metric-driven progress but owned by no single module; Goals itself contains zero provider-specific logic, it only discovers and lists. v1 registry-discovered providers: `finance:budget_pct` (`module_packages/finance/manifest.py`), `contacts:number_field` and (2026-08-29) `contacts:weight` (`module_packages/contacts/manifest.py`) — see `docs/MEMORY.md`'s 2026-08-28 entry (Goals). New (2026-08-29) `directional_pct(current, target, direction="increase"|"decrease", start_value=None) -> int` — 0-100 progress math shared by every `MetricProviderSpec.resolve()` that needs a current-vs-target percent (Goals' own "manual" provider, Contacts' `number_field`/`weight`), lives here rather than inside `module_packages/goals/` specifically so a module registering its own provider doesn't have to import from a sibling module's package for pure math; found and fixed BEFORE shipping — every metric provider built in Round 1 assumed "higher current = more progress," backwards for a decrease-type goal like weight loss (current/target×100 would show 120% on day one and get worse as you actually lose weight) — "decrease" computes `(start - current) / (start - target)`, requiring an explicit `start_value` since a reading like weight has no natural zero baseline. SECURITY: this executes arbitrary Python — safe only because module_packages/ is first-party reviewed code, never community-submitted (see its own docstring)
│   │   ├── module_packages/      → converted modules live here, one dir per id — journal/, home_assistant/, automations/, calendar/, tasks/, household/, team/, chat/, notes/, dashboard/, assets/, contacts/, finance/, and (2026-08-28, the 14th entry, deliberately NOT part of the 13-module rollout below — genuinely new design work, not a conversion, see its own paragraph at the end of this entry) goals/ — 13 of these 14 are the full and FINAL list the Mod Store rollout plan ever targeted (2026-08-24 through 2026-08-28; three locked/uninstallable — tasks/chat/dashboard — the rest optional): manifest.py (ModuleManifest incl. one or more upgrade migrations + help_section; journal also has on_install/on_new_user backfill hooks, the rest don't need any — their per-user/pool folders are lazily created, or for tasks, already unconditionally seeded by the pre-existing brain/USERS/_template/Tasks/ template copy since tasks is locked and was never optional, or for household/team, already lazily self-created by each router's own pre-existing `_ensure_household()`/`_ensure_team()` helpers, unchanged by either conversion), backend/{router,dashboard_block,agent_tools}.py (+ service.py for journal only — home_assistant's ha_service.py, automations' n8n_service.py/automations_config.py, calendar's events_service.py/routers/_event_models.py, and tasks'/household's/team's shared task_service.py/priority_service.py/recurring_service.py/routers/_task_models.py all stay in core instead, each having real consumers outside its own module, see docs/MEMORY.md; calendar and team both have no agent_tools.py — calendar owns no AI tools, team owns none today either, a real pre-existing asymmetry with household's own 5, not something invented or fixed by this conversion), tests/ (tasks'/household's/team's own router-adjacent code stayed in core app/backend/tests/ where the underlying service did — test_task_service.py/test_priority_service.py/test_recurring_service.py — but their OWN moved router/agent-tool code gets first-ever real coverage in module_packages/{household,team}/tests/test_{household,team}_router.py + household/tests/test_agent_tools.py (moved from tests/test_agent_member_resolution.py); new tests/test_{tasks,household,team}_module_conversion.py each cover their own conversion machinery). Frontend counterpart lives at app/frontend/src/module_packages/<id>/ (manifest.js + frontend/{Page,BlockComponent(s),api}.jsx|js) — discovered by lib/moduleRegistry.js, see the frontend section below. A manifest.js's `blocks` is always an array (generalized 2026-08-25 from a singular `block` key once automations needed to own two block types — workflow_status + inbox_summary — the first module with more than one). home_assistant/ itself was renamed from home/ 2026-08-24, in two passes: first id/directory/API-prefix/Brain-folder (m017 carries an already-upgraded instance's state across it), then — at the owner's explicit request for full internal consistency — the frontend route (/home → /home-assistant), the help section anchor id (home → home-assistant), the 4 AI tool names, and the home_favourites → home_assistant_favourites dashboard block type (m018 carries THAT across, including a fix to core migration m010, which seeds every new user's first dashboard and had the old block type hardcoded). automations/ (2026-08-25) deliberately took the opposite approach — id/directory/routes/tool-names all stayed "automations," only the display name ("n8n Automation") changed, per the owner's explicit instruction — see docs/MEMORY.md for both. calendar/ (2026-08-25) took automations' no-rename approach one step further — no display-name change either, "keep as calendar" per the owner — and is the first converted module with a real `?<id>=<record>` deep link, so its manifest.js also carries a new `recordParam: 'event'` field, merged into deepLinks.js's RECORD_PARAM the same way `to` already merges into MODULE_ROUTES. tasks/ (2026-08-25) is the first LOCKED (`uninstallable: True`) conversion — the backend manifest's own upgrade migration (m021) marks it installed unconditionally, no existence-guard, since it was never optional; ModStore.jsx already showed "Always active" for any `uninstallable` entry before this, fully proven now by a real module rather than only a synthetic test fixture. Goals (/goals) deliberately stayed OUT of tasks' manifest AT THE TIME — it was a purely frontend nav id (no `require_module("goals")` existed; App.jsx's /goals route used `moduleId="tasks"`) that was never manifest-driven, so only its page file moved into `module_packages/tasks/frontend/Goals.jsx`; its own `lib/constants.js` nav entry and `content/help.json` section both stayed exactly where they were. **This didn't last**: Goals became a real, independent module the same day as Finance (2026-08-28) — `module_packages/tasks/frontend/Goals.jsx` was deleted outright and moved into `module_packages/goals/frontend/Goals.jsx`, see the goals/ paragraph at the end of this entry. household/ and team/ (2026-08-25, converted together in one increment since neither has its own service file — both ride tasks'/calendar's now-core locations, per the rollout plan) are also no-rename conversions; household/'s router prefix deliberately stays `/api/v1/shared` (the historical mount point, unrelated to the module id) while team's stays `/api/v1/team` (already matched). This increment surfaced two real, generic module-system gaps neither prior conversion had hit: (1) a shared dashboard block (`pool_tasks`, serving both pools) had to split into `household_tasks`/`team_tasks` once `BlockSpec.module` needed to gate it to one real module — see the dashboard_blocks/ entry below; (2) `ModuleManifest` had no way to mark a tool admin-only — household's 5 shared-task-management AI tools (4 admin-only + 1 member-accessible) were the first a module ever owned, so a new `admin_agent_tools` field + `module_registry.admin_agent_tool_names()` were added, mirroring exactly how `read_only_agent_tools` was added for journal's read/write distinction — see docs/MEMORY.md for both. chat/ (2026-08-26) is the second LOCKED (`uninstallable: True`) conversion, after tasks — its own upgrade migration (m025) marks it installed unconditionally, same shape as tasks' m021; unlike tasks, chat has no dedicated service.py of its own to leave in core, because `agent_service.py`'s session/pending-turn/presence/tool-orchestration engine was already confirmed pre-existing, permanent core infrastructure (never converts, per the plan's own closing note) — chat's router.py is a thin HTTP wrapper around it. This conversion found and fixed two real, pre-existing enforcement gaps rather than surfacing new module-system mechanism gaps: (1) `GET /chat/runs` and `GET /chat/runs/{run_id}` were gated on plain `Depends(get_current_user)` instead of `Depends(_require_chat)`, letting a user with chat disabled still read past agent tool-use run history; (2) chat's own archive folder had no `owned_brain_paths` entry anywhere, so a disabled user's past conversations stayed fully readable via both the Brain browser and the AI's own list_brain_files/read_brain_file/search_brain tools — closed by declaring `owned_brain_paths=["Chats"]`, joining `brain_paths_for_disabled()`'s conditional half (`_ALWAYS_SKIP`'s unconditional `{"Tasks", "Business"}` stays separate, structural). See docs/MEMORY.md for the full writeup. notes/ (2026-08-26) is the first conversion to genuinely test the sidecar-share-index sharing pattern (`Notes/_shares.json` + `notes_index.py`) — the direct precedent Assets/Contacts/Finance will need for their own future conversions; `notes_service.py`/`notes_index.py` both stay core, `user_deletion_service.py` imports both directly alongside the equivalent assets/contacts/finance pairs. Surfaced the same three-shaped gap Chat's own conversion found (real pre-existing enforcement holes, not new mechanism gaps): (1) all 7 note AI tools lived unfiltered in the static `_USER_TOOLS` list, closed by making them module-owned; (2) no `owned_brain_paths` entry, closed by `owned_brain_paths=["Notes"]`; (3) `note_embed`'s dashboard block had no `module=` gate at all, the exact `pool_tasks`-before-Household/Team situation — closed the same way. Also surfaced a genuine, previously-latent mechanism gap of its own: `_module_tool_dispatch()`'s call site never threaded `workspace` through to a module's own `execute()` (`execute_fn(name, inputs, user)`, 3 args) — harmless for every prior module's tools (household/tasks/journal/home_assistant are all workspace-blind by construction or already documented as such), but notes' own tools are genuinely workspace-aware (a note lives in either the personal or business `Notes/` folder), so silently dropping it here would have been a real regression, not a no-op. Widened to `execute_fn(name, inputs, user, workspace)`, with every other module's `execute()` picking up the new parameter (default `"personal"`, unused) for signature parity — see docs/MEMORY.md for the full writeup. dashboard/ (2026-08-27) is the third LOCKED (`uninstallable: True`) conversion, after tasks and chat — keeps the pre-existing id "dashboard" (singular), no rename; its own upgrade migration (m027) marks it installed unconditionally, same shape as tasks' m021/chat's m025. Owns zero block types (it's the container, never a REGISTRY entry) and has no `backend/dashboard_block.py`; `dashboards_service.py`/`dashboard_templates_service.py`/`dashboard_index.py` all stay core — the strongest "stays core" case yet, since `migrations/runner.py`'s own core migrations (m010/m011/m012) call `dashboards_service` directly and migrations run before module registration exists at all in boot order, not just "another router imports it" like every prior case. Surfaced the same enforcement-gap shape as Chat's/Notes' own conversions: all 10 dashboard AI tools lived unfiltered in the static tool list (closed by making them module-owned) — no `owned_brain_paths`/block-gating gap existed here since Dashboards data is JSON not markdown (same structural category as Tasks — `"Dashboards"` added to both `_ALWAYS_SKIP` sets alongside `"Tasks"`) and it owns no block type to gate. The one genuinely new problem no prior conversion needed to solve: App.jsx's root route (`to: '/'`) needed to stay hardcoded and ModuleRoute-UNWRAPPED (a user with `dashboard` disabled would otherwise hit a self-targeting redirect loop at "/") — see the frontend `pages/` section below and docs/MEMORY.md for the full reasoning. assets/ (2026-08-27) is the 11th converted module, first of the three largest/most structurally complex remaining (Assets, Contacts, Finance — deliberately last, once the sidecar-share-index pattern (proven by Notes) and the locked-module pattern (proven by Tasks/Chat/Dashboard) were both battle-tested), and the first NORMAL optional-module conversion since Notes (not `uninstallable`, unlike the three locked conversions in between). `routers/assets.py` moved to `module_packages/assets/backend/router.py`, still mounted at `/api/v1/assets`, no rename; two endpoints (`GET`/`POST /automation/token[/rotate]`) deliberately did NOT move with it — gated only by `require_admin`, not `require_module`, inside an optional module's router, meaning uninstalling Assets would have silently taken away the admin's only way to view/rotate a token Contacts' own automation API still depends on (`automations_config.py` itself stays core, unowned by either module) — relocated to `routers/auth.py`'s admin section instead (`GET`/`POST /auth/admin/automation-token[/rotate]`), the same precedent as its other admin/*-settings endpoints. `services/assets_service.py`/`assets_index.py` both stay core — the strongest "stays core" case of any conversion yet, confirmed by `module_packages/dashboard/backend/router.py` (an already-converted SIBLING module) importing `assets_service` directly for its own Dashboard Hero subject resolver, on top of the usual `user_deletion_service.py`/`dashboard_blocks/_actions.py`/`module_packages/contacts/backend/router.py`'s (moved 2026-08-28) link_deal_asset/unlink_deal_asset/`main.py`'s `_warm_share_index()` consumers every prior "stays core" service already had. `services/dashboard_blocks/_assets.py` moved to `module_packages/assets/backend/dashboard_block.py` (4 blocks: documents/linked_tasks/linked_contact/my_assets_summary), and `_collections.py` (the `collection` block resolver, previously filed separately since its own docstring anticipated future generalization to non-Assets record types) folded into the SAME file and deleted outright rather than kept as a hypothetical-future-proofing exception — it's 100% Assets-data-dependent today and, unlike every other block type, had no `module=` gate at all before this, the exact `pool_tasks`-before-Household/Team situation; `dashboard_blocks/registry.py`'s core-resolver import tuple lost both `_assets`/`_collections`, down to `(_actions, _ai_usage, _contacts, _finance, _freeform)`. New `manifest.py`: NOT locked (a normal optional module, unlike the three `uninstallable: True` conversions immediately before it), `owned_brain_paths=["Assets"]` (JSON+binary, same structural category as Tasks/Dashboards — `"Assets"` joined both `_ALWAYS_SKIP` unconditional sets, not the conditional per-user ones Chat/Notes use), 10 `owned_agent_tools` (3 `read_only_agent_tools`, 3 `admin_agent_tools` using the schema-visibility mechanism Household's own conversion established), 5 `owned_block_types`, one guarded upgrade migration (`m028`, same features.json-existence idiom as every optional module's own). Frontend: `pages/Assets.jsx`/`components/AssetModal.jsx`/`AssetView.jsx`/`TemplateManager.jsx` all moved into `module_packages/assets/frontend/`; a new `frontend/api.js` carries the ~34-method client moved verbatim out of `lib/api.js` (which no longer exports `assets` at all), hand-rolling `uploadFile`/`fileBlob`'s multipart/blob fetch logic inline against the exported `BASE` constant, matching automations' own established precedent for the same reason (core's internal `requestFile`/`requestBlob` helpers are deliberately not exported); `automationToken`/`rotateAutomationToken` moved from the old `assets` export to `admin` in `lib/api.js`, matching the backend's URL move. 5 dashboard-block components (`DocumentsBlock`/`LinkedTasksBlock`/`LinkedContactBlock`/`MyAssetsSummaryBlock`/`CollectionBlock`) extracted out of `components/dashboard/blocks.jsx` the same way Calendar's/Tasks'/Notes' own blocks were, `LinkedTasksBlock` importing `TaskRow` and `CollectionBlock` importing `BlockActionButtons` back from core `blocks.jsx` cross-package (see that file's own entry below); `blockRegistry.js`'s 5 corresponding hardcoded entries removed, now supplied by the manifest's own `blocks` array via the pre-existing `MODULE_PACKAGES` merge loop. contacts/ (2026-08-28) is the 12th converted module, second of the three largest/most structurally complex remaining (Assets, Contacts, Finance — deliberately last), converted the same day as Assets. `routers/contacts.py` (1414 lines) moved to `module_packages/contacts/backend/router.py`, still mounted at `/api/v1/contacts`, no rename — unlike Assets, no endpoints moved out this time, since Contacts' own automation endpoints (`/automation/lookup`, `/automation/contacts`, `/automation/interactions`, `/automation/deals`) were already correctly gated only by the shared n8n token (`_require_automation_token`), the same accepted pattern Assets' own automation endpoints already use. `services/contacts_service.py` (2055 lines)/`contacts_index.py` (79 lines) both stay core — MORE dependents than any prior "stays core" service, including a SECOND already-converted sibling module importing it directly (`module_packages/dashboard/backend/router.py`'s Dashboard Hero contact-subject resolver, on top of `module_packages/chat/backend/router.py`'s lazy import for AI system-prompt profile context), plus `services/agent_service.py`'s own core `get_profile`/`update_profile` tools, which deliberately stayed in `agent_service.py` rather than moving into Contacts' new `agent_tools.py` since Profile is a generic concept independent of Contacts' module state — see docs/MEMORY.md's 2026-08-28 entry. A genuine split, the mirror of Assets' own `_collections.py` decision: `services/dashboard_blocks/_contacts.py`'s 4 block types didn't move wholesale — `linked_deals`/`contacts_list`/`linked_assets` (Contacts' own reverse asset lookup, distinct from Assets' `linked_contact` forward direction) moved into `module_packages/contacts/backend/dashboard_block.py`, each gaining a `module="contacts"` gate for the first time (closing a real, previously-open gap — none of the 4 blocks had any `module=` field before this); but `custom_fields` reads from EITHER `contacts_service` OR `assets_service` depending on which config field is set, the same "spans more than one module, owned by none" shape as `dashboard_blocks/_actions.py`'s `nav_button`/`status_button`, so it was extracted into a NEW core file, `services/dashboard_blocks/_custom_fields.py`, and stays ungated (no `module=` field) — `dashboard_blocks/registry.py`'s core-resolver import tuple changed from `(_actions, _ai_usage, _contacts, _finance, _freeform)` to `(_actions, _ai_usage, _custom_fields, _finance, _freeform)`. New `manifest.py`: NOT locked (a normal optional module, like Assets), `owned_brain_paths=["Contacts"]` (JSON+binary/photos only, same unconditional structural category as Tasks/Dashboards/Assets), 6 `owned_agent_tools` (`list_contacts`/`get_contact`/`create_contact`/`update_contact`/`log_interaction`/`create_deal`; 2 `read_only_agent_tools`, NO `admin_agent_tools` — unlike Assets' 3, Contacts owns no admin-only AI tool), 3 `owned_block_types` (`linked_deals`/`contacts_list`/`linked_assets`, not `custom_fields`), one guarded upgrade migration (`m029`, Assets used `m028`). Frontend: `pages/Contacts.jsx`/`components/contacts/{ContactDetail,ContactModal,ContactAvatar,BulkConvertContactsModal,SectionHeader}.jsx`/`phone.js`/`presence.js` all moved into `module_packages/contacts/frontend/`; `components/contacts/ContactPicker.jsx` STAYS CORE — the frontend mirror of `custom_fields` staying core — confirmed by import-grep to have 7 external importers beyond its own module's `ContactModal.jsx` (`module_packages/dashboard/frontend/{DashboardSettingsModal,CreateDashboardModal}.jsx`, `components/finance/{TransactionModal,InvoicesPanel}.jsx`, `components/dashboard/{ModuleAndRecordPicker,BlockPicker}.jsx`, `components/assetDisplay.jsx`), so `components/contacts/` now holds only `ContactPicker.jsx`. Two admin-gated-but-not-module-gated endpoints were found and judged independently, not reflexively fixed the same way: `PUT /contacts/fields` had `require_admin` but no `require_module("contacts")` (unlike its own `GET` sibling) — a real gap, fixed by adding `require_module`; `GET /contacts/available-for-linking` was deliberately left as-is, matching `/contacts/me`'s own precedent that self-contact/account-creation infrastructure must keep working regardless of module state. `_guest_map()`'s pre-existing `guest["contacts"] = False` line was deliberately left untouched — an unconditional post-copy override that keeps working correctly whether "contacts" is a core id or a converted/installed module id, the first converted-and-optional module to carry that exact combination. See docs/MEMORY.md's 2026-08-28 entry for the full writeup. **`manifest.py` gained a second metric provider, `contacts:weight` (2026-08-29, `_resolve_weight`)** — root cause of the original weight-tracking gap: `height_cm`/`weight_kg` are built-in, always-private fields directly on the Contact record (`services/contacts_service.py`'s `_PRIVATE_SHORT_FIELDS`), never visible to `_resolve_number_field`'s own `contact["custom"]` read, so weight needed its own dedicated provider rather than folding into that one. Always resolves the caller's own self-contact, no `contact_id` override (the field is private by design); defaults `direction` to `"decrease"` (weight loss is the more common framing) via its own `_WEIGHT_DIRECTION_FIELD` config-schema constant — deliberately NOT a reuse of `_resolve_number_field`'s shared `_DIRECTION_FIELD`, since that one lists `"increase"` first and the frontend picker pre-selects `options[0]` when a config has no explicit value yet, which would have shown "Increase" in the UI while the backend was already computing "decrease" behind the scenes — a real visual/effective mismatch caught before shipping. finance/ (2026-08-28, converted the same day as Assets and Contacts) is the 13th and FINAL module in the entire rollout plan — the largest and most structurally complex module in the app, and the only one split across SIX separate router files (`routers/finance.py`/`finance_banking.py`/`finance_planning.py`/`finance_invoicing.py`/`finance_sharing.py`/`finance_transfers.py`, 2,196 lines/78 endpoints total) rather than one. Since `ModuleManifest.get_router()` only supports returning ONE router, `manifest.py`'s `_get_router()` composes all six (moved to `router.py`/`router_banking.py`/`router_planning.py`/`router_invoicing.py`/`router_sharing.py`/`router_transfers.py`) into a single parent `APIRouter()` via six `include_router()` calls, each keeping its own original tag rather than adding a new umbrella "finance" tag, so the OpenAPI grouping stays byte-identical to before — a genuinely new assembly pattern no prior conversion needed, confirmed by a dedicated test asserting exactly 78 routes and exactly the original 6 tags. `services/finance_service.py`/`finance_invoice_service.py`/`finance_index.py` all stay core — the strongest "stays core" case yet, since `module_packages/contacts/backend/router.py`'s `GET /contacts/{id}/finance` endpoint makes deep, multi-function calls into both. `services/finance_planning_service.py`/`simplefin_service.py` ALSO stay core, for a genuinely new reason: neither has a sibling-module or core-router dependent, but both are imported directly by `scheduler.py`'s own job functions (`job_finance_nightly`/`job_simplefin_sync`) — a scheduler dependency treated exactly like a sibling-module dependency for the "stays core" test, mirroring `n8n_service.py`'s own precedent from Automations. `services/finance_reports.py`/`finance_import_service.py` DO move (into `reports.py`/`import_service.py`) — the one wrinkle: `finance_planning_service.py`'s `budget_status()` needed `reports.py`'s `month_end()` helper, so that two-line function was relocated INTO `finance_planning_service.py` (core) instead, with the moved `reports.py` importing it back from there — the reverse of the obvious direction, so a scheduler-critical core file never depends on `module_packages/finance/` even in principle. 13 admin-lifecycle SimpleFIN endpoints in `finance_banking.py` were found gated by `require_admin` alone, never `require_module("finance")` — the same narrow-inconsistency shape as Contacts' own `PUT /contacts/fields` finding — fixed by adding `require_module("finance")` to all 13. All 9 Finance AI tools were the sixth confirmed instance of the same unfiltered-`_USER_TOOLS` gap every prior conversion found. Frontend: `pages/Finance.jsx` + 8 of 10 `components/finance/` files moved into `module_packages/finance/frontend/`; `money.js`/`FinanceBookPicker.jsx` stay core (confirmed by import-grep — `money.js` has 3 external importers including an already-converted sibling module, `FinanceBookPicker.jsx` is reached only through core dashboard block-config infrastructure, never Finance's own page — the same "DashboardPicker.jsx-shaped surprise" Dashboards' own conversion first hit). New `frontend/api.js` carries the 79-method client (larger than any prior module's) moved verbatim out of `lib/api.js`, needing three hand-rolled local reimplementations including a genuinely new one, `listBooksForWorkspace`, which overrides the ambient `X-Workspace` header explicitly for admin pool pages. Core `lib/api.js`'s internal `requestBlob()` helper became fully dead once Finance's last real caller moved out — removed outright. **Milestone: `features_service.py`'s `_CORE_MODULE_IDS` is now just `["goals"]`** — the only hardcoded-core module id left in the app (never itself a real backend module, rides Tasks' own permission gate) — mirrored on the frontend by `lib/constants.js`'s `CORE_MODULES` (also just `{goals}`) and `lib/deepLinks.js`'s `_CORE_MODULE_ROUTES`/`_CORE_RECORD_PARAM` (`{goals: '/goals'}` / literally `{}`). See docs/MEMORY.md's 2026-08-28 entry (Finance) for the full writeup. **THIS IS THE LAST MODULE IN THE ENTIRE ROLLOUT** — all 13 modules the plan ever targeted are now converted; what's left is live-instance verification, not further conversions, see docs/TASKS.md's Mod Store tracking item. goals/ (2026-08-28, built the same day, immediately after the rollout's own migration-verification pass, but NOT one of the 13 above — genuinely new design work scoped through its own plan-mode interview, since "Goals" had never been a real backend module at all, just Task records with `type=="goal"` riding on Tasks' own permission gate) is NOT locked (`uninstallable=False`), `owned_brain_paths=["Goals"]`, 7 `owned_agent_tools` (2 read-only: `list_goals`/`get_goal`), `owned_block_types=["goals_progress"]`, one guarded migration `m031_migrate_goals` (converts every existing `type=="goal"` Task into a real Goal record across every real user and both pool pseudo-users, not just a mark-installed no-op). `backend/service.py` deliberately stays FULLY inside the package — unlike task_service.py/assets_service.py/etc., nothing outside needs Goals data directly, so it gets no per-user share-handshake (pool visibility only) and `user_deletion_service.py` needed zero Goals-specific handling. Task gained a new `goal_id` field (mirrors `asset_id`); `type=="goal"` was removed entirely from Task's Literal, forcing real deletions elsewhere: `task_service.cleanup_done_goals()`, `priority_service.py`'s `_pending_non_goal()` (simplified to `_pending()`), `recurring_service.py`'s goal branch, Tasks' own old `goals_progress` block and `POST /tasks/goals/cleanup` endpoint all removed — see each file's own entry below. Pool goals (household/team) are served from Goals' OWN router (`/api/v1/goals`, explicit `pool: bool` flag per endpoint), the same "single owning module serves personal + pool" shape Finance/Contacts/Assets/Notes use, not the older per-pool-router shape Tasks/Calendar predate — this surfaced a real subtlety during implementation: a pool pseudo-user's storage always lives at `workspace="personal"` internally regardless of the caller's own active workspace, caught and fixed via a `(store_user, store_workspace)`-returning `_store_for()` helper (an earlier draft got this wrong). `household_goals`/`team_goals` dashboard blocks live in household's/team's OWN `dashboard_block.py` files instead, gated `module="household"`/`module="team"`, mirroring `household_tasks`/`team_tasks`' own precedent exactly (gate on pool membership, not the data-owning module) — see the dashboard_blocks/ entry below. New generic infrastructure riding along, not Goals-specific: `module_registry.py`'s `MetricProviderSpec`/`owned_metric_providers`/`metric_providers()` (see that file's own entry above), letting a Goal pull its completion percentage from live data in another module — v1 ships a built-in subgoal/task rollup + a "manual" logged-history metric (both native to Goals) plus two registry-discovered providers declared on their OWN modules' manifests, `finance:budget_pct` and `contacts:number_field`; Assets/Journal/Calendar/Home Assistant/Automations providers are a deliberate fast-follow. Progress computation: an explicit metric always wins over a weighted-average rollup of subgoals/tasks (a 60%-complete subgoal contributes 0.6, not just "not done"). Delete takes two independent flags (`cascade`, `delete_linked_tasks`), not one fixed behavior. Four new independently-triggered signals: a completion celebration (via `suggestions_service.py`'s existing `notify_user()`, detected by a new daily scheduler snapshot job rather than a real-time hook, since a metric-driven percent can change from another module's own write with nothing Goals-specific to hook onto), an `on_pace` indicator, a reworked `goal_drift` (now compares current % against an N-days-ago snapshot in a new capped `Goals/goal_progress_history.json` log instead of only checking staleness-by-creation-date), and a new, separate `goal_due_urgency` suggestion — see scheduler.py's own entry above for the 2 new cron jobs. Frontend: new `module_packages/goals/frontend/{api,manifest,Goals,GoalModal,MetricPicker,GoalsProgressBlock}.js(x)` (see pages/ entry below for Goals.jsx/GoalModal.jsx detail) plus a new core `components/dashboard/ContactNumberFieldPicker.jsx` — a real, honest deviation from the plan, which assumed one shared component would serve both this and a hypothetical future Assets number-field picker; in practice Contacts' fields are global/instance-wide while Assets' are per-template, different enough data-fetching shapes that they needed separate components, so this shipped Contacts-only (see components/dashboard/'s own entry below). A known, honest v1 gap: linking/creating a task from within a POOL goal's GoalModal targets the caller's own PERSONAL task store, not the pool's — reading/unlinking already-linked pool tasks works correctly, only new creates/links from a pool goal's view are affected; filed in docs/TASKS.md's Product Backlog, not fixed this pass. Tests: `module_packages/goals/tests/test_goals_{service,router,agent_tools}.py` (24/11/6) + core `tests/test_goals_module_conversion.py` (8, deliberately core-not-nested, matching this repo's established convention for this file class even though Goals isn't technically a "conversion"); `tests/test_full_upgrade_migration_chain.py` updated to `_ALL_14`/`_OPTIONAL` += "goals". **Round 2 (2026-08-29, same still-uncommitted changeset, the owner's own 11-item try-it-out pass on the built module):** nested-modal clipping fix — `.modal-card`'s `backdrop-blur-sm` creates a new containing block for `position: fixed` descendants, so a modal DOM-nested inside another modal's own box sized/clipped against that outer box instead of the real viewport (a real, pre-existing latent bug, not new to this build); `GoalModal.jsx` now wraps its own top-level return in a local `Portal` helper (`createPortal(children, document.body)`, the same precedent `components/assetDisplay.jsx`'s photo viewer already established) so its recursive "+Subgoal" nesting self-solves at every depth, and separately portals its delete-confirmation popup; `module_packages/assets/frontend/AssetModal.jsx`'s pre-existing nested `<TaskModal>` got the identical `createPortal` fix directly (2 other instances there — `archivePrompt`, `SaveAsTemplateModal` — deliberately left unfixed this round, see `docs/TASKS.md`'s Product Backlog). New `poolTaskApi(workspace)` helper in `GoalModal.jsx` (dynamically imports `module_packages/household/frontend/api.js`'s `shared` or `module_packages/team/frontend/api.js`'s `team` client) fixes the Round 1 known gap where a pool goal's "+ Task"/"Link existing task" always hit the personal `tasksApi` regardless of which store the goal lived in — `TaskModal`'s existing `saveApi` prop and a new `listFn` prop on `components/TaskPicker.jsx` (defaults to `tasksApi.list`, override-able) thread it through; two small local wrapper components, `TaskLinkPicker`/`PoolAwareTaskCreateModal` (both inside `GoalModal.jsx`), resolve the async pool-client import before rendering `TaskPicker`/`TaskModal`, since both need a real function reference up front, not a promise. New `module_packages/goals/frontend/GoalPicker.jsx` (123 lines) — search-autocomplete for linking an EXISTING goal as a subgoal (re-parenting), mirrors `TaskPicker.jsx`'s own shape; its own `subtreeIds()` is a client-side port of the backend's `collect_subtree_ids` cycle-guard so the picker excludes a goal and its own descendants (re-parenting under your own descendant 400s server-side); shows a match's current parent title inline ("— currently under 'X'") since re-parenting an already-parented goal is explicitly allowed. No backend change needed — `goals_service.update_goal`'s existing cycle guard already covers it, this was a missing frontend affordance only. **Tab restructure**: `Goals.jsx`'s old root-level-only "ME" tab is gone — "ME" now shows all the caller's own personal goals at any depth (what the old flat "Goals" tab showed); a second tab, labeled "Household"/"Team" per `useWorkspace()`, shows that pool's goals (previously merged invisibly into one list, identifiable only by an unwatched `_owner` field) — hidden entirely via `useAuth()`'s `activeModuleIds`/`user.disabledModules` (the same check `App.jsx`'s `ModuleRoute` uses) when the pool module isn't installed/enabled, not when the viewer merely lacks `pool_edit`. Category-grouping headers and the tab switcher's descriptive text were both removed from `Goals.jsx` (each goal already shows its own category badge). **New core `services/tags_service.py`** (44 lines) and **`routers/tags.py`** (30 lines, see routers/ entry above) — a real persisted, growing tag vocabulary per store (`Tags/tags.json`), case-preserving/case-insensitive-dedup `register_tags()`, deliberately core (not `module_packages/goals/`) since both Goals and core `task_service.py` need it; wired into `task_service.add_task/update_task` and `goals_service.create_goal/update_goal`. Frontend reuses `components/TagInput.jsx` as-is in `GoalModal.jsx`'s form and (new) `components/TaskModal.jsx`'s form; tag PILL display + click-to-filter added to `GoalModal.jsx`'s view mode, `Goals.jsx`'s `GoalCard`, and `module_packages/tasks/frontend/Tasks.jsx`'s `TaskCard`. **Recurring-task completion log**: `task_service.py` gained `completion_log` (capped `_COMPLETION_LOG_CAP = 90`, appended in `update_task()`'s existing recurring-completion branch alongside `streak_count`/`last_completed_date`, symmetric removal on same-day undo); `goals_service.compute_progress`'s rollup loop now calls a new `_recurring_completion_rate(task)` (trailing-30-day count ÷ 30, `_COMPLETION_RATE_WINDOW_DAYS = 30`, fixed not configurable) for a linked task whose `type == "recurring"`, instead of the old binary done-check — a non-recurring linked task is unaffected. `goals_service.py`'s `_resolve_metric`'s manual branch now calls `module_registry.directional_pct` instead of its own inline `current*100/target` (see that function's own entry above). Incidental dead-code cleanup found while touching `Tasks.jsx`: `allTasks`'s leftover `.filter(t => t.type !== 'goal')` (a permanent no-op since Round 1 removed `type=="goal"` from Task entirely) removed, along with its stale comment. New test files: `tests/test_tags_service.py` (6), `tests/test_tags_router.py` (4), `module_packages/contacts/tests/test_contacts_metric_providers.py` (4, covers `_resolve_weight` + confirms `_resolve_number_field` still works with the new `direction` param); `tests/test_module_registry.py` grew to 15 (7 new `directional_pct` tests); `tests/test_task_service.py` grew to 20; `module_packages/goals/tests/test_goals_service.py` grew to 30. Full backend suite: 1112 passed (up from 1079, +33 net). Still not verified: real browser click-through — this round changed enough visible UI (tab restructure, tag pills, the reparent picker) that it deserves real verification before the owner trusts it fully. See `docs/MEMORY.md`'s 2026-08-29 entry for the full writeup, including the `directional_pct` bug-caught-before-shipping story. **Milestone: `features_service.py`'s `_CORE_MODULE_IDS` — `["goals"]` after Finance's own conversion — is now `[]`, completely empty**, mirrored by `lib/constants.js`'s `CORE_MODULES` and `lib/deepLinks.js`'s `_CORE_MODULE_ROUTES`/`_CORE_RECORD_PARAM` (both now `{}`) — see each file's own entry below and `docs/MEMORY.md`'s 2026-08-28 entry (Goals) for the full writeup. Full backend suite: 1079 passed (up from 1030, +49 net, not a clean addition — see that same entry). **Round 3 (2026-08-29, same still-uncommitted changeset, 2 more owner-reported issues):** Task gained `counts_toward_goal: bool | None` (`routers/_task_models.py`) — a linked recurring task's rollup contribution is now OPTIONAL per link rather than automatic, owner-chosen opt-in-off-by-default for newly-linked recurring tasks (see `services/task_service.py`'s own entry above for the default logic in `add_task`/`update_task`). `compute_progress`'s rollup loop only appends a recurring task's rate to `children_pct` when `task.get("counts_toward_goal", True)` is true — a MISSING key (every task linked before this shipped) still defaults `True`, so no existing goal's already-computed percentage moves on upgrade. `_recurring_completion_rate` renamed to `recurring_completion_rate` (dropped its leading underscore) since `router.py`'s `GET /goals/{id}` now calls it too — each recurring linked task in that response's `linked_tasks` gets a `recurring_rate` field (0-100, rounded) regardless of whether it currently counts, so the frontend can show a recurring task's own rate even when excluded from the rollup. `TaskPicker.jsx`'s own long-standing hidden result cap — the actual root cause of the owner's separately-reported "recurring tasks not selectable" — is documented in that file's own entry below, found only via live reproduction in an isolated test instance, not static reading. `module_packages/goals/tests/test_goals_service.py` grew to 32 (+2), core `tests/test_task_service.py` grew to 26 (+6). Full backend suite: 1120 passed (up from 1112, +8 net). See `docs/MEMORY.md`'s 2026-08-29 Round 3 entry for the full writeup
│   │   ├── demo_reset.py         → nightly non-admin Brain-folder + auth-entry wipe for a public demo instance; refuses to run unless DEMO_MODE=true (safety gate — never touches a personal/managed instance); wired via docker/demo_reset.sh on the demo host's cron, not this repo's
│   │   ├── routers/
│   │   │   ├── _task_models.py   → shared Pydantic models for tasks (CreateTaskRequest, UpdateTaskRequest); stayed core when tasks/ converted (2026-08-25) — module_packages/household/backend/router.py and module_packages/team/backend/router.py both import it directly for their own pool tasks
│   │   │   ├── _event_models.py  → shared Pydantic models for calendar events
│   │   │   ├── auth.py           → login, register, logout, /me, admin user management, AI/search/hosting settings, infisical + feature flags; also hosts GET/POST /admin/automation-token[/rotate] (2026-08-27, relocated here from assets.py when assets/ converted — view/rotate the shared n8n automation token; require_admin-gated, not require_module, since Contacts' own automation API depends on the same token regardless of Assets' install state — automations_config.py itself stays core, unowned by either module)
│   │   │   ├── (tasks.py moved → module_packages/tasks/backend/router.py, 2026-08-25 — task CRUD, top3, scored, history; still mounted at /api/v1/tasks, id/every internal name deliberately left unchanged — no rename this time. Tasks is the first LOCKED (uninstallable=True) module conversion)
│   │   │   ├── (chat.py moved → module_packages/chat/backend/router.py, 2026-08-26 — the second LOCKED module after tasks. AI chat with plan/auto/research modes, Brain context injection, tool use, chat save/load; conversations are workspace-scoped sessions keyed by a required `chat_id` — `POST /chat` marks the session "running" before calling `run_agent`, resets to "idle" on an unhandled exception, and calls `notify_user()` on completion; `GET /chat/sessions` (list, for the Chats drawer) + `POST /chat/sessions/{chat_id}/read` (clear unread) read/write agent_service.py's session index; still mounted at /api/v1/chat, no rename. Fixed mid-conversion: `GET /chat/runs`/`GET /chat/runs/{run_id}` were the only 2 of 11 endpoints using plain `get_current_user` instead of `_require_chat` — a real, pre-existing gap (a user with chat disabled via role could still read past agent run history) — now gated like every other endpoint here. `services/agent_service.py` — the whole AI orchestration engine — deliberately never converts, same category as `dashboard_blocks/registry.py`: imported by every module (this one included), owned by none; confirmed by direct read that it has zero router imports, zero chat-HTTP assumptions, and zero module-gating logic of its own before this conversion started)
│   │   │   ├── brain.py          → Brain file read/write, workspace-scoped via X-Workspace (fixed 2026-08-22 — previously always resolved personal regardless of header); path-validated, self-service (any user edits their own `.md` files only — the older "admin-only writes" description here was stale); `Business/` rejected as a path segment since it's the business workspace's own nested base folder on disk, not an ordinary personal subfolder. `_ALWAYS_SKIP = {"Tasks", "Dashboards", "Assets", "Business"}` (unconditional, structural) unioned with `brain_paths_for_disabled()`'s per-user-disabled union (conditional, per module) — `"Chats"` joined the CONDITIONAL half 2026-08-26 when chat/ converted and declared `owned_brain_paths=["Chats"]`, closing a real, previously-open gap: chat archives are ordinary per-user markdown with zero protection before this (unlike Tasks, a structural JSON exception) — a disabled user's own past conversations were fully readable via this browser and the AI's own list_brain_files/read_brain_file/search_brain tools regardless of chat's module state. `"Dashboards"` joined the UNCONDITIONAL half 2026-08-27 when dashboard/ converted — same structural category as Tasks (JSON, not markdown), so it's skipped regardless of module state, not conditional on being disabled; `owned_brain_paths=["Dashboards"]` is also declared on the manifest for honesty (redundant with the unconditional skip, matching Tasks' own precedent exactly). `"Assets"` joined the same UNCONDITIONAL half the same day when assets/ converted — assets.json/templates.json are JSON and attachments are binary files under Assets/files/, no markdown exists to protect, same no-op-but-honest reasoning as Dashboards'/Tasks' own entries; `owned_brain_paths=["Assets"]` likewise declared on its manifest. `"Contacts"` joined the same UNCONDITIONAL half 2026-08-28 when contacts/ converted — contacts.json/interactions.json/deals.json are JSON and photos are binary files under Contacts/photos/, no markdown exists to protect, same reasoning; `owned_brain_paths=["Contacts"]` likewise declared. This conversion also fixed a stale docstring on `_skip_dirs()` here — it used to cite "Notes, Contacts, etc." as an example of the DUAL-ACCESSIBLE (conditional, per-user-disabled) category, which became wrong the moment Contacts moved to the unconditional category; corrected to just "Notes, etc." with Contacts added to the Tasks/Dashboards/Assets unconditional-exception list instead. `"Finance"` joined the same UNCONDITIONAL half the same day when finance/ converted, same JSON-only reasoning; `owned_brain_paths=["Finance"]` likewise declared. `"Goals"` joined the same UNCONDITIONAL half 2026-08-28, the same day but NOT as part of that rollout — `Goals/goals.json` (+ `goal_progress_history.json`) are JSON, no markdown exists to protect; `owned_brain_paths=["Goals"]` likewise declared, and `agent_service.py`'s own `_brain_skip()` hardcoded set is now `{"Tasks", "Dashboards", "Assets", "Contacts", "Finance", "Goals"}`
│   │   │   ├── (notes.py moved → module_packages/notes/backend/router.py, 2026-08-26 — file/folder CRUD, move, archive, and the sharing endpoints (access/respond/leave); still mounted at /api/v1/notes, id/every internal name deliberately left unchanged — no rename this time. The first conversion to genuinely test the sidecar-share-index sharing pattern (Notes/_shares.json + notes_index.py) — the direct precedent Assets/Contacts/Finance will need)
│   │   │   ├── (calendar.py moved → module_packages/calendar/backend/router.py, 2026-08-25 — tasks view + events CRUD; still mounted at /api/v1/calendar, id/every internal name deliberately left unchanged — no rename this time, unlike home_assistant/automations)
│   │   │   ├── priorities.py     → priority order + daily override
│   │   │   ├── tags.py           → (2026-08-29) `GET /tags?pool=` — the caller's own tag vocabulary, or their workspace's pool vocabulary when `pool=true`; login-required, no module gate, mirrors this file's own shape exactly (both Goals and Tasks read from it, so it can't be owned by either module); thin wrapper over `services/tags_service.py`
│   │   │   ├── setup.py          → first-time setup wizard; after the template copytree, calls `on_new_user(brain, name)` for every currently-active module_packages/ module (journal's fills in a fresh Journal/ folder — 2026-08-24, replaces the old unconditional template copy)
│   │   │   ├── health.py         → GET /health (no auth, used by Docker healthcheck)
│   │   │   ├── export.py         → brain zip download (mounted at /api/v1/user)
│   │   │   ├── (shared.py moved → module_packages/household/backend/router.py, 2026-08-25 — household pool: tasks at /shared/tasks, events at /shared/events (admin write); still mounted at /api/v1/shared, tags=["shared"] — the historical prefix, unrelated to the module id "household"; deliberately left unrenamed, no rename requested)
│   │   │   ├── (team.py moved → module_packages/team/backend/router.py, 2026-08-25 — business team pool: tasks at /team/tasks, events at /team/events; own _team pseudo-user; separate from household; still mounted at /api/v1/team)
│   │   │   ├── push.py           → web push subscriptions (VAPID), subscribe/unsubscribe/test
│   │   │   ├── suggestions.py    → proactive AI suggestion engine + per-user custom schedules + notification inbox (dead `_require_chat = require_module("chat")` assignment — never actually used anywhere in the file — removed 2026-08-26 during chat/'s conversion, along with the now-unneeded `require_module` import)
│   │   │   ├── infisical.py      → Infisical secrets manager integration (admin only; status, token set/clear)
│   │   │   ├── features.py       → feature flags + custom role management (admin only)
│   │   │   ├── (automations.py moved → module_packages/automations/backend/router.py, 2026-08-25 — import/run/logs n8n workflows (personal + business scopes) + n8n admin config + Automation Inbox; still mounted at /api/v1/automations, id/every internal name deliberately left unchanged — only the display name "n8n Automation" is new)
│   │   │   ├── (assets.py moved → module_packages/assets/backend/router.py, 2026-08-27 — templates (admin), asset tree CRUD, shares/hidden_from, pool convert, attachments, n8n automation API (X-Automation-Token); still mounted at /api/v1/assets, no rename. Two endpoints — GET/POST /automation/token[/rotate] — did NOT move here; gated only by require_admin, not require_module, they relocated to routers/auth.py's admin section instead (GET/POST /auth/admin/automation-token[/rotate]), since living inside this optional module's router meant uninstalling Assets would have silently taken away the admin's only way to manage a token Contacts' own automation API still depends on — see that entry below and docs/MEMORY.md)
│   │   │   ├── (finance.py moved → module_packages/finance/backend/router.py, 2026-08-28 — finance module: books/accounts/categories/transactions CRUD, monthly report, net worth (access via finance_service._resolve_book_access); GET/PUT /finance/prefs — workspace-keyed last-opened-book pointer (finance_service.get_last_book_id/set_last_book_id, file_service.finance_prefs_path), consulted by Finance.jsx only when no `?book=` override is present; still mounted at /api/v1/finance, no rename. This is one of SIX finance router files — see the module_packages/ entry above for how manifest.py composes all six into one router since ModuleManifest.get_router() only supports returning one)
│   │   │   ├── (finance_banking.py moved → module_packages/finance/backend/router_banking.py, 2026-08-28 — SimpleFIN connections (member request + mapping; ADMIN claim/reveal/disconnect/sync), CSV import (preview/commit), payee rules. 13 admin-lifecycle SimpleFIN endpoints gained `require_module("finance")` alongside their pre-existing `require_admin` during this conversion — see docs/MEMORY.md)
│   │   │   ├── (finance_planning.py moved → module_packages/finance/backend/router_planning.py, 2026-08-28 — budgets (+status), recurring bills (+upcoming), planned one-offs, balance projection endpoints)
│   │   │   ├── (finance_invoicing.py moved → module_packages/finance/backend/router_invoicing.py, 2026-08-28 — clients CRUD + AR rollup, invoices CRUD, partial payments (w/ linked income tx))
│   │   │   ├── (finance_sharing.py moved → module_packages/finance/backend/router_sharing.py, 2026-08-28 — book/account audience (shares + contributors + hidden_from), share handshake respond, leave, member/role pickers)
│   │   │   ├── (finance_transfers.py moved → module_packages/finance/backend/router_transfers.py, 2026-08-28 — cross-book/cross-workspace Transfer type: POST creates both linked legs (same-currency + edit-access-on-both required), PATCH/DELETE update or remove a pair together; imports _find_or_404/_require_edit from module_packages.finance.backend.router now, same sibling-router pattern as the other finance router files, import path updated only)
│   │   │   ├── (contacts.py moved → module_packages/contacts/backend/router.py, 2026-08-28 — Contacts (CRM): contacts/interactions/deals CRUD, pipeline, admin custom fields, sharing handshake, CSV import/export, contact money view, write-focused n8n automation API, self-contact (GET/PATCH /contacts/me — login-gated only, not the contacts module), affiliation link/unlink; still mounted at /api/v1/contacts, no rename. `PUT /contacts/fields` gained `require_module("contacts")` alongside its pre-existing `require_admin` — a real, narrow gap where an admin with Contacts disabled for their own account could still edit its field schema, found during this conversion's own research; `GET /contacts/available-for-linking` deliberately stayed ungated by `require_module`, matching `/contacts/me`'s own always-available precedent — see docs/MEMORY.md)
│   │   │   ├── (home.py moved → module_packages/home_assistant/backend/router.py, 2026-08-24 — entity control, scenes, automations, favourites, admin config; mounted at /api/v1/home_assistant since the id/prefix rename later that same day, see docs/MEMORY.md)
│   │   │   ├── help.py            → Help system: GET /help/content (authored guide), /help/whats-new (banner state), GET/PUT /help/onboarding (first-run checklist); auth required, NO module gate (like Settings)
│   │   │   ├── update.py         → update status check + trigger (admin only); works with update.sh on host
│   │   │   ├── ai_usage.py       → AI usage metering + caps: GET /overview, /users, /defaults (admin), PATCH /defaults, /users/{id}/limits (admin), GET /me (any user, for the Chat toolbar indicator)
│   │   │   ├── (dashboards.py moved → module_packages/dashboard/backend/router.py, 2026-08-27 — the third LOCKED module after Tasks and Chat; still mounted at /api/v1/dashboards, id stays "dashboard" singular — no rename, matching Automations'/Calendar's own no-rename precedent, since the existing id predates this package and a plural rename would need its own carry-forward migration for every stored disabled_modules/role entry, the same weight as Home Assistant's home→home_assistant move) → Custom Dashboards: standalone dashboard CRUD/access/render, sharing handshake, catalog, cross-module reference lookup, members/roles (share picker support); also hosts Dashboard Templates CRUD (`/templates*`, declared before `/{dashboard_id}` — same route-ordering rule as assets.py) and `/{id}/subject` + `/{id}/detach-template` for templated-dashboard instances; `get_render`'s `_resolve_subject()` resolves a dashboard's `subject_type`/`subject_id` into a small `{type, id, name, icon?}` summary for the frontend hero header — never raises, just omits `subject` if the viewer's lost access. `services/dashboards_service.py`/`dashboard_templates_service.py`/`dashboard_index.py` all stay core — `migrations/runner.py`'s own core migrations (m010/m011/m012) call `dashboards_service` directly, and migrations run BEFORE module registration in boot order, so a module-package service literally can't be imported that early; `module_packages/household/manifest.py`'s m023 and `module_packages/home_assistant/manifest.py`'s m018 also import `dashboards_service._all_stores()` directly for their own carry-forward migrations
│   │   │   ├── presence.py       → app-wide online/offline presence: `POST /presence/ping` records the caller's own last-seen (rate-limited); deliberately no lookup-by-username endpoint — presence is only ever meant to surface embedded in an already access-controlled contact read, never queried directly
│   │   │   └── mod_store.py      → Mod Store: GET /catalog (admin), GET /installed + GET /active (any user — the latter reads app.state.active_module_ids, distinct from /installed since a fresh install needs a restart before its router is actually live), POST /install|uninstall/{id} (marker-flip only, no restart), POST /restart (admin-triggered only, warns via 409 if other users appear online unless force=true, then restarts the app's own container via the same socket-proxy Docker mechanism n8n_service.py uses)
│   │   ├── services/
│   │   │   ├── file_service.py        → atomic Brain file reads/writes — ALWAYS use this, never open(...,'w')
│   │   │   ├── auth_service.py        → user CRUD, JWT create/verify, bcrypt, JTI revocation
│   │   │   ├── ai_provider.py         → AI abstraction layer (Anthropic + OpenAI-compatible; sync/async bridge)
│   │   │   ├── agent_service.py       → multi-tool AI agent orchestration (plan/auto/research modes, tool registry); load_sessions/get_session/upsert_session/mark_session_read/delete_session_by_filename manage a capped (_SESSIONS_CAP=50), workspace-scoped chat_sessions.json index (status idle/running/awaiting_approval/awaiting_answer + unread + preview) — workspace-scoped (not user_path) because chat archives live under ws_path(...)/Chats/. 9 personal-scope task tools (list_tasks/add_task/update_task/delete_task/get_top3_tasks/get_scored_tasks/get_task_history/create_tasks/get_week_snapshot) moved to `module_packages/tasks/backend/agent_tools.py` when tasks/ converted (2026-08-25); the 5 household-pool tools (list_shared_tasks/add_shared_task/update_shared_task/delete_shared_task/complete_shared_task) similarly moved to `module_packages/household/backend/agent_tools.py` when household/ converted the same day — 4 of the 5 (all but complete_shared_task, member-accessible) are also the FIRST tools anywhere to use the new `admin_agent_tools` manifest field (`module_registry.py`'s `admin_agent_tool_names()`), since `_get_tools()` previously offered every module's `owned_agent_tools` schema to any module-enabled user regardless of role — fine for every prior module (none owned an admin-only tool), wrong for household's shared-task management tools once they became module-owned instead of hardcoded in `_ADMIN_TOOLS`. Stayed 100% core when chat/ converted (2026-08-26, the router that actually calls run_agent/the session-index helpers/pending-turn helpers) — confirmed by direct read: zero imports from any router, zero chat-HTTP-shape assumptions, zero module-gating logic of its own anywhere in the file; module_registry.py's own register_routers() docstring had already named Chat alongside Tasks/Dashboards as "deeply load-bearing" before this conversion existed, independent confirmation the split was already correctly anticipated. 7 note tools (list_notes/read_note/create_note/update_note/delete_note/move_note/create_note_folder) moved to `module_packages/notes/backend/agent_tools.py` when notes/ converted (2026-08-26) — search_brain stays here, it isn't a notes tool (also rglobs journal/memory/profile files). Notes is the first module whose tools genuinely needed the caller's active workspace, which `_module_tool_dispatch()`'s call site never threaded through before (`execute_fn(name, inputs, user)`, 3 args) — widened to `execute_fn(name, inputs, user, workspace)`, with every other module owning an `agent_tools.py` (household/tasks/journal/home_assistant) picking up the new parameter (default `"personal"`, unused) for signature parity. Tasks' own tools remain documented as workspace-blind regardless (its own agent_tools.py docstring already said so before this widening — out of scope to fix retroactively as part of notes' own conversion). `search_brain`'s own two-halves gap (found same-day, answering the owner's own "does uninstalling notes affect other modules" question) — its rglob-over-own-files half already respected `_brain_skip()` (Notes included once the manifest declared `owned_brain_paths`), but its second half (shared/pool notes, reached directly via `notes_service.list_visible_notes()`) bypassed that gate entirely — fixed by skipping that loop too when `"Notes" in skip`, same skip-set both halves now share. 10 dashboard tools (list_dashboards/get_dashboard/list_dashboard_templates/get_dashboard_block_catalog/create_dashboard/add_dashboard_block/update_dashboard_block/remove_dashboard_block/create_dashboard_template/update_dashboard_template) plus the layout-clamping helpers (`_LG_COLS`/`_MAX_ROWS`/`_MAX_Y`/`_block_layout_for_agent`/`_clamp_block_layout_override`/`_apply_layout_override`) moved to `module_packages/dashboard/backend/agent_tools.py` when dashboard/ converted (2026-08-27) — all 10 were already correctly workspace-threaded inline before the move (notes' own dispatch-widening the day before already covers this, no further mechanism change needed here, just the relocation). 10 asset tools (list_asset_templates/list_assets/create_asset/update_asset/archive_asset/search_assets/move_asset/delete_asset/create_asset_template/update_asset_template) moved to `module_packages/assets/backend/agent_tools.py` when assets/ converted (2026-08-27), the largest single tool set moved by any conversion so far — `_RESEARCH_TOOLS` lost the 3 read-only ones (now supplied dynamically via `read_only_agent_tool_names()`), and `_brain_skip()`'s hardcoded set grew from `{"Tasks", "Dashboards"}` to `{"Tasks", "Dashboards", "Assets"}` (same unconditional/structural reasoning as `routers/brain.py`'s own `_ALWAYS_SKIP` update, see above — a no-op today since Assets has no markdown to protect, declared for honesty). 6 contact tools (list_contacts/get_contact/create_contact/update_contact/log_interaction/create_deal) plus their 6 case branches moved to `module_packages/contacts/backend/agent_tools.py` when contacts/ converted (2026-08-28) — `_RESEARCH_TOOLS` lost `list_contacts`/`get_contact`, `_brain_skip()`'s hardcoded set grew to `{"Tasks", "Dashboards", "Assets", "Contacts"}`. `get_profile`/`update_profile` deliberately stayed here in core, untouched — Profile is a generic concept independent of Contacts' module state (both tools call `contacts_service` internally regardless), confirmed still visible to a user with Contacts disabled by `test_contacts_module_conversion.py`
│   │   │   ├── task_service.py        → task business logic (CRUD, pagination, type handling); deliberately stayed in core when tasks/ converted (2026-08-25) rather than moving to module_packages/tasks/backend/service.py — module_packages/household/backend/router.py and module_packages/team/backend/router.py both import it directly for their own pool tasks (against the "_household"/"_team" pseudo-users, unaffected import-wise by household/team's own 2026-08-25 conversion since task_service.py never moved), and agent_service.py/suggestions_service.py/dashboard_blocks/_actions.py all depend on it directly too (dashboard_blocks/_assets.py also did, until it moved to module_packages/assets/backend/dashboard_block.py 2026-08-27 — still imports task_service directly from its new location, an ordinary core-to-core import unaffected by assets/'s own conversion), none of task_service.py itself ever converting; same "real external consumers keep a service in core" pattern as ha_service.py/n8n_service.py/events_service.py. Gained a new `goal_id: str | None` field on Task (2026-08-28, mirrors the existing `asset_id` field exactly, part of Goals becoming a real module — see module_packages/ entry above); `cleanup_done_goals()` (the old Goals "Clear completed" mechanism) deleted outright the same day, since `type=="goal"` was removed from Task's Literal entirely and Goals now owns that concept over real Goal records instead. Gained `completion_log` (2026-08-29, capped list of `{"date"}` entries, `_COMPLETION_LOG_CAP = 90`) and `tags` (2026-08-29) — the former appended in `update_task()`'s existing recurring-completion branch alongside `streak_count`/`last_completed_date`, feeding `goals_service.compute_progress`'s new recurring-task rollup; the latter union-registered into `services/tags_service.py`'s per-store vocabulary via `register_tags()` whenever `add_task`/`update_task` sees a tag not yet known — see module_packages/ entry above (Goals, Round 2). Gained `counts_toward_goal: bool | None` (2026-08-29, Round 3) — `add_task()` defaults it `False` when a task is created already-linked (recurring + `goal_id` set at creation) with no explicit value given; `update_task()` defaults it `False` when an update newly sets a different `goal_id` on a recurring task with no explicit value in that same payload — a single choke point covering the frontend's link/create flows AND the AI's `link_task_to_goal` tool with zero changes needed to the latter. A missing key (every task linked before this field existed) is read as `True` (still counts) by `goals_service.compute_progress`'s rollup loop — see module_packages/ entry above (Goals, Round 3)
│   │   │   ├── events_service.py      → calendar event CRUD; deliberately stayed in core when calendar/ converted (2026-08-25) rather than moving to module_packages/calendar/backend/service.py — module_packages/household/backend/router.py and module_packages/team/backend/router.py both import it directly for their own pool calendars, confirmed independent of the calendar module's own install state (they gate on require_module("household")/("team"), never require_module("calendar")); routers/_event_models.py (EventCreate/EventUpdate) stayed core for the same reason
│   │   │   ├── notes_service.py       → notes CRUD, folder management, move; + sharing (sidecar Notes/_shares.json, folder-cascade resolve_access, pool notes, handshake, list_visible_notes/find_note_store — find_note_store() takes an optional owner hint to disambiguate a path that could exist in more than one reachable store); meets(access, need) access-level helper, shared by module_packages/notes/backend/router.py and agent_service.py's search_brain. Deliberately stayed core when notes/ converted (2026-08-26) rather than moving to module_packages/notes/backend/service.py — services/user_deletion_service.py imports it (and notes_index.py) directly, alongside the equivalent assets/contacts/finance pairs, and main.py's _warm_share_index() unconditionally rebuilds notes_index at every boot the same way it does for those three; same "real external consumers keep a service in core" pattern as task_service.py/events_service.py
│   │   │   ├── profile_service.py     → pool (_household/_team) priority order only — real users' profile data lives on their self-contact (contacts_service.py) since the Profile/Contacts merge
│   │   │   ├── priority_service.py    → life priority scoring formula + top3 logic; stayed core when tasks/ converted (2026-08-25) — suggestions_service.py's daily_digest suggestion imports `get_top3` directly (a real, separate consumer, corrected here after initially being undersold as tasks-only), plus tasks' own moved agent tools/dashboard blocks import it back from core the same direction task_service.py's other consumers use, and lib/constants.js's scoreTask() is a JS port of its formula. `_pending_non_goal()` simplified to a plain `_pending()` (2026-08-28) — its goal-exclusion filter became dead code once `type=="goal"` became impossible on a Task at all, part of Goals becoming a real module
│   │   │   ├── tags_service.py        → (2026-08-29) shared tag vocabulary for Goals + Tasks — `get_tags(store_user, workspace)`/`register_tags(store_user, workspace, tags)`, one real persisted, growing `Tags/tags.json` per store (personal or pool), not live-scanned from existing records; case-preserving but case-insensitive-deduped (`_VOCAB_CAP = 500`). Deliberately core, not inside `module_packages/goals/` — both Goals' own package and core `task_service.py` need it, the same "real external consumer keeps it core" test this project has used throughout. `routers/tags.py` is its thin HTTP wrapper (see routers/ entry above)
│   │   │   ├── hosting_service.py     → runtime hosting config (reads brain/hosting.json at request time)
│   │   │   ├── rate_limiter.py        → IP-based rate limiting (respects trust_proxy_headers)
│   │   │   ├── recurring_service.py   → recurring task date advancement + streak logic; stayed core when tasks/ converted (2026-08-25) — scheduler.py's nightly recurring-processor job depends on it directly, never converting itself. Its `t.get("type") in ("recurring", "goal")` check simplified to just `"recurring"` (2026-08-28) once `type=="goal"` became impossible on a Task, part of Goals becoming a real module
│   │   │   ├── notification_service.py → ntfy push notification delivery
│   │   │   ├── push_service.py        → web push subscription management + VAPID send; `_build_vapid_jwt` only prepends `mailto:` to `VAPID_SUBJECT` if it isn't already a `mailto:`/`https:` URI (some push services reject a `sub` claim that isn't a real URI)
│   │   │   ├── suggestions_service.py → proactive suggestion generation + custom schedule management
│   │   │   ├── web_search_service.py  → Tavily API web search (for chat research mode)
│   │   │   ├── infisical_loader.py    → Infisical secrets pull on startup; token validation + file storage
│   │   │   ├── features_service.py    → feature flags + role resolution (get_effective_disabled). **`_CORE_MODULE_IDS` is now `[]`, completely empty** (2026-08-28 — `"finance"` was removed earlier the same day when finance/ converted, leaving just `["goals"]`; then `"goals"` itself was removed too, the same day, when Goals became a real `module_packages/goals/` entry) — there is no longer any hardcoded-core module id anywhere in the app; every module is now either a converted `module_packages/` entry or permanent cross-cutting infrastructure the rollout plan never intended to convert, see `docs/MEMORY.md`'s 2026-08-28 entries (Finance, then Goals)
│   │   │   ├── assets_service.py      → assets core: templates, field validation, tree ops, per-node archive (+cascade), share/hidden resolution, pool conversion, history, attachments; `asset_links_contact(asset, template, contact_id)` — "does this asset reference this contact in any contact-type field" — shared by `list_assets_for_contact` and the dashboard Collection block resolver, one implementation not two. Deliberately stayed core when assets/ converted (2026-08-27) rather than moving to module_packages/assets/backend/service.py — the strongest "stays core" case of any conversion yet: on top of the usual `user_deletion_service.py`/`dashboard_blocks/_actions.py`/`module_packages/contacts/backend/router.py`'s (moved 2026-08-28) link_deal_asset-unlink_deal_asset/`main.py`'s `_warm_share_index()` consumers, `module_packages/dashboard/backend/router.py` — an already-converted SIBLING module package, not just another core router — imports it directly for its own Dashboard Hero subject resolver, independent confirmation from a second module package that this has to stay shared infrastructure
│   │   │   ├── assets_index.py         → derived share-routing cache (_system/assets_share_index.json); rebuildable, warmed at startup; sharers_for()/reindex_owner()/rebuild_share_index(). Same "stays core" reasoning as assets_service.py above — main.py's _warm_share_index() unconditionally rebuilds it at every boot alongside finance/contacts/notes' own indexes
│   │   │   │   # Phase 2: per-user templates in USERS/{name}/Assets/templates.json (global in _system/asset_templates.json); assets ref template_id; request-based sharing (accepted[]) + accept/decline notifications
│   │   │   ├── finance_service.py     → finance core: books/accounts/categories/transactions (per-book per-year shards), computed balances, _resolve_book_access (single access gate); transactions carry an optional transfer_pair_id + denormalized transfer_peer_* fields (book/account/workspace) for Transfer legs; get_transaction_by_transfer_pair() looks up a leg's counterpart. Deliberately stayed core when finance/ converted (2026-08-28) rather than moving to module_packages/finance/backend/service.py — the strongest "stays core" case of any conversion yet: module_packages/contacts/backend/router.py's `GET /contacts/{id}/finance` endpoint makes deep, multi-function calls into this file directly, on top of the usual user_deletion_service.py/dashboard_blocks/_actions.py/main.py's _warm_share_index() consumers
│   │   │   ├── (finance_reports.py moved → module_packages/finance/backend/reports.py, 2026-08-28 — finance reports computed on read: monthly income/expense by category, net worth; confirmed by direct grep to have zero consumers outside Finance's own routers/services once its `month_end()` helper relocated into finance_planning_service.py, see below — imports it back from there now)
│   │   │   ├── simplefin_service.py   → SimpleFIN bridge client: claim setup token → read-only access URL (per-user secret), account mapping, sync engine w/ dedup + error throttle. Deliberately stayed core when finance/ converted (2026-08-28) — a genuinely new "stays core" reason, not a sibling-module/core-router dependent: scheduler.py's own job_simplefin_sync job function imports it directly, unconditionally at boot, mirroring n8n_service.py's own scheduler-dependency precedent from automations' conversion
│   │   │   ├── (finance_import_service.py moved → module_packages/finance/backend/import_service.py, 2026-08-28 — CSV statement import: preview + column-mapped commit, import_hash dedup, Decimal→cents parsing; matches automations' own inbox_service.py precedent, a module-exclusive helper with no external dependents)
│   │   │   ├── finance_planning_service.py → budgets+alerts, recurring bills (matching/advance/missed), planned items, projection, deviation checks, nightly sweep; also now defines `month_end(year, month)` (relocated here from finance_reports.py 2026-08-28, when finance/ converted and reports.py moved — a two-line pure calendar helper, deliberately duplicated-then-relocated rather than left in the now-optional module package, so this scheduler-critical core file never depends on module_packages/finance/ even in principle). Deliberately stayed core when finance/ converted — scheduler.py's own job_finance_nightly job function imports it directly, unconditionally at boot, the same new "scheduler dependency = stays core" reasoning as simplefin_service.py above
│   │   │   ├── finance_invoice_service.py → clients (reserved contact_id for future CRM), invoices (derived totals/overdue, auto-numbering), payments, AR rollup. Deliberately stayed core when finance/ converted (2026-08-28) — same reasoning as finance_service.py above, module_packages/contacts/backend/router.py's `GET /contacts/{id}/finance` endpoint calls into this file directly too (list_clients/list_invoices/list_invoices_for_deal)
│   │   │   ├── finance_index.py       → derived share-routing cache (_system/finance_share_index.json); rebuildable, warmed at startup; sharers_for()/reindex_owner(). Same "stays core" reasoning as finance_service.py above — main.py's _warm_share_index() unconditionally rebuilds it at every boot alongside assets/contacts/notes' own indexes, unaffected by finance/'s 2026-08-28 conversion
│   │   │   ├── contacts_service.py     → Contacts core: contacts/interactions/deals, custom fields, pipeline, asset-style sharing (resolve_access read/contribute/edit), find_match dedup, follow-up reminders, self-contact (self_of marker, get/create_self_contact, cross-workspace resolution; type is guarded to stay "person" — a self-contact can't become a company), affiliated_contact_ids link/unlink, private-field stripping (_strip_private), format_profile_text() (AI chat context); company-type-only structured fields locations (capped list) + hours (always 7 entries, Mon–Sun) via _validate_locations()/_validate_hours(); custom field definitions carry `applies_to: ['person','company']` (_validate_applies_to, default both), fields_for_type(contact_type) filters a definition list by it — ContactModal.jsx/ContactDetail.jsx call this to only render matching-type fields. Deliberately stayed core when contacts/ converted (2026-08-28) rather than moving to module_packages/contacts/backend/service.py — the MOST-depended-on "stays core" service of any conversion yet: on top of the usual `user_deletion_service.py`/`main.py`'s `_warm_share_index()` consumers, `module_packages/dashboard/backend/router.py` AND `module_packages/chat/backend/router.py` — two already-converted SIBLING modules — both import it directly (Dashboard Hero's contact-subject resolver; Chat's own AI system-prompt profile context), plus `services/agent_service.py`'s core `get_profile`/`update_profile` tools, `services/profile_service.py`, `dashboard_blocks/_actions.py`, the new `dashboard_blocks/_custom_fields.py`, `finance_invoice_service.py`/`finance_service.py`, `routers/auth.py`/`routers/setup.py`, `migrations/runner.py`, and `scheduler.py`'s `run_followup_reminders` job
│   │   │   ├── contacts_index.py       → derived share-routing cache for Contacts (_system/contacts_share_index.json); warmed at startup. Same "stays core" reasoning as contacts_service.py above — main.py's _warm_share_index() unconditionally rebuilds it at every boot alongside assets/finance/notes' own indexes, unaffected by contacts/'s 2026-08-28 conversion
│   │   │   ├── notes_index.py          → derived share-routing cache for Notes (_system/notes_share_index.json); scans each store's Notes/_shares.json; warmed at startup
│   │   │   ├── automations_config.py  → instance automation API token (generate/rotate/verify) for n8n → LogCore writes. Deliberately stayed in core when automations/ converted (2026-08-25) — module_packages/contacts/backend/router.py (moved 2026-08-28) and module_packages/assets/backend/router.py (moved 2026-08-27) both depend on it directly (`verify_api_token`) for their own unrelated write-focused automation-token endpoints, not just n8n's. The admin-facing view/rotate endpoints (`get_api_token`/`rotate_api_token`) used to live inside assets.py, gated only by require_admin — relocated to routers/auth.py when assets/ converted, since living inside an optional module's router meant uninstalling Assets would have silently taken away the admin's only way to manage a token Contacts' own automation API still depends on; see routers/auth.py's own entry above and docs/MEMORY.md
│   │   │   ├── (automation_inbox_service.py moved → module_packages/automations/backend/inbox_service.py, 2026-08-25 — named inboxes (notify/reviewers/workflow routing), item dedup by (workflow_key, external_id), status lifecycle, trim, batched notifications)
│   │   │   ├── n8n_service.py         → n8n REST API client; import/execute/delete/activate workflows; bundled-container lifecycle (docker_sdk, same socket-proxy mechanism mod_store.py's own restart uses) + reconcile() auto-idle policy; write docker/n8n.env; sync_business_workflows() for auto-sync. Deliberately stayed in core when automations/ converted (2026-08-25) — scheduler.py's boot/cron jobs, infisical_loader.py's secrets pipeline, and dashboard_blocks/_actions.py all depend on it directly, same reasoning as ha_service.py staying core
│   │   │   ├── ha_service.py          → Home Assistant REST API client; config CRUD, entity states, service calls, scenes, automations, user favourites. Deliberately stayed in core when home_assistant/ converted (2026-08-24) rather than moving to module_packages/home_assistant/backend/service.py — it has consumers outside the module (Hosting.jsx's admin config form, agent_service.py's ha-configured gate), unlike journal_service.py which had exactly one consumer
│   │   │   ├── help_service.py        → Help content single source (loads content/help.json): get_content, as_text (markdown for the AI, incl. /help#id anchors), capabilities_index (enabled-modules index for chat context), onboarding state get/set
│   │   │   ├── whats_new_service.py    → on version bump, notify every user once (announce_if_updated at boot) + drive the few-day What's-New banner (get_banner); state in _system/whats_new_state.json
│   │   │   ├── update_service.py      → GitHub release check (cached 4h), pending_update flag trigger, update log reader
│   │   │   ├── ai_usage_service.py     → AI usage metering + caps: daily-bucketed usage in _system/ai_usage.json, derived daily/weekly/monthly totals, per-user hard/soft cap enforcement + warn/over notifications; the single place chat_completion/agent_completion record usage
│   │   │   ├── dashboards_service.py   → Custom Dashboards core: CRUD, resolve_access() (mirrors finance_service._resolve_book_access), floor-of-one delete protection, share_underlying_data owner-only setter, self-healing default resolution; also the Dashboard Templates instance side — `_sync_blocks_from_template`/`_sync_templated_dashboard` (self-healing sync-on-read, same shape as the default-resolution above: reconcile a templated dashboard's `blocks` against its template's current blocks, keeping each existing slot's own layout untouched — the entire "block set stays synced, layout stays independent" mechanism in one pure function), `_apply_layout_only` (server-enforced layout-only PATCH for templated dashboards), `set_subject`/`detach_template`, `_attach_template_labels` (denormalizes `_template_label`/`_template_icon` onto every listed dashboard for the grouped-navigation UI)
│   │   │   ├── dashboard_templates_service.py → Dashboard Templates CRUD, near-verbatim mirror of assets_service.py's template section (two-tier global/personal stores, `visible_templates`, personal-template sharing handshake, `template_reference_count`/delete guard). A template is `{label, icon, subject_type: contact|asset|null, blocks: [{id, type, config}]}` — no stored layout (a newly-synced instance slot gets an auto-stacked layout instead, computed in dashboards_service.py); `config` may use the literal sentinel `"$subject"` on a contact_id/asset_id field, substituted per-instance at render time (see dashboard_blocks/render.py)
│   │   │   ├── dashboard_index.py      → derived share-routing + reverse "referenced by" cache for Dashboards (_system/dashboard_index.json); mirrors assets_index.py, keyed off each block type's declared record_ref_fields (no per-block-type hardcoding)
│   │   │   ├── dashboard_blocks/       → block-type registry + resolver package: registry.py (BlockSpec/REGISTRY; also `scoped_target(ctx)` — extracted from tasks' own `_tasks.py` when tasks/ converted (2026-08-25) since it was never task-specific to begin with — imported by `_actions.py`/`_ai_usage.py` here in core, plus every converted module's own `dashboard_block.py` that owns a scope_configurable block, journal/home_assistant/automations/calendar/assets included; `_assets.py` was also a direct importer until it moved to `module_packages/assets/backend/dashboard_block.py` 2026-08-27, still importing it from its new location), render.py (the security-critical two-pass render_block — read-through-never-a-bypass + the one share_underlying_data exception; also `_resolve_subject_config` — the one central place a templated dashboard's `"$subject"` config sentinel gets substituted with that instance's own `subject_id` before any resolver runs, so no resolver below needs to know the sentinel exists), _custom_fields.py/_ai_usage.py/_freeform.py/_actions.py (one resolver module per source module family — `_finance.py` itself is gone too, moved wholesale to `module_packages/finance/backend/dashboard_block.py` when finance/ converted 2026-08-28 (registers `finance_activity`/`finance_book_report`, both gaining `module="finance"` gating for the first time — confirmed genuinely Finance-owned, neither resolver branches to another module's service, unlike Contacts' own `custom_fields`); `_contacts.py` itself is gone too, split when contacts/ converted 2026-08-28: 3 of its 4 blocks moved out to the module package, its 4th (`custom_fields`, spanning both Contacts and Assets, owned by neither) became this new core `_custom_fields.py` file, see below; journal's, home_assistant's, automations', calendar's, tasks', notes', and assets' own resolvers moved to `module_packages/journal/backend/dashboard_block.py`, `module_packages/home_assistant/backend/dashboard_block.py` (2026-08-24, directory renamed same day from `home/`), `module_packages/automations/backend/dashboard_block.py` (2026-08-25, registers both `workflow_status` and `inbox_summary` — the first converted module owning more than one block type), `module_packages/calendar/backend/dashboard_block.py` (2026-08-25, moved from `_calendar.py`, registers `upcoming_events` + `single_event`), `module_packages/tasks/backend/dashboard_block.py` (2026-08-25, moved from `_tasks.py`, registers `top3_tasks`/`due_today`/`streaks`/`goals_progress`/`single_task`), `module_packages/notes/backend/dashboard_block.py` (2026-08-26, moved from `_notes.py`, registers `note_embed` with `module="notes"` added — previously ungated, the exact pool_tasks-before-Household/Team situation), and `module_packages/assets/backend/dashboard_block.py` (2026-08-27, moved from `_assets.py`, registers `documents`/`linked_tasks`/`linked_contact`/`my_assets_summary` — plus `collection`, folded in from the separately-filed `_collections.py`, deleted outright rather than kept as a hypothetical-future-generalization exception since it's 100% Assets-data-dependent today and, like `note_embed` before it, had no `module=` gate at all; `_load_all_resolvers()`'s core-resolver import tuple lost both `_assets`/`_collections`, down to `(_actions, _ai_usage, _contacts, _finance, _freeform)`). `_pool.py` (the old unified `pool_tasks` block, unowned) deleted entirely 2026-08-25 — split into `module_packages/household/backend/dashboard_block.py` (`household_tasks`, `module="household"`, `workspace="personal"`) and `module_packages/team/backend/dashboard_block.py` (`team_tasks`, `module="team"`, `workspace="business"`) once both pools became real, discoverable modules — the same two files each gained a `household_goals`/`team_goals` block too (2026-08-28) once Goals itself became a real module, same `module=`-gates-on-pool-membership reasoning, still living in household's/team's own files rather than Goals' — `BlockSpec.module` can only ever hold one string, so a single block type serving both pools couldn't be gated to either individually; also closes a second, independent pre-existing bug (`resolve_pool_tasks` had no workspace-consistency check, so a viewer requesting the "wrong" pool's data via a stale/mixed-workspace dashboard got it with no gate). A carry-forward migration (household's own m023) rewrites any dashboard that already had a `pool_tasks` block to the correct new type based on THAT dashboard's own workspace — home_assistant's own m018 block-type-rename is the direct precedent this mirrors, just workspace-aware instead of a flat 1:1 rename. Core migration `m010_seed_home_dashboards` (migrations/runner.py) also had its own hardcoded `"pool_tasks"` seed value for new business-workspace users, fixed to `"team_tasks"` the same way m010's `home_favourites` hardcode was fixed during Home Assistant's own rename — now discovered dynamically rather than hardcoded here — see `_load_all_resolvers()`. _actions.py still imports `n8n_service`/`task_service` directly for block action buttons — both stayed core (see services/n8n_service.py/task_service.py above), so these are ordinary core-to-core imports, unaffected by either conversion; _actions.py is display-only — nav_button/status_button/generic block actions' actual writes go through each target module's own existing endpoint directly from the frontend, never through here; also exposes a small reusable per-module label/lookup helper, shared by the standalone Action blocks and by any opted-in block's generic `actions[]` resolution; `module_packages/contacts/backend/dashboard_block.py` (2026-08-28, moved from `_contacts.py`, registers `linked_deals`/`contacts_list`/`linked_assets` — each gaining a `module="contacts"` gate for the first time, none had one before; `_contacts.py`'s 4th block, `custom_fields`, did NOT move here — it reads from EITHER `contacts_service` OR `assets_service` depending on config, the same "spans more than one module, owned by none" shape as `_actions.py`'s own `nav_button`/`status_button` below, so it was extracted into a new core file `_custom_fields.py` instead and stays ungated; `_load_all_resolvers()`'s core-resolver import tuple changed from `(_actions, _ai_usage, _contacts, _finance, _freeform)` to `(_actions, _ai_usage, _custom_fields, _finance, _freeform)`, then to `(_actions, _ai_usage, _custom_fields, _freeform)` when finance/ itself converted 2026-08-28 and `_finance.py` moved out too — this is the FINAL core-resolver tuple, now that all 13 modules in the rollout have converted); `module_packages/assets/backend/dashboard_block.py`'s `resolve_collection` (moved here from the old core `_collections.py`, see above) is the generic one — source (asset template + optional contact-link filter) / view (list/kanban/count) / action (a status field write, same `assetsApi.update` call status_button already makes) instead of a new hand-coded resolver per use case), agent_schemas.py (`AGENT_CONFIG_SCHEMAS` — maintained Python mirror of `blockRegistry.js`'s `CONFIG_FIELD_SCHEMAS` for the chat agent's `get_dashboard_block_catalog` tool, flattening the one frontend-only composite picker kind into flat config keys; already had `top3_tasks`/`due_today`/`single_task` entries before tasks/ converted — unchanged by the move, since this mirror is content-addressed by block type, not by where the frontend schema physically lives)
│   │   │   ├── user_deletion_service.py → admin user-deletion orchestration: preview (owned+shared items across Assets/Finance/Contacts/Notes + read-only blast radius), completeness validation, execute (transfer/delete decisions → reference cleanup in every other store → index rebuild → batched new-owner notifications → account+Brain-folder delete, in that order)
│   │   │   ├── presence_service.py → app-wide online/offline tracking, generalized from agent_service.py's chat-only `record_chat_presence`/`is_chat_present`; one small per-user file (`presence.json`, not a shared multi-writer file), `record_presence()`/`is_online()` (90s staleness — a bit looser than chat's 45s, one ping interval not tied to a specific view)
│   │   │   └── mod_store_service.py → installed_modules.json (current state, fcntl-locked) + installed_modules_history.json (append-only, capped) + get_catalog() (merges content/mod_store_catalog.json with live discover_manifests()/install state — status precedence: error > available-once-code-present > whatever the static file says)
│   │   ├── content/
│   │   │   ├── help.json         → authored Help content (sections + FAQ + support + whats_new); SINGLE source read by the Help page, the ⓘ buttons, and the AI's get_help tool — help_service.get_content() also merges in every discovered module's own manifest.help_section
│   │   │   └── mod_store_catalog.json → first-party module catalog (id/name/description/icon/category/status); hand-authored, ships with core releases; a module with no matching module_packages/ entry stays "coming_soon"
│   │   ├── automations_stubs/    → committed stub files (*.stub.json) that drive business workflow auto-sync; each has name/key/tags only — no workflow logic ever committed here
│   │   ├── migrations/
│   │   │   └── runner.py         → runs pending Brain schema migrations at startup
│   │   └── tests/                → pytest suite (see Testing section in AGENTS.md); `test_full_upgrade_migration_chain.py` (2026-08-28) — end-to-end proof that a single `run_pending()` pass correctly installs all real modules on an upgrading instance, leaves optional ones uninstalled on a fresh instance, is idempotent on a second pass, and that `discover_manifests()` finds zero migration-name collisions — written to directly verify data-migration correctness after the rollout's final increment (Finance), not tied to any one module's own conversion; updated the same day to `_ALL_14`/`_OPTIONAL` += "goals" once Goals itself became a real module a few hours later, even though Goals isn't part of the 13-module rollout this test was originally written to verify. `test_goals_module_conversion.py` (2026-08-28, 8 tests) — Goals' own conversion-machinery coverage, deliberately core-not-nested (`tests/`, not `module_packages/goals/tests/`) matching this repo's established naming convention for this file class even though Goals isn't technically a "conversion": fresh-vs-upgrade, the real legacy-task-to-goal migration (personal + pool stores), idempotency, zero migration-name collisions, and the metric-provider registry actually discovering Finance's/Contacts' own providers once those modules are installed (and not discovering them when they aren't). `test_tags_service.py` (2026-08-29, 6 tests) and `test_tags_router.py` (2026-08-29, 4 tests) — new core coverage for `services/tags_service.py`/`routers/tags.py`, both deliberately core-not-nested for the same reason the service itself stays core (see services/ entry above). `test_module_registry.py` grew from 8 to 15 tests (2026-08-29, +7 `directional_pct` cases: increase-from-zero default, explicit start, decrease/weight-loss shape, decrease-with-no-start-value-reads-as-zero, clamping above 100 and below 0, no-target-is-zero). `test_task_service.py` grew to 20 tests (2026-08-29, +5: recurring completion-log write/undo, a non-recurring task never logging completions, and tags defaulting-empty/registering-vocabulary on create and update)
│   │
│   └── frontend/
│       └── src/
│           ├── lib/
│           │   ├── api.js         → ALL API calls go here — never fetch() directly in components; injects X-Workspace header on every request; chat.send/chat.resume take `chatId` as their first param, chat.sessions()/chat.markSessionRead() back the Chats drawer. No longer exports `assets` at all (2026-08-27, moved verbatim into `module_packages/assets/frontend/api.js` when assets/ converted) — `admin.automationToken()`/`admin.rotateAutomationToken()` moved here from the old `assets` export instead, matching the backend's own `/auth/admin/automation-token[/rotate]` URL move, since admins need token access regardless of whether Assets is installed. Also no longer exports `contacts` (2026-08-28, moved verbatim into `module_packages/contacts/frontend/api.js` when contacts/ converted) or `finance` (2026-08-28, moved verbatim into `module_packages/finance/frontend/api.js` when finance/ converted — the 79-method client, the largest of any module's own; its own internal `requestBlob()` helper was removed outright as dead code once Finance's `receiptBlob`, its last real caller, moved out too). Finance was the last module to convert in the entire rollout, so this file's own exported-client list is now stable going forward barring a future, non-Mod-Store change. Gained a new `tags` export (2026-08-29, `list: (pool=false) => get('/tags?pool=${pool}')`) — exactly that kind of change: backs `services/tags_service.py`/`routers/tags.py`, a new core service unrelated to any module conversion
│           │   ├── auth.jsx       → useAuth() hook + AuthProvider; polls /me every 30s; preferences server-only (not in localStorage)
│           │   ├── constants.js   → ALL_MODULES = CORE_MODULES (hand-maintained, must match backend require_module IDs) + moduleRegistry.js's MODULE_PACKAGES (auto-discovered, zero per-module edit needed here once converted — 2026-08-24); CATEGORY_COLORS, DEFAULT_SHORTCUTS, getShortcutsForUser(user, workspace), scoreTask(task, categoryOrder, todayStr) — JS port of priority_service.py's score_task(), used by Tasks.jsx's Priority sort mode. **CORE_MODULES is now `[]`, completely empty** (2026-08-28 — `finance` was removed first when finance/ converted, leaving just `{goals}`; then `goals` itself was removed too the same day when Goals became a real module) — the frontend mirror of the backend's `features_service.py::_CORE_MODULE_IDS` milestone, see `docs/MEMORY.md`'s 2026-08-28 entries (Finance, then Goals)
│           │   ├── moduleRegistry.js → `import.meta.glob('/src/module_packages/*/manifest.js', { eager: true })` — auto-discovers every converted module's frontend manifest at BUILD time (2026-08-24); exports MODULE_PACKAGES (each with `loadPage`/optional `block.loadComponent`, real `import()`s Vite code-splits into their own chunks) and `isPackageModule(id)`. "Discovered" (shipped in this build) is distinct from "active" (AuthProvider's `activeModuleIds`, from `GET /mod-store/active`) — App.jsx's `ModuleRoute` requires both before allowing a package module's route
│           │   ├── workspace.jsx  → WorkspaceProvider context + useWorkspace() hook; persists active workspace to localStorage
│           │   ├── theme.js       → CSS variable theme engine (accent color, dark mode, background, density, corners)
│           │   └── deepLinks.js   → MODULE_ROUTES (core routes + moduleRegistry.js's MODULE_PACKAGES merged in, same pattern as constants.js) + RECORD_PARAM (core params + each converted module's own manifest.js `recordParam` field merged in, 2026-08-25 — same treatment MODULE_ROUTES already got, added when calendar became the first converting module with a real `?event=<id>` deep link) + deepLinkUrl(module, recordId) — single source of truth for "module → route" and its record-deep-link query param, used by Layout.jsx's notification navTarget() and the dashboard's nav_button block so URL construction is never hand-maintained in two places; RECORD_PARAM's `chat: 'chat_id'` + Layout.jsx's `open_chat` navTarget case route a chat notification straight into that conversation. **Both core halves are now completely empty** (2026-08-28 — `finance` was removed from each first when finance/ converted, leaving `_CORE_MODULE_ROUTES: {goals: '/goals'}`; then `goals` itself was removed too the same day, once Goals became a real module with its own manifest.js `to`/`recordParam` fields): the hand-maintained `_CORE_MODULE_ROUTES` is now `{}` and `_CORE_RECORD_PARAM` was already `{}` before Goals (unaffected — Goals never had a `_CORE_RECORD_PARAM` entry to begin with) — every module route and record-linked deep-link param in the app is now discovered from a converted module's own manifest, none hardcoded, the frontend mirror of the backend's `_CORE_MODULE_IDS` milestone (see `docs/MEMORY.md`'s 2026-08-28 entries, Finance then Goals)
│           ├── pages/
│           │   ├── (Dashboard.jsx moved → module_packages/dashboard/frontend/Dashboard.jsx, 2026-08-27 — imported directly/eagerly by App.jsx, NOT via `lazy(pkg.loadPage)` like every other converted module, since it's the hardcoded, ModuleRoute-UNWRAPPED root route (`to: '/'`); the manifest.js deliberately declares no `loadPage` at all — one would just create an orphaned, never-fetched Vite chunk since App.jsx never calls it. App.jsx's own `MODULE_PACKAGES.filter(pkg => pkg.to !== '/')` keeps the generic route-generation loop from ALSO registering a second, ModuleRoute-wrapped route for "/", which would self-redirect-loop for a user with `dashboard` in their disabled set) → Custom Dashboards: single-dashboard viewer/editor (?id= deep link, falls back to the resolved default), view/edit mode, delegates rendering to components/dashboard/; a templated dashboard (`current.template_id`) hides "+ Add Block" and passes `blocksLocked` through to DashboardGrid/BlockRenderer since its block set is template-controlled — only layout stays freely editable; "+ New Dashboard"/empty-state both open CreateDashboardModal.jsx instead of creating blank directly; renders DashboardHero.jsx above the grid whenever `current.subject` is present
│           │   ├── (Tasks.jsx moved → module_packages/tasks/frontend/Tasks.jsx, 2026-08-25 — personal task management (flat list, no category headers — sorted by a "Sort by" control: Priority score via lib/constants.js's scoreTask(), Date/Time, or Alphabetical, remembered in localStorage; filter tabs, edit modal, household assigned tasks); route stays /tasks; `?task=` deep link opens that task's edit modal (mirrors Assets.jsx's `?asset=` pattern), now via the module's own `recordParam: 'task'` field on its manifest.js)
│           │   ├── (Goals.jsx moved twice — first → module_packages/tasks/frontend/Goals.jsx, 2026-08-25, a purely nav-id-hosted page filtering tasks where type='goal'; then, 2026-08-28, the OLD file DELETED outright and a real replacement built at → module_packages/goals/frontend/Goals.jsx once Goals became its own independent module — originally shipped as a Goals/ME tab switcher splitting by DEPTH ("Goals" = flat operational list at any depth, "ME" = root-level goals only); **reworked 2026-08-29 (Round 2) to split by OWNERSHIP instead**, after the owner reported "no idea where the pool goals are" (Round 1 merged pool goals invisibly into one list, identifiable only by an unwatched `_owner` field): "ME" now shows ALL the caller's own personal goals at any depth, and a second tab labeled "Household"/"Team" per `useWorkspace()` shows that pool's goals — hidden entirely via `useAuth()`'s `activeModuleIds`/`user.disabledModules` when the pool module itself isn't installed/enabled (not merely when the viewer lacks `pool_edit` — a member without write access still sees it read-only, matching pool tasks/notes today); category-grouping headers and the tab switcher's old descriptive text were both removed as redundant (each goal already shows its own category badge); due_date is no longer required, so undated goals always show regardless of the timeline window, a real UX change from the old mandatory-due-date version; route stays /goals, now flows through the generic MODULE_PACKAGES loop like every other converted module instead of App.jsx's old hardcoded import+route) → also new `module_packages/goals/frontend/GoalModal.jsx` (the detail/drill-down, mirrors AssetModal/AssetView's own drill-down-by-reopening pattern rather than an inline expanding tree: metric/progress + on-pace indicator, stacked Subgoals + Linked Tasks sections, a "+ Subgoal" action that reopens this same component recursively in create mode with parent_id preset, a "+ Task" quick-create via the shared TaskModal's new defaultGoalId/goal_id prop AND an existing-task picker via TaskPicker — more flexible than Assets' own create-only precedent for linked records, an explicit owner choice; delete is a real popup with two independent cascade/delete_linked_tasks checkboxes; **2026-08-29: wraps its own top-level return in a local `Portal` helper (`createPortal` to `document.body`) so nested modals — its own recursive "+Subgoal" GoalModal, its "+ Task"/"Link existing" flows, its delete-confirmation popup — no longer clip against the outer modal's own `backdrop-blur` containing block; gained a `poolTaskApi(workspace)` helper + two small wrapper components, `TaskLinkPicker`/`PoolAwareTaskCreateModal`, so linking/creating a task from a POOL goal now targets the correct household/team task store instead of always the caller's personal one; gained a Tags field (reuses `components/TagInput.jsx`) + tag pill display/click-to-filter in view mode**) and `MetricPicker.jsx` (provider dropdown + dynamically-rendered config fields via a local kind-dispatch switch modeled on BlockPicker.jsx's own internal renderField, deliberately a separate small copy rather than exporting/reusing that unexported function for an unrelated config shape) and (2026-08-29) `GoalPicker.jsx` (123 lines — search-autocomplete for linking an EXISTING goal as a subgoal/re-parenting, mirrors TaskPicker.jsx's shape; its own `subtreeIds()` client-side-ports the backend's `collect_subtree_ids` cycle-guard to exclude a goal and its own descendants; shows a match's current parent inline since re-parenting an already-parented goal is explicitly allowed, not just root-level linking) — see module_packages/ entry above and docs/MEMORY.md's 2026-08-28 and 2026-08-29 entries (Goals) for the full writeup. **Round 3 (2026-08-29):** `GoalModal.jsx` gained `toggleTaskCounts(taskId, current)` (mirrors `unlinkTask`'s own pool-aware API resolution pattern) and its linked-tasks list item was restructured — each recurring task now shows `(recurring — N% / 30d)` next to its title (the new `recurring_rate` field off `GET /goals/{id}`) plus a "Counts toward this goal's progress" checkbox, `checked={t.counts_toward_goal !== false}` so a missing field renders checked/on (matching the backend's own default-true fallback — an earlier draft used `?? false` instead, which would have shown every pre-existing linked recurring task as unchecked despite still counting server-side, caught and fixed before shipping); both the badge and checkbox only render for `t.type === 'recurring'`
│           │   ├── (Chat.jsx moved → module_packages/chat/frontend/Chat.jsx, 2026-08-26, 1339 lines — the largest single page file of any converted module so far. AI chat: plan/auto/research modes, proposal cards, step trace, memory save; multi-conversation — state keyed by `chat_id`, the "Chats" drawer (fed by `chat.sessions()`) opens a conversation directly on click (no preview step) with active/unread indicators per row, cleared on open; `newChat()` starts a fresh `chat_id`; switching conversations doesn't block on one still running; route stays /chat, `?chat_id=` deep link now via the module's own `recordParam: 'chat_id'` field on its manifest.js; owns no dashboard block type — the first converted module to declare `blocks: []`)
│           │   ├── (Calendar.jsx moved → module_packages/calendar/frontend/Calendar.jsx, 2026-08-25 — month grid, events + dated tasks overlay, EventModal; route stays /calendar. New `poolModuleVisible` gate — same stacked installed+active check ModuleRoute/Hosting.jsx already use — hides the household/team pool-events toggle entirely when that pool module isn't installed/active for the viewer, rather than showing a button that silently no-ops; `?event=` deep link opens that event's edit modal, now via the module's own `recordParam: 'event'` field on its manifest.js, merged into deepLinks.js's RECORD_PARAM the same way MODULE_ROUTES already merges `to`)
│           │   ├── (Household.jsx moved → module_packages/household/frontend/Household.jsx, 2026-08-25 — household hub (personal workspace): shared task pool (all read/write), shared events (admin write); route stays /household, imports its own `shared` client from ./api.js)
│           │   ├── (Team.jsx moved → module_packages/team/frontend/Team.jsx, 2026-08-25 — business team hub (business workspace): shared task pool, shared events — mirrors Household but on _team pool; route stays /team, imports its own `team` client from ./api.js)
│           │   ├── (Notes.jsx moved → module_packages/notes/frontend/Notes.jsx, 2026-08-26 — markdown notes with folder tree, auto-save, create/delete/move, sharing UI (NoteShareModal); route stays /notes, `?path=` deep link now via the module's own `recordParam: 'path'` field on its manifest.js. `NoteEmbedBlock` extracted from `blocks.jsx` into its own file; the block's `configSchema` also moved off `blockRegistry.js`'s static `CONFIG_FIELD_SCHEMAS` onto the manifest's own block entry — the first record-linked block config to actually exercise that merge mechanism, which existed but had never been used by a real converted module before)
│           │   ├── Brain.jsx      → browse + edit user's Brain markdown files directly
│           │   ├── Profile.jsx    → thin self-view: fetches the user's self-contact via /contacts/me, renders module_packages/contacts/frontend/ContactDetail.jsx (read) / ContactModal.jsx (edit) with a local view/edit mode switch — Profile.jsx itself stays core/`pages/`, deliberately NOT gated by `ModuleRoute` on the contacts module in App.jsx (matching `/contacts/me`'s own always-available design), yet reaches directly into the Contacts module package's own components — a deliberate, precedent-consistent choice made when contacts/ converted (2026-08-28), not an architectural violation; its own `contactsApi.me()` call also moved to import from `module_packages/contacts/frontend/api.js`
│           │   ├── (Automations.jsx moved → module_packages/automations/frontend/Automations.jsx, 2026-08-25 — Workflows|Inbox views: n8n workflow cards (import/run/logs) + Automation Inbox (item review actions, named-inbox chips, settings modal, ?view=inbox deep link); route stays /automations)
│           │   ├── (Assets.jsx moved → module_packages/assets/frontend/Assets.jsx, 2026-08-27 — template-driven object tree (expand/collapse, filters, archived toggle), both workspaces; route stays /assets, `?asset=` deep link now via the module's own `recordParam: 'asset'` field on its manifest.js — the exact pattern Tasks.jsx's own `?task=` deep link already mirrored. App.jsx's hardcoded `/assets` route + `Assets` import both removed outright — handled entirely by the pre-existing generic `MODULE_PACKAGES.filter(pkg => pkg.to !== '/').map(...)` loop, no special case needed, unlike Dashboards' own root-route exception)
│           │   ├── (Finance.jsx moved → module_packages/finance/frontend/Finance.jsx, 2026-08-28, 612 lines — book chips, Overview (balances + monthly summary) | Transactions (filters, add/edit, Transfer kind + TransferEditModal for linked-pair rows) | Budgets | Recurring | Invoices | Reports views, both workspaces; deep-link `?book=&view=` now via the module's own `recordParam: 'book'` field on its manifest.js; route stays /finance — App.jsx's hardcoded `import Finance` + `<Route path="/finance">` both removed outright, handled entirely by the pre-existing generic `MODULE_PACKAGES.filter(pkg => pkg.to !== '/').map(...)` loop, an unremarkable normal route like Assets'/Contacts' own. This is the 13th and FINAL module page to move into module_packages/ — see docs/MEMORY.md's 2026-08-28 entry (Finance) for the full writeup)
│           │   ├── (Contacts.jsx moved → module_packages/contacts/frontend/Contacts.jsx, 2026-08-28 — Contacts (CRM): list + search, alphabetical with A-Z jump strip, detail (fields/interactions/deals/money — company vs. person fields differ, see ContactModal.jsx), ContactModal, CSV import/export; both workspaces; route stays /contacts — App.jsx's hardcoded `import Contacts` + `<Route path="/contacts">` both removed outright, handled entirely by the pre-existing generic `MODULE_PACKAGES.filter(pkg => pkg.to !== '/').map(...)` loop, an unremarkable normal route unlike Dashboards' own root-route special case)
│           │   ├── Help.jsx        → Help & Guide page: fetches /help/content; TOC chips, per-section cards (blurb/how-to/tips), search, "only my modules" filter, What's New, FAQ, Contact & Support (mailto); hash-scrolls to #section from ⓘ deep-links
│           │   ├── (Home.jsx moved → module_packages/home_assistant/frontend/Home.jsx, 2026-08-24 — entity tiles by domain, scenes panel, HA automations, favourite stars; directory renamed from home/ same day; route renamed /home → /home-assistant later the same day, alongside the AI tool names and the home_favourites → home_assistant_favourites dashboard block type)
│           │   ├── Settings.jsx   → Settings drill-down menu (icon+label rows, no inline cards): Profile/Appearance/Notifications/Shortcuts(mobile)/Account rows + admin-only "Admin Settings" row
│           │   ├── settings/      → Settings sub-pages, one route per row (each with its own "← Back" header via components/settings/SettingsPageHeader.jsx):
│           │   │   ├── Appearance.jsx    → dark mode, accent color, background presets/upload, density, corners
│           │   │   ├── Notifications.jsx → ntfy channel+rotate, Web Push, Proactive Suggestions (merged from 3 former cards)
│           │   │   ├── Shortcuts.jsx     → shortcuts editor — drives both mobile's bottom bar AND (2026-08-18) desktop's sidebar "Pinned" section from the same per-workspace list; no longer mobile-only, row shown on every screen size in Settings.jsx
│           │   │   ├── Account.jsx       → timezone, "Your Brain" link (→ /brain), export Brain zip
│           │   │   ├── AdminMenu.jsx     → Admin Settings drill-down menu (admin-only): Users & Roles/AI/General/Team/Household/Hosting/Contact Fields/Mod Store rows
│           │   │   └── admin/            → one page per Admin Settings row (relocated from the old Admin.jsx + AiUsage.jsx, not rewritten):
│           │   │       ├── Users.jsx           → user list; "+ Add User" and "Role Definitions →" rows
│           │   │       ├── NewUser.jsx         → create-user form as its own page
│           │   │       ├── UserDetail.jsx      → per-user page: role, feature role, workspace access, pool-edit grants, module overrides, bank connection (claim/reveal/sync/disconnect), delete (routes to UserDeletionReview when the user owns shared items)
│           │   │       ├── UserDeletionReview.jsx → delete-review page: per-item transfer-to-user / transfer-to-pool / delete decisions (bulk-default + per-item override) for everything the user owns that's already shared, plus a read-only "will also lose access to" section
│           │   │       ├── RoleDefinitions.jsx → custom feature-role editor (create/edit/delete + module toggles)
│           │   │       ├── Ai.jsx              → AI Provider + Web Search + AI Usage & Limits (month picker, stat cards, defaults, per-user limits) — 3 sections, one page
│           │   │       ├── General.jsx         → Registration, Workspace visibility, Session Length (single global admin-set value, no per-user override), Updates
│           │   │       ├── Team.jsx            → Team pool priorities + PoolBankConnections (pool="team")
│           │   │       ├── Household.jsx       → Household pool priorities + PoolBankConnections (pool="household") — Home Assistant's config form moved to Hosting.jsx, 2026-08-24, see docs/MEMORY.md
│           │   │       ├── Hosting.jsx         → Hosting (domain/proxy/tunnel) + Managed Hosting (Infisical, self-hides when unconfigured) + n8n Automation + automation token reveal/rotate (calls `adminApi.automationToken()`/`rotateAutomationToken()` since 2026-08-27, moved off the old `assetsApi.*` calls to match the backend's own `/auth/admin/automation-token[/rotate]` move — this card works regardless of whether Assets is installed) + Home Assistant config (URL+token, gated on the home_assistant module's installed state — the first section here with that gate, since Automations/n8n hasn't converted yet)
│           │   │       ├── ModStore.jsx        → Mod Store (2026-08-24): GET /mod-store/catalog+/installed+/active drive 7 explicit per-row states (rowState()) crossing the `installed` marker with the `active` (really-registered) signal; a page-level "Restart Now" banner appears whenever any row is pending either direction; confirm-before-uninstall + the online-users-before-restart warning both use plain window.confirm() (matches UserDetail.jsx's existing convention, not a custom modal)
│           │   │       └── ContactFields.jsx   → Contacts custom-field admin authoring screen (mirrors TemplateManager.jsx's add/edit/reorder/delete pattern) — key/label/type/select-options + a Person/Company `applies_to` checkbox pair, guards against unchecking the last remaining value; the only frontend caller of `PUT /contacts/fields` (none existed before this)
│           │   ├── Login.jsx      → login + register form
│           │   └── Setup.jsx      → first-time setup wizard (Personal/Business profile, priorities, timezone)
│           └── components/
│               ├── Layout.jsx     → root shell: sidebar nav, user menu, theme toggle, module access guard; notification navTarget() has an `open_chat` case (deepLinks.js's `chat` route) so a chat-completion/approval notification opens straight into that conversation
│               ├── settings/      → shared Settings/Admin building blocks: MenuRow.jsx (icon+label+subtitle+chevron row), SettingsPageHeader.jsx ("← Back" + title), PriorityList.jsx (shared by admin/Team.jsx + admin/Household.jsx), PoolBankConnections.jsx (per-pool SimpleFIN: read-only summary of members' accounts mapped in, plus a real connect/reveal/sync/disconnect + mapping UI for a bank account owned by the pool itself — independent instance per pool)
│               ├── TaskModal.jsx  → create/edit task form (title, category, type, recurrence, due date/time, assigned_to, linked asset); stays a core shared component (Household.jsx/Team.jsx/AssetModal.jsx/module_packages/calendar's own Calendar.jsx all use it, plus module_packages/goals/frontend/GoalModal.jsx since 2026-08-28) — imports `tasksApi` from `module_packages/tasks/frontend/api.js` since 2026-08-25, same cross-package pattern EventModal.jsx already uses for calendar. `TYPES` lost `'goal'` and the `goal_requires_due_date`-mirroring client-side validation (`form.type === 'goal' && !form.due_date`) + the conditional "Target Date (required)" vs "Due Date (optional)" label logic were both removed (2026-08-28, Goals becoming a real module), simplified back to a single always-optional "Due Date"; gained a new `defaultGoalId`/`goal_id` prop+field mirroring the existing `defaultAssetId`/`asset_id` exactly, so GoalModal's own "+ Task" quick-create can preset the link. (2026-08-29) Gained a Tags field (`components/TagInput.jsx`, suggestions from the new `tags` export on `lib/api.js`, `pool` inferred from whether a `saveApi` override was passed in — the same tell GoalModal's own pool-vs-personal linking uses); `saveApi` prop (already existed) is now also what `GoalModal.jsx`'s `PoolAwareTaskCreateModal` wrapper uses to pass a resolved household/team client through for a pool goal's "+ Task" quick-create
│               ├── (AssetModal.jsx moved → module_packages/assets/frontend/AssetModal.jsx, 2026-08-27, 971 lines — the largest single file moved by any conversion so far — asset modal: opens an existing asset in read-first view (AssetView), Edit flips to the editor (dynamic template fields, attachments, share/hide selectors, history, 3-choice archive, delete/convert); auto-flips create→edit; a blank asset's `CustomFieldsEditor` now authors typed field defs (same 6 types Templates use, key auto-slugified from the label) + their values together, one row at a time, plus a "Save as template" action (`SaveAsTemplateModal`) that creates a real Template from those defs and attaches it back (2026-08-18); imports its own `assets` client from ./api.js; **2026-08-29: its nested `<TaskModal>` (rendered when `showTaskModal` is true) now renders through `createPortal(..., document.body)`** — the identical latent nested-modal-clipping bug found and fixed in GoalModal.jsx's own recursive nesting the same round (`.modal-card`'s `backdrop-blur-sm` creates a containing block for `position: fixed` descendants); this file has 2 OTHER instances of the identical pattern (`archivePrompt`, `SaveAsTemplateModal`) deliberately left unfixed this round, see `docs/TASKS.md`'s Product Backlog)
│               ├── (AssetView.jsx moved → module_packages/assets/frontend/AssetView.jsx, 2026-08-27, 526 lines — read-only asset overview: header, attachments, fields as label/value pairs, notes, child list (drill-in), linked tasks, sharing summary, history; ✎ Edit button (owner/editor only))
│               ├── assetDisplay.jsx → shared asset display helpers (no circular import): AttachmentThumb (image attachments open in a new in-app `ImageLightbox` — `.modal-overlay`-based, closes on ✕/backdrop/Escape — instead of a new browser tab, using the blob URL already held for the thumbnail; non-image files keep the existing new-tab open; the single shared component behind both AssetView.jsx's own attachment section and the dashboard Documents block), ImageLightbox, FieldInput, CapsSelector (contribute caps checkbox panel), formatChanges(), fieldDisplay(). Stays core, unmoved by assets/'s 2026-08-27 conversion — real cross-package consumer of both, not name-pattern coincidence: shared by `AssetView.jsx`/`DocumentsBlock.jsx`, both now living in `module_packages/assets/frontend/`; imports its own `assetsApi` from `module_packages/assets/frontend/api.js` since 2026-08-27, same cross-package pattern EventModal.jsx/TaskModal.jsx already established for calendar/tasks
│               ├── dashboard/     → Custom Dashboards UI: DashboardGrid.jsx (react-grid-layout wrapper — 36 cols/24px rows on desktop, 12 cols on mobile (MOBILE_COLS export), both breakpoints independently drag/resize-editable (`layout.lg`/`layout.sm` are genuinely separate saved layouts) with `compactType={null}` + `preventCollision={true}` so a block stays exactly where it's dropped/resized including snug side-by-side, instead of auto-shuffling neighbors; touch dragging is hand-rolled (press-and-hold ~550ms arms a block via a delegated capture-phase `touchstart` interceptor, since react-draggable itself has no start-delay concept — a plain swipe scrolls the page normally instead of grabbing a block; resize is untouched, still instant on the small corner handle) — real on-device iOS PWA verification of the *feel* is still the open item, mouse dragging is unaffected; `blocksLocked` prop, threaded straight through to BlockRenderer.jsx, doesn't touch any of this — a templated dashboard's layout stays exactly as draggable/resizable as a freeform one), BlockRenderer.jsx (dispatch by type + locked placeholder + "✎" edit-config button gated on `isConfigurable()`; threads an `onAction` callback to every block, a no-op for passive types; passes `actions={block.config?.actions}` through on both render paths so any `recordKind`-opted block can render its own configured buttons; the icon+label header — and blocks flagged `chromeless: true` get a small ✎/✕ corner overlay instead — are both edit-mode-only, zero reserved space in view mode, "makes it look cleaner" per 2026-08-06 owner request; a `locked` prop additionally suppresses just the ✎/✕ buttons, for a templated dashboard whose block set isn't locally editable; a `shape` lookup off blockRegistry.js — `'list'` drops the padded/bordered `.card` wrapper entirely for genuinely list-shaped blocks, default `'detail'` keeps today's card look — content-aware styling instead of every block getting identical box chrome regardless of what's inside it), DashboardHero.jsx (identity banner for a subject-bound dashboard — avatar/icon + name + the dashboard's own template label, reading the `subject`/`template_label` fields `GET /dashboards/{id}/render` now returns; renders nothing for a freeform or subject-less dashboard), blockRegistry.js (frontend render-component map mirroring the backend registry, plus `CONFIG_FIELD_SCHEMAS` — the per-block-type config field list keyed by picker `kind`, with an optional `showIf: {key, equals}` for fields that only apply to one branch of another field's value; several record-linked labels spell out their actual data source, e.g. "Contact's Deals", "Asset's Linked Contact"; `recordKind` (task/asset/contact/event/note, from `actionKinds.js`) opts a block into the generic action-button system — set on every `shape:'list'`/`shape:'detail'` block whose rows resolve through that module's own standard API by plain id; deliberately unset on `household_tasks`/`team_tasks` (formerly the single shared `pool_tasks`, split 2026-08-25) since their rows route through each module's own `shared`/`team` client by `_source`, not `tasksApi`; `contacts_list` (added 2026-08-15, the first general contacts-list block), `linked_deals`, and `linked_assets` entries removed from this file's hardcoded `BLOCK_REGISTRY`/`CONFIG_FIELD_SCHEMAS` dicts when contacts/ converted (2026-08-28) — now sourced from `module_packages/contacts/manifest.js`'s own `blocks` array via the pre-existing `MODULE_PACKAGES` merge loop; `custom_fields`'s own entry was LEFT UNCHANGED, still hardcoded here — the frontend mirror of the backend decision to keep its resolver in core; re-exports `ACTION_MODULE_BY_KIND`/`ACTION_PRESETS_BY_KIND` from `actionKinds.js`), actionKinds.js (small shared constants — action-kind→module map + per-recordKind preset action lists — pulled out of blockRegistry.js/blocks.jsx into its own file specifically so both can import it without a circular import, since blockRegistry.js already imports render components from blocks.jsx), blocks.jsx (30+ block presentational components, including `StatusButtonBlock` — the first write-capable block; it calls each target module's own existing endpoint directly, e.g. `tasksApi.update`/`contactsApi.update`(contact fields)/`assetsApi.archive`, never a dashboard-specific write path; it and `NavButtonBlock` render as a bare `.btn-pill` — no surrounding text — so several fit in a small space, and both correctly show a config'd custom `label` now (2026-08-06 fix: the backend resolvers never forwarded it); `runStatusAction`/`ActionButton`/`BlockActionButtons` are the generic action-button trio reused by every `recordKind`-opted block — `BlockActionButtons` renders once per row for list-shaped blocks and once for the whole block for detail-shaped ones, reusing `nav_button`/`status_button`'s exact click logic rather than a new write path; `TaskRow` (exported, 2026-08-25) is the same kind of genuinely cross-cutting helper — Tasks' own 4 block components import it back from here (`Top3TasksBlock`/`DueTodayBlock`/`GoalsProgressBlock`/`SingleTaskBlock`, all moved to `module_packages/tasks/frontend/`), but so did `LinkedTasksBlock` (Assets' own block, core at the time — moved out to `module_packages/assets/frontend/LinkedTasksBlock.jsx` when assets/ itself converted 2026-08-27, still importing `TaskRow` back from here cross-package, the same direction Calendar's/Tasks' own extracted blocks already established), discovered only once ESLint caught the leftover reference after the first draft tried moving `TaskRow` into the tasks package too — that would have created a real circular import (`TaskRow.jsx` needs `BlockActionButtons` from here, `blocks.jsx` would've needed `TaskRow` from there), so it stays, same reasoning `BlockActionButtons` itself stays for; `ContactsListBlock` (added 2026-08-15, moved out to `module_packages/contacts/frontend/ContactsListBlock.jsx` when contacts/ itself converted 2026-08-28, alongside 2 new extractions `LinkedDealsBlock.jsx`/`LinkedAssetsBlock.jsx`, importing `BlockActionButtons` back from here cross-package — same direction Calendar's/Tasks'/Assets' own extracted blocks already established; `CustomFieldsBlock` stays defined here in core, mirroring its backend resolver staying in core `_custom_fields.py`); `UpcomingEventsBlock`/`SingleEventBlock` moved out → `module_packages/calendar/frontend/{UpcomingEventsBlock,SingleEventBlock}.jsx`, 2026-08-25, importing `BlockActionButtons` back from here — generic cross-block infra, safe to import cross-package, same as `blocks.jsx` itself never moving; `Top3TasksBlock`/`DueTodayBlock`/`StreaksBlock`/`GoalsProgressBlock`/`SingleTaskBlock` moved out the same way → `module_packages/tasks/frontend/*.jsx`, 2026-08-25; `PoolTasksBlock` (formerly here, unowned, "conceptually Household/Team's, not Tasks'") deleted the same day and split into `module_packages/household/frontend/HouseholdTasksBlock.jsx` and `module_packages/team/frontend/TeamTasksBlock.jsx` once Household/Team themselves converted, each keeping its own small local `priorityDot`/`Empty` helper copy rather than sharing — same trivial-duplication precedent as `Empty` itself across the calendar/tasks block files; new `module_packages/household/frontend/HouseholdGoalsBlock.jsx`/`module_packages/team/frontend/TeamGoalsBlock.jsx` (2026-08-28) joined their own `*TasksBlock.jsx` siblings the same way once Goals became a real module — gated `module="household"`/`module="team"` (not `"goals"`), mirroring `household_tasks`/`team_tasks`' own established precedent exactly rather than living in Goals' own package (see module_packages/ entry above); `DocumentsBlock`/`LinkedTasksBlock`/`LinkedContactBlock`/`MyAssetsSummaryBlock`/`CollectionBlock` (+ its own private `CollectionStatusControl`/`fieldSummary` helpers) moved out the same way → `module_packages/assets/frontend/*.jsx`, 2026-08-27, when assets/ itself converted — `LinkedTasksBlock` importing `TaskRow` and `CollectionBlock` importing `BlockActionButtons` (+ its own new `frontend/api.js`) both back from here cross-package, the same direction Calendar's/Tasks' own extracted blocks already established), BlockPicker.jsx ("+ Add Block" catalog + config form; dual add/edit mode via an optional `editingBlock` prop; renders a real search/tree/select picker per field `kind` — never a raw id/path text box; filters fields by `showIf` before rendering; a search input filters the type-grid step client-side by label, so 30+ tiles across 4 categories stay findable; `templateMode`/`subjectType` props — set only when DashboardTemplateManager.jsx reuses this same component to edit a *template's* block — add a "$subject" checkbox on any contact/asset-kind field matching the template's own subject type, storing the literal sentinel instead of a picked id; new `ActionsEditor` repeater — modeled on `LocationsEditor`'s add/remove/edit shape — appears whenever the selected block type has a `recordKind`, letting the user author/reorder the block's own `actions[]` config), ModuleAndRecordPicker.jsx (nav_button's target picker: pick a module page — or Settings, a hand-added extra option since it isn't a real module — then optionally narrow to one specific section/sub-page (`MODULE_SECTIONS` in `lib/deepLinks.js`, e.g. Finance's Budgets tab or a specific Settings page) or one specific record, mutually exclusive; `dashboard` is a record-linkable module too via `DashboardPicker.jsx`, so a nav button can jump to one particular dashboard by id rather than only ever landing on your current default), DashboardPicker.jsx (nav_button's dashboard-record picker — a `<select>` over `dashboardsApi.list()`, same base shape as `finance/FinanceBookPicker.jsx`, only ever offers dashboards the viewer can already see; options are grouped into native `<optgroup>`s by each dashboard's `_template_label`, mirroring DashboardSwitcher.jsx's own grouping off the same server-denormalized field), CreateDashboardModal.jsx (new-dashboard flow: blank or from-a-template; picking a template with a `subject_type` shows the matching ContactPicker/AssetPickerField before creating — replaces the old direct blank-create calls in Dashboard.jsx's empty state and DashboardSwitcher.jsx's "+ New Dashboard"), DashboardTemplateManager.jsx (Dashboard Templates CRUD UI, structurally mirrors TemplateManager.jsx — list/edit/share views, own/global/shared sections, admin gating for global — but the per-template form manages a *block list* via BlockPicker.jsx in `templateMode` instead of field definitions; no grid/layout editing here by design, see dashboards_service.py's sync mechanism for why), TemplatePicker.jsx (the Collection block's "show records from" field — plain `<select>` over `assetsApi.listTemplates()`, picks a whole template/kind-of-record, not one asset instance), TemplateFieldsPicker.jsx (the Collection block's "fields to show" — checkbox multi-select over a chosen template's own field keys), AssetSelectFieldPicker.jsx (pick one of a source's own select-type template fields, then one of that field's own options — both picked, never typed; source is either one specific asset via `assetId` (status_button's "set an asset field" action, reads the asset's embedded `_template`) or a whole template directly via `templateId` (the Collection block's "status field" config, which has no single asset instance to read from) — exactly one of the two props is passed by any given caller), ContactFieldPicker.jsx (status_button's "update contact data" picker: same two-stage pick-field-then-pick-value shape, but a hardcoded field list — `gender`/`marital_status` — since Contacts have no per-instance template to read options from, and most other seemingly-pickable Contact fields are either free text or private), ContactNumberFieldPicker.jsx (new 2026-08-28, for Goals' metric-provider mechanism — the config picker behind the `contacts:number_field` provider, letting a goal target a number-type Contacts custom field; needs no assetId/templateId-style dependency the way AssetSelectFieldPicker.jsx below needs, since Contacts' custom field DEFINITIONS are global/instance-wide rather than per-record — genuinely simpler; the original plan assumed one shared component would serve both this and a hypothetical future Assets number-field picker, but Assets' fields are per-template while Contacts' are global, different enough data-fetching shapes that they needed separate components in practice — a real, honest deviation from the plan, this one shipped Contacts-only, not shared), DashboardAccessModal.jsx (sharing + share_underlying_data toggle; target picker is a real `<select>` of members/household/team/roles via `GET /dashboards/members`+`/roles`, not free text), DashboardSettingsModal.jsx (per-dashboard options: rename, change icon via EmojiPicker, Share/Set as default/Delete — replaces what used to be a loose row of buttons in the edit toolbar, both to give dashboards an actual settings surface and because that wider row was overflowing awkwardly on mobile; for a templated dashboard, also shows its template's label, a "Change subject" inline Contact/AssetPickerField, and "Detach from template" — the escape hatch that freezes current blocks/layout in place as an ordinary freeform dashboard), DashboardSwitcher.jsx (searchable dashboard picker; groups by `_template_label` into collapsible sections with an "Other" bucket for ungrouped dashboards whenever there's no active search query — a live query flattens back to a plain filtered list; "+ New Dashboard" opens CreateDashboardModal.jsx, a "Templates" button opens DashboardTemplateManager.jsx), LockedBlockPlaceholder.jsx. **2026-08-27, dashboard/ converted**: 6 of these moved into `module_packages/dashboard/frontend/` — CreateDashboardModal.jsx, DashboardAccessModal.jsx, DashboardSettingsModal.jsx, DashboardSwitcher.jsx, DashboardTemplateManager.jsx, DashboardHero.jsx (each real, verified-by-import-grep to be reached only from `pages/Dashboard.jsx` or another Dashboards-owned file). Everything else in this directory stays core — confirmed by direct grep of every real importer, not name-pattern-guessing: DashboardGrid.jsx/BlockRenderer.jsx/blockRegistry.js/blocks.jsx/actionKinds.js/LockedBlockPlaceholder.jsx/BlockPicker.jsx and the 5 generic field-kind pickers are all imported by other converted modules' own block components (Chat.jsx's live preview, Calendar's/Tasks'/Notes' own block files) or are generic dispatch infrastructure with no Dashboards-specific logic. **DashboardPicker.jsx is the one non-obvious exception** — despite its name, it's not page-owned at all: reached only via BlockPicker.jsx → ModuleAndRecordPicker.jsx's `dashboard: DashboardPicker` kind-map entry (configuring a `nav_button` block whose target module is "dashboard"), never imported by `Dashboard.jsx` itself — confirmed by direct grep before assuming otherwise. The `dashboards`/`dashboardTemplates` API clients moved from `lib/api.js` into `module_packages/dashboard/frontend/api.js`; `Chat.jsx`'s live dashboard-block preview and `Layout.jsx`'s `dashboard_share` notification handling both now cross-import from there, same pattern as Calendar's EventModal/EventPicker. **2026-08-27, assets/ converted**: the 5 block components (see `blocks.jsx`'s own entry above) took `blockRegistry.js`'s 5 corresponding `BLOCK_REGISTRY`/`CONFIG_FIELD_SCHEMAS` entries with them — now supplied by `module_packages/assets/manifest.js`'s own `blocks` array via the pre-existing `MODULE_PACKAGES` merge loop, the same mechanism Notes' `note_embed` config already proved out. `assetDisplay.jsx` (AttachmentThumb/ImageLightbox/FieldInput/CapsSelector/formatChanges/fieldDisplay) stays core, unmoved — real cross-package consumer of both the now-moved `AssetView.jsx` and `DocumentsBlock.jsx`, not name-pattern coincidence. The `assets` API client moved from `lib/api.js` into `module_packages/assets/frontend/api.js`; `assetDisplay.jsx`/`AssetPickerField.jsx` both now cross-import it from there. **2026-08-28, contacts/ converted**: `LinkedDealsBlock.jsx`/`LinkedAssetsBlock.jsx`/`ContactsListBlock.jsx` (see `blocks.jsx`'s own entry above) took `blockRegistry.js`'s 3 corresponding entries with them, now supplied by `module_packages/contacts/manifest.js`'s own `blocks` array; `CustomFieldsBlock` stays defined in core `blocks.jsx`, mirroring its backend resolver staying in core `_custom_fields.py`. The `contacts` API client moved from `lib/api.js` into `module_packages/contacts/frontend/api.js`; `ContactPicker.jsx` (stays core) and 9 other cross-module consumers now cross-import it from there — see `components/contacts/`'s own entry above for the full list. **2026-08-28, finance/ converted**: `FinanceActivityBlock`/`FinanceBookReportBlock` extracted out of this file into `module_packages/finance/frontend/`, both importing `fmtMoney` back from core `components/finance/money.js` cross-package (money.js itself stays core, see that directory's own entry above) — the now-unused `fmtMoney` import at the top of this file was removed too, since nothing else here used it once these two blocks moved out; `blockRegistry.js`'s 2 corresponding entries removed, now sourced from `module_packages/finance/manifest.js`'s own `blocks` array. The `finance` API client moved from `lib/api.js` into `module_packages/finance/frontend/api.js` — the 79-method client, the largest of any module's own; see `components/finance/`'s own entry below for the full cross-module consumer list. This was the LAST module-owned block extraction in the entire rollout — every module-specific dashboard block has now either moved into its own `module_packages/<id>/frontend/` or, like `CustomFieldsBlock` above, was deliberately kept core because it spans more than one module
│               ├── (TemplateManager.jsx moved → module_packages/assets/frontend/TemplateManager.jsx, 2026-08-27, 346 lines — admin template editor: ordered typed fields (TagInput options), EmojiPicker icon, defaults, example insert)
│               ├── TagInput.jsx    → GitHub-topics-style chip input (free-text or strict selector mode); inline capped suggestion box — template options, share/hide members
│               ├── EmojiPicker.jsx → curated self-contained emoji grid popover (right-aligned) for template icons
│               ├── AssetTreePicker.jsx → foldered expand/collapse asset picker; reused by Move + create-asset parent chooser
│               ├── AssetPickerField.jsx → compact single-asset field (current selection + "Change ▾" toggles an inline AssetTreePicker) — same idiom as AssetModal's own parent picker; used by dashboard record-linked block config. Stays core, unmoved — imports its own `assetsApi` from `module_packages/assets/frontend/api.js` since 2026-08-27, same cross-package pattern EventModal.jsx/TaskModal.jsx already established
│               ├── TaskPicker.jsx, EventPicker.jsx, NotePicker.jsx → ContactPicker-pattern search-autocomplete pickers (single id/path value, graceful fallback to plain text if the list fetch fails) over the user's own tasks/calendar events/notes; used by dashboard record-linked block config so no block ever needs a raw id typed in. TaskPicker.jsx imports `tasksApi` from `module_packages/tasks/frontend/api.js`, EventPicker.jsx imports `calendarApi` from `module_packages/calendar/frontend/api.js`, both since 2026-08-25, same as EventModal.jsx/TaskModal.jsx above. TaskPicker.jsx gained an optional `listFn` prop (2026-08-29) — overrides the task source (e.g. a household/team pool client's own `.list()`), defaults to `tasksApi.list`; added for `GoalModal.jsx`'s new `TaskLinkPicker` wrapper so linking an existing task from a POOL goal searches the pool's own tasks instead of always the caller's personal ones. **A real, separate bug fixed 2026-08-29 (Round 3), found only via live reproduction (a fully isolated disposable test instance, real headless Chromium via playwright-core) — static re-reading of this file, GoalModal.jsx, and backend code all read clean:** the `matches` computation (~line 56-71) filtered by substring in raw array (creation) order and applied a hardcoded `.slice(0, 8)` with zero relevance ranking, so a recurring task sharing a word with enough older tasks could be pushed past position 8 and never appear — even searching its own exact title. Fixed with a new module-level `_matchRank(title, q)` helper (~line 4-9, exact match 0 / starts-with 1 / other substring 2) that `matches` now sorts by, and by dropping the `.slice(0, 8)` cap entirely, filtered or not — the dropdown already had `max-h-56 overflow-y-auto`, a real scroll container the cap had simply been defeating. A first pass left the cap on the unfiltered empty-query browse view; same day, the owner asked for that to scroll too ("its a convenience factor for users"), so `matches` is now just `q ? (filtered+ranked) : all`, no `.slice()` anywhere. WorkflowPicker.jsx (same pattern, over n8n workflows) moved → module_packages/automations/frontend/WorkflowPicker.jsx, 2026-08-25 — BlockPicker.jsx (core) imports it cross-module for the workflow_status block's config field, same pattern as Hosting.jsx importing a converted module's own api.js
│               ├── finance/       → money.js (cents↔display helpers: fmtMoney/toCents/centsToInput/todayStr/monthStr) and FinanceBookPicker.jsx (plain-select book picker; used by dashboard record-linked block config) — the ONLY 2 files left here since finance/ converted 2026-08-28 (everything else below moved out); both stay core, confirmed by import-grep: money.js has 3 external importers including an already-converted sibling module (`module_packages/assets/frontend/AssetView.jsx`) plus `components/dashboard/blocks.jsx`/`components/settings/PoolBankConnections.jsx`; FinanceBookPicker.jsx is reached ONLY through core dashboard block-config infrastructure (`ModuleAndRecordPicker.jsx`'s `finance: FinanceBookPicker` kind-map entry, `BlockPicker.jsx`), never by Finance's own page at all — despite its name/location, the same "DashboardPicker.jsx-shaped surprise" Dashboards' own conversion first ran into, confirmed here by direct grep rather than assumed. **2026-08-28, finance/ converted**: everything else moved into `module_packages/finance/frontend/` — TransactionModal.jsx (+tax flags+receipts+ContactPicker payee; Transfer kind swaps category/payee for a cross-book/cross-workspace To Book+To Account picker with client-side currency-match validation), TransferEditModal.jsx (edit/delete a linked transfer pair together — amount/date/notes, both legs), BookSettings.jsx (accounts/categories/tax buckets/CSV import), SimpleFinPanel.jsx (bank connect+mapping), BudgetsPanel.jsx, RecurringPanel.jsx (+planned one-offs+deductible), InvoicesPanel.jsx (597 lines, the largest moved component — invoices/payments via ContactPicker + AR + printable InvoicePrint), ReportsPanel.jsx (P&L + tax export). New `frontend/api.js` carries the 79-method `finance` client (BY FAR the largest of any module's own — Contacts' was 41, Assets' ~34) moved verbatim out of `lib/api.js` (which no longer exports `finance` at all); needed THREE hand-rolled local reimplementations — `uploadReceipt`/`csvPreview` (multipart), `receiptBlob` (protected blob fetch), and a genuinely NEW one no prior module needed, `listBooksForWorkspace` (overrides the ambient `X-Workspace` header explicitly via a small local `getForWorkspace()` helper, since admin pool pages read the pool's own fixed workspace regardless of the admin's own active toggle, and core `lib/api.js`'s exported `get()` has no way to pass extra headers). Core `lib/api.js`'s own internal `requestBlob()` helper became completely unused once Finance's `receiptBlob` (its last real caller) moved out — Assets'/Contacts' own `photoBlob`/`fileBlob` had already switched to local hand-rolled implementations during their own conversions — removed outright rather than left as dead code. 6 cross-module consumer files got their `finance` import path updated from `lib/api` to here: `module_packages/assets/frontend/AssetModal.jsx`, FinanceBookPicker.jsx itself (stays core, but its own `financeApi` import still needed updating, the same self-referential fix ContactPicker.jsx needed during Contacts' conversion), `module_packages/contacts/frontend/ContactDetail.jsx`, `components/Layout.jsx`, `components/settings/PoolBankConnections.jsx`, `pages/settings/admin/UserDetail.jsx`. 2 dashboard-block components (`FinanceActivityBlock.jsx`/`FinanceBookReportBlock.jsx`) extracted from `components/dashboard/blocks.jsx` into `module_packages/finance/frontend/`, both importing `fmtMoney` back from core `money.js` — the reverse-direction cross-package import every prior conversion's own extracted blocks needed when a shared utility stayed core; `blockRegistry.js`'s 2 corresponding entries removed, now sourced from new `module_packages/finance/manifest.js`'s own `blocks` array. See docs/MEMORY.md's 2026-08-28 entry (Finance) for the full writeup — this is the 13th and FINAL module directory to be reduced down to its shared-infrastructure remainder
│               ├── contacts/      → ContactPicker.jsx (search-first contact autocomplete + quick-create; reused by transaction payee + invoice client + affiliation/employer pickers) — the ONLY file left here since contacts/ converted 2026-08-28 (everything else below moved out); stays core, confirmed by import-grep to have 7 external importers beyond its own module's ContactModal.jsx: `module_packages/dashboard/frontend/{DashboardSettingsModal,CreateDashboardModal}.jsx` (an already-converted SIBLING module depending on it directly, the same strongest-possible-evidence pattern that kept `contacts_service.py` core on the backend), `module_packages/finance/frontend/{TransactionModal,InvoicesPanel}.jsx` (moved there 2026-08-28 when finance/ itself converted — the import path updated the same way ContactPicker.jsx's own consumers keep needing to, one sibling module at a time), `components/dashboard/{ModuleAndRecordPicker,BlockPicker}.jsx`, `components/assetDisplay.jsx` — mirrors AssetTreePicker.jsx staying core for the identical reason. **2026-08-28, contacts/ converted**: everything else moved into `module_packages/contacts/frontend/` — ContactDetail.jsx (read-first `<dl>`-grid card, shared by Contacts.jsx and Profile.jsx — self-contact "(ME)" badge, affiliated-contacts chips, career history, profile-merge field sections, `fullPage` mode, owner-only "hidden from others" badges next to a hidden section's heading), ContactModal.jsx (971 lines, the largest moved file — edit form: same profile-merge fields, private-only-when-self, affiliation picker, career history resume editor with direct past-role add/edit, photo upload, priorities reorder editor, `fullPage` mode, convert-to-pool button, per-section hide toggles via SectionHeader.jsx), ContactAvatar.jsx (shared photo-or-fallback-icon avatar + `useContactPhotoUrl()` hook — fetches the photo as an authenticated blob since a plain `<img src>` can't carry the `X-Workspace` header; used by the Contacts list, ContactDetail's header, and ContactModal's photo uploader preview; also renders the online/offline presence dot overlay and the click-to-open last-seen popover, anchored off the avatar's left edge; fires an optional `onPopoverToggle` callback so ContactRow (Contacts.jsx) can raise its own row's stacking context above sibling rows while open, 2026-08-18), BulkConvertContactsModal.jsx (checklist, defaults to everything selected — converts many of the caller's own personal contacts into the workspace pool at once, 2026-08-17), SectionHeader.jsx (section heading + optional self-contact-owner-only "Hide from others" toggle, 2026-08-18 — confirmed by import-grep to be used only by ContactModal.jsx), phone.js (shared `formatPhone(p)` display formatter — used by ContactDetail.jsx and Contacts.jsx's list-row preview; the list row previously showed the raw undashed digits, a separate bug from ContactDetail's own formatting, 2026-08-15), presence.js (`formatLastSeen()` — minutes for the first hour, hours up to a day, days uncapped after that, 2026-08-17 — confirmed by import-grep to be used only by ContactAvatar.jsx). New `frontend/api.js` carries the 41-method `contacts` client moved verbatim out of `lib/api.js` (which no longer exports `contacts` at all), hand-rolling `uploadPhoto`/`photoBlob`/`exportCsv`'s multipart/blob/header-fetch logic inline, matching automations'/assets' own established precedent (core's internal `requestFile`/`requestBlob`/`headers` helpers are deliberately not exported). 10 cross-module consumer files got their `contacts` import path updated from `lib/api` to here: Layout.jsx, `module_packages/assets/frontend/AssetView.jsx`, `components/finance/{InvoicesPanel,TransactionModal}.jsx` (these two moved on again to `module_packages/finance/frontend/` when finance/ itself converted 2026-08-28, needing this same `contacts` import path updated a second time as part of that later move), `components/assetDisplay.jsx`, ContactPicker.jsx itself (stays core, but its own `contactsApi` import still needed updating), Profile.jsx (see its own entry above), `components/dashboard/blocks.jsx`, `pages/settings/admin/NewUser.jsx`, `pages/settings/admin/ContactFields.jsx` (plus `components/dashboard/ContactFieldPicker.jsx` — both stay core, matching Team.jsx/Household.jsx and Assets' own AssetSelectFieldPicker.jsx staying core after their modules converted) — plus one additional stale import a naive grep would have missed: `module_packages/dashboard/frontend/DashboardHero.jsx` imported `ContactAvatar` directly from this old path, found via a broader sweep and fixed
│               ├── EventModal.jsx → create/edit calendar event form (title, dates, times, all_day, color, notes); stays a core shared component (Household.jsx/Team.jsx use it for pool events too) — imports its default (non-pool) `calendarApi` from `module_packages/calendar/frontend/api.js` since 2026-08-25, same cross-package pattern `blocks.jsx` uses for `BlockActionButtons`
│               ├── CalendarGrid.jsx → month view: day cells with event/task indicators, click to open detail
│               ├── HelpButton.jsx  → small ⓘ affordance next to a page title; deep-links to /help#<section>
│               ├── WhatsNewBanner.jsx → dismissible bar shown for a few days after an update (reads /help/whats-new); per-version localStorage dismiss; links to /help#whats-new
│               ├── DemoBanner.jsx → non-dismissible bar shown only when GET /auth/status reports demo_mode: true; standing data-loss disclosure ("resets nightly") + Privacy/Terms links, rendered by AuthProvider (lib/auth.jsx) above every route including /login
│               ├── GettingStarted.jsx → first-run checklist card on the Dashboard (reads/writes /help/onboarding); hides when dismissed or all steps done
│               └── ErrorBoundary.jsx → catch React render errors, display fallback UI
│
├── brain/                         → starter Brain (mounted at /data/brain in Docker)
│   ├── AGENTS.md                  → AI boot protocol (in-app AI session start order)
│   ├── SOUL.md                    → AI personality and communication principles
│   ├── USERS.md                   → user registry and selection logic
│   ├── MEMORY_MAP.md              → navigation index for all Brain files
│   ├── Memory/
│   │   └── Long_Term_Memory.md    → system-wide stable facts (shared AI context)
│   ├── USERS/_template/           → copied for each new user at setup
│   │   └── Business/              → placeholder — provisioned as empty business workspace for new users
│   ├── skills/life-priorities/    → task scoring + recurring task logic
│   ├── _system/auth.json          → user accounts, JTI blacklist (NEVER commit; volume-mounted)
│   ├── _system/features.json      → feature flags + custom role definitions (created at first setup)
│   ├── _system/migrations.json    → migration tracking (which schema migrations have run)
│   ├── _system/vapid_keys.json    → VAPID keypair for web push notifications (auto-generated)
│   ├── _system/n8n_config.json    → n8n URL + API key (written by Admin → n8n card)
│   ├── _system/ha_config.json     → Home Assistant URL + long-lived token (written by Admin → Hosting's Home Assistant card)
│   ├── _system/automations_index.json → business workflow metadata (n8n IDs + tags)
│   └── _system/ai_usage.json      → AI usage metering + caps: per-user daily buckets (messages/tokens, personal+business), defaults, mode/period/limits, alert dedup — operational metadata, never in a user's portable Brain folder
│   ├── ai_settings.json           → AI provider, model, API keys (written by Admin UI; not in git)
│   └── hosting.json               → runtime hosting config written by Admin → Hosting panel
│
├── docker/
│   ├── docker-compose.yml         → service definitions (app + ntfy + n8n)
│   ├── .env.example               → env var template
│   ├── .env                       → live secrets (NEVER commit; generated by launch.sh)
│   ├── backup.sh                  → Brain backup script (keeps 30 most recent)
│   ├── update.sh                  → in-place update + auto-rollback; --watch daemon for flag-file trigger; --cron for crontab
│   └── demo_reset.sh              → wraps `docker compose exec app python3 demo_reset.py` for a public-demo host's cron; safe no-op on any instance without DEMO_MODE=true
│
├── agent/
│   ├── README.md                  → in-app AI agent architecture: modes, tool registry, brain skills
│   └── skills/                    → pointer files for brain skills (source lives in brain/skills/)
│       └── life-priorities/       → pointer → brain/skills/life-priorities/ (task scoring + top 3)
│
└── docs/
    ├── README.md                  → docs folder overview and file table
    ├── AGENTS.md                  → AI boot protocol + dev conventions
    ├── SOUL.md                    → AI personality and principles
    ├── PROJECT.md                 → system architecture + development roadmap
    ├── TASKS.md                   → active product work queue + backlog
    ├── MEMORY.md                  → design decisions, security rules, known gotchas
    ├── MAP.md                     → THIS FILE — navigation index
    ├── API.md                     → REST API endpoint reference
    ├── TESTING.md                 → testing guide: brain fixture, patterns, coverage targets
    ├── Security-Audit-2026-07-19.md → full security audit + remediation log (passes 1–5)
    ├── Daily Notes/               → per-session work logs (YYYY-MM-DD.md)
    ├── skills/                    → dev tools for Claude Code sessions
    │   ├── README.md              → skill index and usage
    │   ├── diagnose/              → full security/architecture audit with severity levels
    │   ├── run-tests/             → run pytest + structured GREEN/RED report
    │   └── run-agent/             → CLI wrapper: send goals to the in-app AI, see tool trace
    └── hooks/
        ├── docs_loader.sh         → UserPromptSubmit hook: injects key docs at session start
        ├── docs_reminder.sh       → Stop hook: prompts doc updates at end of each turn
        ├── commit_reminder.sh     → Stop hook: reminds to commit every 30 min if changes exist
        └── safety_check.sh        → PreToolUse hook: blocks destructive Bash commands
```

---

## Key Reference Points

| What you need | Where to look |
|---|---|
| What this project is | `docs/README.md`, `docs/PROJECT.md` |
| Current priorities / tasks | `docs/TASKS.md` |
| Design decisions & hard rules | `docs/MEMORY.md` |
| Architecture & roadmap | `docs/PROJECT.md` |
| What was worked on recently | `docs/Daily Notes/YYYY-MM-DD.md` |
| Full API reference | `docs/API.md` |
| How to work on this codebase | `docs/AGENTS.md` |
| AI behavior rules | `docs/SOUL.md` |

---

## Update Rule

Any time a new file or folder is added to this repo, add it here with a one-line description before the session ends.
